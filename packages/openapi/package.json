{
  "name": "@metasheet/openapi",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "OpenAPI source merge + contract validation for MetaSheet V2",
  "scripts": {
    "build": "tsx tools/build.ts",
    "validate": "tsx tools/validate.ts dist/openapi.yaml",
    "generate:sdk": "pnpm run build && pnpm --dir dist-sdk build",
    "guard:codegen": "node tools/guard-codegen.mjs"
  },
  "devDependencies": {
    "js-yaml": "^4.1.0",
    "tsx": "^4.19.0"
  }
}
