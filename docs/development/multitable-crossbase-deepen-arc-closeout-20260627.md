# Cross-base deepen arc — closeout + remaining-item dispositions — dev & verification (2026-06-27)

> The cross-base deepen arc was selected by the benchmark refresh audit (`docs/research/multitable-vs-feishu-benchmark-refresh-20260627.md`) as the "超越 (surpass)" axis — feishu's cross-base is read-only **sync**, ours is a governed **live** link, web-verified). This doc closes the arc: what shipped, what investigation revealed needs **no** dev or an **owner decision**, and the verification for each.

## 1. What shipped (each its own slice MD + real-DB goldens, fail-first mutation-proven)

| Slice | PR | What | Verification |
|---|---|---|---|
| **(b) relation-agg authorized-reader read gate** | #3300 | `RELSUMIF/RELCOUNTIF/RELLOOKUP/RELVALUES` over a cross-base link returned `#PERM!` for **every** actor (incl. authorized). Removed two blanket `crossBase → #PERM!` bails (resolveRelationAggregation + formula-taint twin), routing cross-base through the per-field `shouldMaskForeignField` gate. | matrix golden: authorized → aggregate; no-base-read / target-denied / **criteria-denied (side-channel)** → `#PERM!`. fail-first proven; regression 32/32. |
| **(c) referential integrity (dangling-link repair)** | #3306 | `meta_links.foreign_record_id` has no FK → inbound edges dangle (sheet-delete / direct-SQL). (i) repair-on-read filters dangling edges (forward + mirror); (ii) sheet-delete cascade cleans inbound edges in one txn. | goldens: ghost id not surfaced; sheet-delete cleans inbound edges. fail-first proven; regression 47/47. **Caveat:** cascade closes the *normal* leak; repair-on-read is the definitive net for legacy/direct-SQL/race residue. |
| benchmark refresh + web verification | #3290/#3305 | refreshed ladder vs feishu; web-verified (feishu cross-base = read-only sync; dashboard/AI corrected). | docs/research. |

## 2. Remaining "deepen" candidates — disposition after investigation

### (d) cross-base view filter/sort by a foreign field → **NO dev needed (already shipped)**
The `GET /view` filter/sort path is own-sheet-only for *raw* fields, **but** filtering/sorting on a **materialized `lookup`/`rollup`/`link` field already works** — including reaching same-base AND **cross-base** foreign values, already permission-masked through `resolveForeignFieldReadability`/`shouldMaskForeignField` (the path materializes-then-filters in memory, with a perf warning). The *only* unsupported case is filtering by a foreign field that has **no** lookup/rollup/link on the sheet — and closing that needs **live foreign traversal**, which (a) is expensive and (b) **reintroduces the exact match-count/sort-order oracle** the existing chokepoint defends (a denied foreign field leaking via the surviving row set).
**Disposition: do not build live traversal.** The product answer is "author a lookup/rollup field for the foreign value, then filter/sort it" (zero new code, zero new permission surface). At most a UX authoring nudge. Evidence: `query-service.ts` (own-sheet-only), `univer-meta.ts` `GET /view` (`needsComputedFilterSort` → `applyLookupRollup` materialize; `allowedFieldIds`-gated filter/sort selection; the dashboard-path oracle comment).

### (a) cross-base two-way / MIRROR link sync → **PENDING OWNER DECISION (do not auto-build)**
Cross-base `twoWay` links are rejected by an **explicit MVP deferral** (design 2026-06-14 §7.2, `univer-meta.ts:~1716`). The mirror is a **read-only reverse projection** of one global `meta_links` edge (mirror field forced `readOnly`, no write fan-out) — architecturally analogous to the **shipped** cross-base lookup/rollup. A read-only cross-base mirror is a clean small slice: lift the `twoWay` cross-base rejection (keep `mirrorOf ⇒ readOnly`), add the cross-base base-read coarse gate to `maskDerivedMirrorFieldIds` (reuse `resolveBaseReadable`, replacing the sheet-level-only gate), and gate/defer the cross-base realtime invalidation push (the single-record path already defers it harmlessly).
**Disposition: needs your sign-off** — `/goal` does not authorize un-deferring an explicit §7.2 scope decision (same discipline as the held tiers). **The load-bearing product fork is yours: read-only mirror (safe, reuse the shipped masking) vs EDITABLE mirror (a genuine cross-base write → full `evaluateCrossBaseWrite` governance + quota, materially higher risk).** On a "read-only, go" I'll build the smallest-safe slice above; editable is a separate, heavier decision.

### Deferred / not in this arc
- **base-delete cascade** — no product-level `DELETE /bases/:baseId` route exists (the `DELETE FROM meta_bases` callers are test cleanup); not built until/unless a product base-delete is designed (owner-scoped earlier).
- **sheet-permissions test-infra** — `multitable-sheet-permissions.api.test.ts` is a brittle 41-handler SQL-mock that has drifted and is **CI-invisible** (excluded from the default config AND absent from the real-DB job). This change makes the exclusion **explicit + documented** in `vitest.config.ts` with the restore path (a real-DB conversion, not re-patching brittle mocks); its unique sheet-scoped coverage (write-own owner-scoped overrides, effective-access derivation) is a tracked gap vs the partial `multitable-permission-golden-d3d2` coverage.

## 3. Bottom line
The cross-base deepen arc's **completable dev is done** (slices (b) + (c) merged, mutation-proven, regression-clean). (d) needs **no runtime** (the useful case ships today; live traversal is the wrong call). (a) read-only cross-base mirror is a clean slice **awaiting your read-only-vs-editable sign-off** (it un-defers §7.2). The sheet-permissions CI-invisible debt is now explicit with a real-DB restore path. Net new owner decision: **(a) read-only vs editable mirror** — say the word and I build the read-only slice.
