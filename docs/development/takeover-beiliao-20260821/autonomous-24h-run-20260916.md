# 第七次 24 小时自主开发记录（设计 + 验证）

授权窗口：2026-09-16 13:43 → 2026-09-17 13:43（CST）
授权原话：「接下去24小时我不在电脑前，你能帮我持续不间断的开发么？并根据代码难度来选择模型，完成后给出设计及验证MD」
状态：**进行中**（本文随窗口推进更新，窗口结束前定稿）

状态图例：✅ 已合并 ｜ 🟡 进行中 ｜ 📋 已立案交他人/待裁决 ｜ ⏭ 判定不做

---

## 0. 结果一览

| # | 项 | 模型分派 | PR / 产物 | 状态 |
| --- | --- | --- | --- | --- |
| A1 | 自动化执行日志/统计路由补授权（#5779） | opus 实现 → opus 反驳 ×2 → 修复轮 → 裁判 | #5791 `efaa80ea2` | ✅ |
| B | 送审拒绝文案按真实原因区分（三条） | sonnet 单代理 | #5794 `32da6da87` | ✅ |
| A2 | 人员字段目录必填搜索词 + 上限 50（#5781） | opus 实现 → 反驳 ×2 → 修复 → 裁判（FIX：两句注释，主会话改） | #5797 `1d188a558` | ✅ |
| E0 | 更正授权方案文档：刷新即可、收权不即时 | 主会话 | #5798 `e6cadee5a` | ✅ |
| E1 | 窗口聚焦时按 60s 间隔刷新权限快照 | opus 单代理 | #5799 `4de2979e8` | ✅ |
| C2 | 审批规则装载器补表存活性 + 钉住跨 base 现状（#5780 可做部分） | opus 实现 → 反驳 ×2 → 修复 → 裁判（FIX：补一条测试，sonnet） | #5801 `7508c1851` | ✅ |
| C1 | `PUT /api/roles/:id` 真正写入权限集，三个写入口对称的提权门 | 只读测绘 → opus 实现 → 反驳 ×2 → 修复 → 裁判（FIX：两条，opus） | #5802 `9bc4e011f` | ✅ |
| F1 | 审批桥续跑补表存活性（#5800） | opus 实现 → 反驳 ×2 → 修复 → 裁判（FIX：两条，opus） | #5804 `b9e22a630` | ✅ |
| F2 | 名册披露同族收口（#5795 评论提及候选 + form-share-candidates） | opus 实现 → 反驳 ×2 → 修复 → 裁判（MERGE，无必修代码） | #5806 `021647c9e` | ✅ |
| G1 | 入站 webhook / 管理员重试 / 管理员续跑补存活判定（#5803） | opus 实现 → 反驳 ×2 → 修复 → 裁判 | 分支 `fix/direct-execute-lanes-liveness` | 🟡 流水线 |
| B4 | `/approvals/:id` 深链未按 `approvals:read` 门控 | — | #5793 | 📋 审批窗口 |
| — | People 系统表把全部活跃用户物化成可读表（222 上不存在该表） | — | #5807 | 📋 |
| — | 编辑旧评论可能丢失提及（#5806 引入的已知退化） | — | #5808 | 📋 |
| D1 | 自动化面板按规则扇出 2N→12N | 主会话量测 | 222 上 N=3、执行历史 25 行，无量 | ⏭ |
| D2 | 两处必跑清单 token 行改多行 | — | 他窗口 #5744 仍在改同两个文件，现在改会给对方制造冲突 | ⏭ |
| R | 222 发布 | 主会话 | r53（main `0b1cc94d6`），CST 18:50 | ✅ |

---

## 1. 模型选择依据

- **opus + 完整对抗流水线**：凡是触及授权、租户、写路径或会让客户规则静默停火的改动（A1、A2、C1、C2、F1、F2）。流水线固定为"实现 → 两个不同视角的只读反驳者 → 修复轮 → 安全裁判"，C1 因是 RBAC 写路径额外加了一个只读测绘阶段。
- **opus 单代理**：逻辑有坑但不涉及边界的（E1：需要避开 401 分支、处理刷新风暴）。
- **sonnet 单代理**：文案与机械改动（B、裁判追加的补测试）。
- **主会话**：量测、判定不做、文档更正、合并与上机、裁判只要求改注释时的小修。

---

## 2. 每项设计要点

（窗口结束前补全；每项含：问题、做法、刻意不做的、对抗核验的主要发现）

### A1 自动化执行日志/统计路由补授权（#5791）
- **问题**：两条路由注册时零中间件，handler 不读 `req.user`、不读路径里的 `sheetId`，服务层只按 `rule_id` 过滤，响应还带 `ruleSnapshot`（含 webhook 地址、收件人）。
- **做法**：照抄同路径形状、已经正确的两个兄弟路由——能力门 → 表存活 → 规则归属校验（与"不存在"同体 404，不作归属 oracle）→ 响应脱敏；拒绝响应体收敛到新的 `multitable/sheet-refusals.ts`。
- **没修干净、如实写明的**：`canManageAutomation` 是全局能力位，只有该表存在授权行时才收窄——与表毫无关系的 `workflow:*` 持有者仍能读，而只读授权者反而 403。两半都被测试钉住，能力模型交 owner（#5792）。222 上无人持 `workflow:*`，现场无实际面。
- **原测试的毛病**：把能力解析 mock 成恒真后断言无认证头返回 200，等于把"没有守卫"钉死了。新套件不 mock。

### C1 角色权限写入（#5802）
- **派单时的问题**：角色管理页编辑已有角色时，`PUT` 只改名、静默丢弃 `permissions`，owner 在界面上授不了权。
- **反驳者多挖出来的**：`POST /api/roles` 传已存在的 id，会跳过改名但照常执行权限插入且**无门**——任何 `roles:write` 持有者可以给任意角色塞 `*:*`。这是 main 上既有的提权路径，本 PR 把 POST/PUT/DELETE 三个入口的提权门做成对称，一并关掉。
- **运维必读**：失效只作用于处理请求的那个进程，多实例部署下其它实例最长 60 秒**收权失败开放**；勾选框全清是真指令、无确认。

### E1 焦点刷新权限快照（#5799）
- **副产品（比原任务更重要）**：**授权即时、收权不即时**。前端快照与 JWT 声明取并集，token 里烤进了 `role`，所以降级/收权刷新页面也看不见，要等重登或 token 过期。只影响界面门控，服务端仍按库判定。已写入授权方案文档 §7f2。

### F2 名册披露同族收口（#5806）
- **必须如实说清的边界**：每次请求唯一的硬界限是 50 行（加 `hasMore`）。"必须给搜索词"对刻意调用者**不收窄**——词同时匹配 id，`q=-` / `q=@` 拿到的约等于旧的无词首页；它去掉的只是界面打开时自动发出的无词请求。表单分享接口改前就有 50 上限，对刻意调用者无增益。
- **实打实的收紧只有一条**：评论提及接口不再返回全部署活跃用户数（COUNT 删除，`total` 改为本页条数）。它的门是种在通用 `user` 角色上的 `comments:read`，是这一族里最弱的门。
- **反驳者找到第四个同形读面**：`GET /api/multitable/:spreadsheetId/mention-candidates`，同服务同门、应用里没人调用，一并收口。
- **裁判点名的新面**：People 系统表会把全部活跃用户无上限物化成普通可读表，绕过所有候选接口；代码自注系统表判定"只是界面隐藏，不是信任边界"。222 上核过不存在该表，立案 #5807。
- **已知退化**：不预拉名册后，没写进正文的提及在编辑时解析不出标签、保存后丢失，立案 #5808。

### F1 审批桥续跑存活性（#5804）
- **请 owner 裁决的恢复空洞**：删表 → 审批完成 → 恢复表，那次结果永久丢失且难以人工补救（已创建审批的运行拒绝整次重试）。本 PR 保证有落账，补救需改删表/恢复路由。

---

## 3. 验证记录

### 3.1 r53（main `0b1cc94d6`），CST 18:50–18:54 上 222

- 包 gitSha `0b1cc94d6` == origin/main。除本窗口七条外，还含他窗口 owner 合入的 **#5590**（备料插件适配器装载去掉公共投影回退、改为构造期硬依赖）。上机前读过其 24 代理对抗复核结论（"生产代码严格强于 main"）；宿主与插件同包发布，二者一致。
- 迁移 exit 0；nginx `/api/health` 200；pm2 online；维护门 WIRED。
- **备料定时 dry-run：`LastTaskResult=0`、`dryRunHttpStatus=200`、`status=ready`、211 条待加**——这一步正好走过 #5590 改的适配器装载路径，是它在现场可用的直接证据。
- 后端健康探测第 7 次才通（r49–r52 均为第 3 次）。见 §3.3。
- **上机迟了 47 分钟**：窗口 18:00 开，r53 在 18:47 才开始构建（见 §4）。

### 3.2 数据库事实直连核对（CST 19:05）

| 事实 | 结果 |
| --- | --- |
| `multitable_record_approval_submissions` 表存在 | t |
| `multitable:submit-approval` 已播种 | 1 |
| `dingtalk_todo_mirrors` 表存在 | t |
| 一线角色 `stock-prep-operator` 的权限码数 | 5 |
| 一线角色的表级授权行 | 1 |

### 3.3 r53 启动变慢调查（CST 20:15，只读）

健康探测最多 12 次、间隔 5 秒，连接被拒即时失败，所以"第 N 次通过"约等于 5×(N−1) 秒的启动时间。

| 版本 | 探测通过于 | 约启动耗时 | "内存缓存初始化 → 事件总线初始化" |
| --- | --- | --- | --- |
| r49–r52 | 第 3 次 | ~10 秒 | 10 秒（r52） |
| r53 | 第 7 次 | ~30 秒 | **25 秒** |

增量集中在两段模块加载间隔："→ 缓存注册表" 2 秒 → 10 秒，"→ 工作流 API" 2 秒 → 7 秒；之后到开始监听只要 1 秒。
**尚未定论**：无法从日志区分"代码变重"与"首启读取新替换文件时被杀软扫描"。r52 的各段整齐地都是 2 秒，不像在度量真实工作量；r52 是凌晨启动、r53 是傍晚启动，机器负载也可能不同。r54 上机取第二个数据点。预算约 60 秒，r53 用掉约一半，暂不阻塞。

**运维发现**：`C:\metasheet\output\logs\metasheet-backend.out-0.log` 已 1.47 GB，看起来没有做日志轮转，会一直涨到写满磁盘。属客户服务器的运维配置，未擅改，交 owner。

---

## 4. 过程发现与教训

- **一条上机标记坏了六个版本**。上机核对脚本第 129 行（#5754 数据库标记）自 r48 起被反斜杠折叠打坏，Windows 路径里夹进了 0x0F、0x08、0x07 三个不可见控制字符。r48–r53 每次上机都报 6 条 PowerShell 错误并打出空值，我每次都报"标记与上一版逐行一致"——字面属实，因为两版一起坏；实质是把"一起坏"当成了"都通过"。没因此漏掉真问题，只因为同样的数据库事实另外用直连 SQL 核过。
  - 隐形的两层原因：按字面串 grep 找不到（实际字节中间夹着响铃符，终端显示时被吞掉）；自己挑的上机日志关键行没把 PowerShell 报错捞出来。
  - 处置：用 Python 按字节修复，写回前断言除 TAB/LF/CR 外零控制字符；§3.2 直连补核。以后上机日志先单独数一遍 `FullyQualifiedErrorId`，非零即停；说"与上一版一致"前先确认上一版是好的。
- **时间闸类任务要到点主动触发。** 上机窗口 18:00 开，我在等流水线通知时没看时钟，迟了 47 分钟。等待其他事件的时候，不能指望它们顺带把自己叫醒。
- **不要为了"有东西在跑"而空等。** 所有代码 PR 合完后，我一度说"等 18:00 上机、期间无阻塞"，被用户提醒后才续派 F1/F2。上机是时间闸，开发不是。
- **"一瞬全绿"会骗合并。** 监控报 `CLEAN` 的那一刻发起合并，随即有检查重跑，GitHub 拒绝。之后改成合并前先当场复核 `mergeStateStatus` 与在跑数，并把监控的完成条件收紧为"CLEAN 且零在跑"。没有用 `--admin` 绕过必跑门。
- **自己交付的文档也会错。** 授权方案文档里写了"授权后必须退出重登"，是照搬审计代理的结论；追代码后发现刷新就够，当窗口内更正（#5798）。
- **代理常常找到比派单更重的东西**：C1 派的是"界面授不了权"，关掉的是一个提权洞；E1 派的是"焦点刷新"，带回了"收权不即时"的不对称；C2 的裁判点名了第三条会往软删表写记录的车道（后成 F1）。派单时写清"只读你能证明的"，这些发现才可信。
- **时区差点造出一条假发现，而且我的第一次"更正"也是错的。** 我一度判断"r52 与 r53 的后端日志时区不同"。核对服务器后确认日志一直是服务器本地时间 CST，只是 r52 恰好在凌晨上机。随后我在账本里把原因写成"本机 `date` 是本地时间"，这又错了：本机 Git Bash 的 `date` 与 `date -u` 输出相同，都是 UTC（环境变量 TZ 是 Git Bash 解析不了的时区名）。记忆里早有这一条。本文的时间都按 UTC 取数再加 8 小时，或用 PowerShell 直接取本地时间。
- **反斜杠折叠在同一天里又咬了一次**：修那条坏标记时，我先在 bash heredoc 里写 Python，heredoc 本身又把反斜杠吃了。改用 Write 落文件才修成。记忆里早有这一条，仍然踩了。

---

## 5. 交接

（窗口结束前定稿）

### 5.1 需要 owner 裁决
| 事项 | 出处 |
| --- | --- |
| 删表 → 审批完成 → 恢复表，结果永久丢失：要不要"恢复时重放"或"删表时预警" | #5804 正文 |
| `canManageAutomation` 的表级语义（无授权行不收窄、只读授权行反而 403） | #5792 |
| 审批规则装载的结构性作用域（归属列怎么定） | #5780 |
| 多维表列表无租户/业务线谓词 | #5788 |
| 表单分享候选接口的钉钉三个状态字段（含 `dingtalkGrantEnabled`）对全局 `multitable:write` 持有者可见，是否合适 | #5806 正文 |
| 222 后端输出日志 1.47 GB、疑无轮转 | 本文 §3.3 |
| 一共享 stash（被取代的旧半成品，补丁已存 scratchpad） | 本文 §5.3 |

### 5.2 交给其他窗口
- #5793 `/approvals/:id` 深链门控（审批窗口）
- #5776 实例级读取放行参与人（审批窗口）

### 5.3 环境残留
- git stash 一条：同一任务被中断时留下的半成品，正式实现已合进 main；补丁副本在 `scratchpad/wt-p6/impl-preexisting-leftover.patch`。未删，交 owner 定。
