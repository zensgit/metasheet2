# W7 设计稿三轮对抗复核附录汇总(2026-09-06)

> **说明**:以下两份附录记录同一批 W7 设计稿(先《项目归属与按人限制》,后《大 BOM 后台作业 worker 化》)成文过程中的三轮对抗复核,均为只读核验。**第 1 轮**是两路 opus 对初稿的反驳(逐条给出 blockers/minors,裁定当轮稿子"不能直接交 owner 拍板");作者按 blockers 修订后交**第 2 轮**、**第 3 轮**复核(同样两路 opus,核验上一轮的修订是否真的处置了被点名的问题,并可能提出新的 blockers/minors)。**第 4 轮是定向修订**——只按第 3 轮点名的项逐条处置(处置表见各设计稿正文末尾的"第四轮变更表"),**未再提交复核**。因此,第 3 轮复核意见里未在第四轮变更表中标记"已处置"的部分,即为**已知未闭合项**,随第四轮定向修订稿一并交 owner 参考,不代表已被认可或已被否定。

---

## 附录一:《项目归属与按人限制》设计稿三轮对抗复核记录

> 三轮均为只读核验;每轮作者按 blockers 修订后由两路 opus 复核。第四轮为定向修订(只改指定处),未再复核——残留意见即本附录第三轮部分。

## 第 1 轮对抗复核(opus 两路)

### 反驳 1
裁定:不能直接交 owner 拍板:行号与现状盘点(§1.1–§1.6)可以直接用,但整份推荐链的地基 F0 被四步顺序证伪(reconcile 在 apply 之前、且只在计划被扣住时跑,pull-target 必然为空),PR-1 的先例依据与 board 的实际做法相反,A/C 两案都被 integration:admin 绕过且漏了八条 large-BOM 写路由——请先撤掉 F0/PR-2、补齐 large-BOM 与 list 底座两处范围、把对比表里三处偏袒 C 的格子改成有条件结论,再拿 §4 的五个问题去拍板。

#### blockers
- 【F0 修不了它声称修的那个 case,而全稿以它为地基】稿子 §0/§1.7/§3 三处写「把 archive_empty 换成看板双库判据,新项目首次对账的误 403 就消失」。四步顺序证伪:apps/web/src/services/integration/stockPreparation/projectSync.ts:23-28 的顺序是 1 试算 → 2 对账(reconcile) → 3 写入(apply) → 4 批次存档;:722 起 reconcile 只在 held(计划被扣住)分支调用,而同一 held 分支在 :758-761 把第 3 步 apply 刻意跳过(注释 Deliberately NOT applying)。于是新项目第一次跑到 reconcile 时,plm_stock_preparation_main 里这个 projectNo 一行都没有,readPullTargetRowFacts(stock-preparation-project-board.cjs:414 起)返回 rowCount 0,双库判据与 archive_empty 结论完全一样:403。更糟的是 C13 当初开操作员 reconcile 就是为破「计划被扣住 → 队列永远空」的闭环(workbench-access.cjs:330-336 自陈「Every door in the room was painted on」),PR-2 等于在同一位置把那个闭环重新造出来。修法:F0 及 PR-2 整条撤回或改为「整段删门」,不能作为 PR-2 与 §3 第 1 步的推荐。
- 【稿子没有引用、也没有反驳它要改的那个文件里已写死的反对意见,且对判据的租户属性说反了】stock-preparation-operator-project-directory.cjs:371-374 明写:「Broadening the predicate to 'the ledger has pending rows for this number' would NOT be the fix either: it would admit precisely the case the gate exists to refuse ... The real fix is a project-ownership store, which does not exist yet.」F0 属同一族「放宽存在性判据」的改法,稿子一字未提。另,对比表写「门仍是租户级」,但 stock-preparation-project-board.cjs:583-596 自陈:action.target 是 deploy 级配置、全部署共享一张表(the sheet it names is not derived from anybody's tenant),plm_stock_preparation_main 没有 tenant 列,行级唯一 scope 就是 projectNo,并明确拒绝做租户隔离的断言(WHAT THIS DOES NOT CLAIM,:592-596)。所以第二条析取项对拥有该表的那个租户来说是「全表可见」,不是「租户级」。
- 【§1.5 说 board 的审计行填了 projectId,事实相反,而这正是 PR-1 的先例依据】http-routes.cjs:9018-9021 明写「project_id stays NULL and the projectNo appears nowhere: migration 083 says why that matters most here, on the one route that is ABOUT a single project」;miss 分支 :8999-9013 同样不填。全仓真正填 projectId 的只有 export(:8269)与 handoff(:8799、:8831)。PR-1「把已有的 handle 放进已有的可空列、export 早已这么做」是选择性引用——代码里存在一条与之相反、且带理由的先例。附带:被援引的 migration 083 在仓库里不存在(packages/core-backend/migrations/ 只有 080/081/082/084/085/086),那条理由本身查无实据,PR-1 必须先把这一点解决掉再动。
- 【PR-1 的 confirm 半边不是「无行为变化、0.3d sonnet」】http-routes.cjs:8163-8179 的 audit.append 刻意排在 confirmConfirmationDecision 之前,注释:「Record intent FIRST: if the SQL audit store is unavailable or refuses the payload, no multitable patch may occur」。要拿到 projectNo 必须先按 decisionId 读账本行(confirmation-decisions.cjs:1256 的 queryRecords),即在审计之前插入一次 multitable 读,这推翻那条 fail-closed 顺序不变式,并把「拒绝的调用者不写审计行」变成「拒绝前先做一次读」。这是要重新论证顺序保证的改动,不是把已有 handle 塞进已有列。reconcile 半边(:5986-5993,prepared.parameters.projectNo 已在手)才成立。
- 【hasPermission(user,'admin') 包含 integration:admin,所以 A/C 都没关掉稿子说它们关掉的那条路】http-routes.cjs:902:`if (permissions.includes('role:admin') || permissions.includes('integration:admin')) return true`;而 isTenantlessPlatformAdmin(:1046 起)要求 role:admin 且无 tenantId,是另一回事。reconcile 的第二道门 :5916 以 `!hasPermission(user,'admin')` 短路,任何接在同一位置的 A/C 谓词都会被 integration:admin 持有者绕过——包括租户内的管理员,不只是平台管理员。对比表「是否留下能清掉别人填的值这条路:A 关掉 / C 关掉」应改为「对 stock-prep operator 关掉,对 integration:admin 持有者仍开着」;§3 第三条论据同理需要重写。
- 【路由清单漏了八条操作员层、吃调用者 projectNo、且带写的 large-BOM 路由】stock-preparation-workbench-access.cjs:279-326 把 large-bom expansion-start/-get/-run/-plan、apply-start/apply-get/apply-run、cancel 八条全放进了操作员步骤表,注释自陈「These are the SAME pull under the same frozen action id」。start 的 body 允许 parameters(http-routes.cjs:1563 VALID_TABLE_ACTION_LARGE_BOM_START_BODY_KEYS),即 projectNo 由调用者给;apply-jobs 两条是写(legacyGate 'write',handlers 在 :6356/:6395)。§1.3 的表、§4 Q4 的收窄范围、PR-4/PR-6 的接线顺序全部没有覆盖它们。一个只管 reconcile/confirm/value-entry 的归属谓词,旁边留着一条操作员可达、按 projectNo 写的完整通道。三个方案的工作量都要按此重估。
- 【方案 C 的认领写入点覆盖不到多数项目,第 2 条放行会退化成常开】PR-5 把认领写在「第一次成功的 reconcile 之后」,但按 projectSync.ts:722-725,计划没有 held 行时第 2 步直接 skip(NOTHING_TO_CONFIRM),reconcile 根本不发生。于是任何从未产生冲突的项目永远不写认领行,归属表对它恒空,C 的第 2 条放行(表里一行都没有 → 认领并放行)对这些项目常开。§3 的理由写的是「谁跑了这个项目的 reconcile/apply」,PR-5 只写了 reconcile——理由与 PR 不一致。对比表里 C 的「归属来源可信吗 ✓」应改为「仅覆盖出过冲突的项目」,否则 owner 会以为 C 能给全部项目定归属。

#### minors
- P-13f 行号错:§1.6 引 __tests__/stock-preparation-operator-pull-gate.test.cjs:887-929,实际 :888 只是 P-13 的小节标题,P-13f 在 :1097-1180,轨迹级逐项相等断言在 :1152-1158。
- PR-2 会红的守卫没列:operator-pull-gate.test.cjs:1029-1055 的 P-13 (4) 两条断言(「无 MVP 项目表也放行」:1038、「有表但空档案也放行」:1054)在换判据后必红——harness 里没有 bound-target 行。PR-2 写「0.3d / 关时逐字节不变 / revert」,没提要重写这两条正例。
- §1.6 内部自相矛盾:先写「关时该路由与 main 逐字节相同」,后写「始终生效的一条(不受开关控制)= :5902-5915 的 400」。两句不能同时成立;:5908-5915 是 #5516 引入、不受开关控制的新行为(P-13f 自己在 :1173-1177 断言它「whatever the flag says」)。
- 计数错:§1.3 注里说账本模块「十处 assertAdminPermission」,实际九处(stock-preparation-confirmation-decisions.cjs:436/461/911/1134/1194/1226/1439/1475/1546,grep -c 结果为 9)。
- list 路由的问题被写小了。§1.3 只在格子里注「resolveTenantId(req, input),不是 operator scope」。resolveTenantId(http-routes.cjs:1023-1046)自陈其租户比较在无租户声明的 token 上是请求头对请求头(:1041-1045),只有 assertVerifiedTenantClaim 开关打开才补。也就是 list 的租户底座本身就比同族路由弱一档;在弱底座上加按项目谓词,谓词继承的是弱底座。这该在 §4 Q4 之前单独提给 owner,不是脚注。
- §2 C 的并发写法引错了模式。handoff-store 的 23505 分支(stock-preparation-handoff-store.cjs:228-243)是 cursor 的 compare-and-set,不是幂等 claim 插入;而 plugin 的 scoped db(lib/db.cjs)只有 insertOne 与 upsertOne,后者在 :271 明确拒绝「没有可更新列」的调用(use insertOne for insert-only writes)——即 INSERT … ON CONFLICT DO NOTHING 在现有 scoped helper 里表达不出来。要么改 db.cjs(共享基础设施,不是备料局部),要么写成 insertOne + 23505 catch。PR-3「0.5d sonnet 纯新增」没算这一格。
- §1.5 说 plm_stock_preparation_project.owner 「被断言永不外泄」准确,但断言的是目录路由的投影(stock-preparation-operator-scope-tripwires.test.cjs:488-504 断 serialized.includes('"owner"') === false)。A/C 的 UI 要在项目卡片显示「负责人」,若经目录路由加字段,S-02a(:469-486,顶层 key 集与每行十键投影双冻结)会与 S-02b 一起红。§2 A 的守卫⑥只提了 S-02a。
- 工作量口径把工时与日历时间混为一谈。§2 A 自己写「必须先定义空表语义 + 一次性回填,否则演示当天全线 403」,而 §4 Q5 又问回填要不要人工确认——那 A 的「2.5–3d」不含 owner 裁决 + 一轮人工核对的周期。C 的「2d」同理不含「影子期跑够一轮 r14 再切 enforce」(§2 C 自己写的)。这两项应拆成「工时」与「可上线时间」两列。
- §6.2 说 reconcile 两条租户推导「今天二者取值相同」——建议补上这句成立的条件:只在 assertVerifiedTenantClaim 关闭且 auth 中间件按 x-tenant-id 填 user.tenantId 时相同,一旦该开关打开,resolveOperatorValueScope 会拒绝 resolveAuthUserTenantId 放行的调用者。统一到 scope 的建议正确,理由该写成「两条门槛不同」而不是「今天取值相同」。

### 反驳 2
裁定:不能直接交 owner 拍板:核心的「关键发现 F0」与代码事实方向相反(reconcile 是第 2 步、pull target 由第 3 步 apply 写,换掉 archive_empty 会把新项目首次对账从「放行」变成「永久死锁」),推翻方案 B 的那一格对比论证引用的是一条 owner 已于 2026-08-29 裁决过的行为(Q5-A)且三案都关不掉它,再加上归属变更无审计动作/无迁移、PR-1 的 confirm 半边不成立、两支 source-level 计数守卫必红、F-04 与缺失的改派路由——先按上面七条改完,再把 §4 的五个问题重排(Q3 已被 DDL 预答),才是一份能拍板的稿子。

#### blockers
- 【F0 是错的,且方向相反】稿子的「关键发现」和 §3 第 1 步、§5 PR-2 全部建立在「把 archive_empty 换成看板双库判据,新项目首次对账 403 就消失」之上。代码说的正相反:reconcile 是一线四步的**第 2 步**,pull target 的行是**第 3 步 apply** 写的。证据:apps/web/src/services/integration/stockPreparation/workbenchAccess.ts:354-357 逐行列出 `dry-run 步1 / reconcile 步2 / apply 步3 / mvp-persist 跳过`;后端同一张表 plugins/plugin-integration-core/lib/stock-preparation-workbench-access.cjs:348-353(操作员步骤表只含 reconcile)与 :357-364(mvp-persist 留在平台管理员);plugins/plugin-integration-core/lib/stock-preparation-project-board.cjs:574-575 明写 pull target 是「the rows `apply` wrote」。于是一个全新项目在第一次 reconcile 时:归档表无行(mvp-persist 没跑)、pull target 无行(apply 还没跑),双库两条析取全假 → 403。而 F0 主张**换掉**(不是叠加)archive_empty,原本能放行的全新/未归档租户反而变成 403;首次 reconcile 被拒 → apply 永远到不了 → pull target 永远没有行 → 该项目永久死锁。这比 #5516 今天的行为更糟,不是更好。§1.7、§3.1、§5 PR-2 需整段重写。
- 【PR-2 在门的当前位置拿不到 action.target,而门的位置是被断言钉死的安全属性】门在 plugins/plugin-integration-core/lib/http-routes.cjs:5916-5936;`const action = assertStockPreparationTargetReady(await tableActions.getTableAction(...))` 在其**之后**(prepared 在 :5972)。readPullTargetRowFacts 的签名是 (recordsApi, ownSheet, boundTarget, projectNo),boundTarget 就是 action.target(project-board.cjs:562-563),所以 PR-2 必须把门挪到动作查找之后。__tests__/stock-preparation-operator-pull-gate.test.cjs 的 P-13 case-2 用硬断言钉住了拒绝发生在这一切之前:`refused.adapterPrincipals.length === 0`、`refused.loadPrincipals.length === 0`(无连接解析/凭据解密)、`refused.auditAppends` 为空,注释在 :915-917。挪门还会把「目标未配置的部署」的答复从 403 变成 target-not-ready,改掉拒绝优先级。附带:resolveOwnBoundSheet / readPullTargetRowFacts 只在 stock-preparation-project-board.cjs:687-694 的 `__internals`(测试专用口)导出,生产复用要先提升为正式导出。sonnet 0.3d + 「关时逐字节不变」这两条同时成立不了。
- 【对比表「是否留下能清掉别人填的值这条路:A 关掉 / B 保留 / C 关掉」是偏袒 A/C 的错误论证,而它正是推翻 B 的主要理由】reopen 发生在 plugins/plugin-integration-core/lib/stock-preparation-confirmation-decisions.cjs:1009-1040,触发条件是**指纹回摆 A→B→A**,发生在**任何一次 reconcile**里——包括归属人自己那一次。§4 Q1/Q3 自己就设想了班组/两班倒/多负责人,那么归属人(或任一共同负责人)一跑 reconcile,同事填的 resolutionAction / resolvedValue / resolvedAuxValue / notes 照样被 :1029-1034 置 null。归属只换「谁能触发」,三个方案没有一个关掉这条路。更关键的是 :1013-1022 的注释写明这是 **owner 已经裁决过的保守默认(Q5-A,2026-08-29 确认:陈旧输入上的已决行必须重新确认,绝不静默复活)**——稿子把一条已裁决的设计当成「B 的代价」来否定 B。这一格必须改成「三案皆保留,A/C 只收窄触发者」,并注明 Q5-A 已裁决。
- 【归属的产生与改派完全没有审计,而补审计要再加一支迁移,稿子的 PR 清单里两样都没有】审计动作词表是 DB CHECK + 代码双份冻结:plugins/plugin-integration-core/lib/stock-preparation-audit-store.cjs:49-88 现有 14 个动作(不是注释里说的 9),:210-212 对不在 ACTION_SET 的动作直接 422 fail-closed;每加一个动作都配一支迁移(词表注释里点名 080 / 082 / 085 / 086,对应 packages/core-backend/migrations/085_*、086_* 的 `ADD CONSTRAINT ... CHECK (action IN (...))`)。C 的认领写入(PR-5)、A/C 的管理员改派都改变「谁能动这个项目」,却在 PR-1..PR-8 里没有任何审计动作,也没有 088 迁移。结果二选一:要么归属变更无痕(直接推翻稿子 §1.5「今天靠审计连谁动过哪个项目都反查不出来」这个立论),要么一 append 就 422。
- 【PR-1 的 confirm 半边不是「把已有 handle 放进已有可空列」,估时与「无行为变化」都不成立】confirm 路由此刻手上**没有** projectNo:http-routes.cjs:8167-8179 的 audit.append 发生在 confirmConfirmationDecision 之前,而行定位只用 decisionId(stock-preparation-confirmation-decisions.cjs:1256),projectNo 只能从账本行读出(:1046 写入)。要补 projectId 就得在审计之前插一次单行查询,这会破坏该处 :8163-8166 明写的「Record intent FIRST:审计store 不可用或拒收就不许发生多维表写」次序,也会破坏 :8175-8176 明写并靠注释保护的性质「请求畸形时审计行仍然落地」。reconcile 那半边是干净的(prepared 在 :5972,audit 在 :5986,确实白拿),confirm 这半边不是。PR-1 应拆成两支,或把 confirm 半边改成在 confirm 成功后追加一条 projectId 已知的审计行。
- 【两支 source-level 计数/扫描守卫会红,稿子三个方案的守卫清单里一条都没提】(a) __tests__/stock-preparation-handoff.test.cjs:2984 `assert.equal(callSites, 11, 'G9: eleven call sites — if this changes, the header list must too')`,数的是 http-routes.cjs 里 `resolveOperatorValueScope({` 的出现次数,注释 :2971-2983 说明这是刻意的漂移闸。B 删门会掉到 10 → 红;C 的 PR-4/PR-6 会涨到 12/13 → 红;两边都还要同步改 stock-preparation-operator-scope.cjs 的表头枚举(:119 起提到该门)。(b) __tests__/stock-preparation-tenant-scoped-write-guard.test.cjs:348-378 是逐 handler 的源码扫描,对被钉住的 handler 断言「不得出现 resolveTenantId、不得出现裸 user.tenantId」。PR-6 想收窄的 list 现在恰恰用 resolveTenantId(http-routes.cjs:8067;定义 :1023-1045,:1040-1045 自陈 claim flag 关时是 header 比 header)。把 list 接进按项目谓词 = 先做一次 #5445 形状的 scope 迁移 + 改这支扫描的钉子,不是「仅在 enforce 下收窄」。
- 【PR-7 撞上 F-04 的 presence==grant 等式,且 C 的 PR 清单里根本没有改派路由这支后端 PR】apps/web/tests/stockPrepPermissionMatrix.spec.ts:283-284 断言前端 STOCK_PREP_WORKBENCH_CAPABILITIES 与后端逐项深等;F-04 本身还要求「渲染出来的 control id == 授予的 control」双向相等(:24)。apps/web/src/services/integration/stockPreparation/workbenchAccess.ts:205-208 明写:运行时另有门控的控件必须 `control: null`,否则「F-04 会为一个正确的 UI 报红」;apps/web/tests/StockPreparationHandoff.spec.ts:12-16 与 :302-307 就是 handoff 为此另写的十条见证。「非负责人按钮禁用」正是运行时门控,PR-7 因此不是加一行 capability,而是 manifest 两侧 + control:null + 另写一套见证。同时 §2 C 与对比表都按「管理员可改」给 C 记了功,但 PR-1..PR-8 里**没有任何一支后端改派/分配路由**(只出现在「若改采 A」的脚注)。opus 0.5d 与「C 支持管理员改派」二者至少有一个是假的。

#### minors
- 核对成立、可以放心留着的部分(供 owner 参考):§1.1 的 16 列与 stock-preparation-templates.cjs:835-871 逐条相符(projectNo :856、resolutionAction–notes :863-866、confirmedBy/confirmedAt :867-868,确无作者列);§1.4 的 :998-1008 / :1009-1040 / :1046 / :1058-1085 / :1225-1332 / :1256 / :1306-1307 全部成立;§1.6 的 http-routes.cjs:1341-1343 开关、:5902-5915 的 400、:5916-5936 的门位置成立;requireTableActionAccess:988-1014 与 legacy 分支 :993-999 成立;db.cjs:25 的 integration_ 前缀白名单成立;审计表 project_id 不受 values-free 形状门约束确如 stock-preparation-audit-store.cjs:217-220 与表头 :20-22 所述,export 路由 :8269 已经这么做。§6 两处不一致属实(http-routes.cjs:227-230 说 reconcile 仍是平台管理员专属,与 workbench-access.cjs:348-353 相反;:5851 与 :5923 两条租户推导),值得单独一支注释修正 PR。
- §1.5「plm_stock_preparation_project.owner 目前被断言永不外泄」偏强。__tests__/stock-preparation-operator-scope-tripwires.test.cjs:488-504(S-02b)钉住的只是**目录路由**的响应里不出现 owner 值与 "owner" 字段名,不是全局断言。改成「目录路由已断言不外泄」。
- §1.6 引 __tests__/stock-preparation-operator-pull-gate.test.cjs:887-929 说「P-13f 对此有轨迹级断言」,那一段其实是 P-13 的注释头。P-13f 的实际断言在 :1135-1177(objectSheetLookups 逐项同序相等、adapterPrincipals/loadPrincipals/auditAppends 计数相等)。改引后者,owner 或下一个读代码的人才能一眼核实。
- 方案 B 的删除清单不完整。删掉 assertOperatorMaySeeProject 还要删 http-routes.cjs:619 的导入与 stock-preparation-operator-project-directory.cjs:431 的导出,并处理随之变成死代码的 projectArchiveIsEmpty(:121-134)和 listOperatorProjectDirectory 的 includeArchiveEmptiness 形参(:212)——后者是目前唯一的调用方。
- 迁移号 087 确实空着(现存最高是 086_extend_stock_prep_audit_project_board_read_action.sql),这一条成立。但若按 blocker 4 补一支审计动作迁移,C 会同时占用 087 与 088,PR 清单要写出来,否则两支并发 PR 会撞号。
- A/C 的唯一键 UNIQUE (tenant_id, project_no, owner_kind, owner_id) 已经替 owner 回答了 §4 Q3「一个项目允许几个负责人」——答案是「任意多个」。要么把 Q3 从待裁决里拿掉,要么把唯一键标成待定,不要一边问一边在 DDL 里定死。
- handoff-store 的写法引用偏了。packages/core-backend/migrations/084_create_integration_stock_prep_handoff.sql:91-98 明写「唯一索引本身不是并发故事」,真正仲裁的是 SELECT … FOR UPDATE 加带 step_index 谓词的 UPDATE——那是为**compare-and-set 游标**准备的。一张只做 claim 的表用 INSERT … ON CONFLICT DO NOTHING 就够,不需要 FOR UPDATE。PR-3 的 0.5d 里塞了不必要的复杂度。
- C 的「归属表为空即认领」还有一个稿子没讨论的占坑面:reconcile 本身可以逐个项目号调用来抢注。稿子只论证了「不拿 dry-run 当认领点」,但 reconcile 同样是可枚举的(只是更贵——它会真的读源)。shadow 期能观察到,但 enforce 后需要一条速率/次数限制或「认领只在本人 dry-run 过的项目上成立」的附加条件。
- §1.3 表里 list 用 resolveTenantId(req, input) 这一格是对的,但没有点出这正是 #5445 修掉的 x-tenant-id 形状——http-routes.cjs:1040-1045 自陈 claim flag 关时上面几行的比较是「header 比 header」。PR-6 打算在 list 上加按项目的门,那这条前置必须先写进设计稿,否则门可以被绕过去。
- 稿子引 http-routes.cjs:5982 的「migration-frozen (9 actions)」是照抄代码注释,但那句注释本身已经过时——现在是 14 个(audit-store.cjs:49-88)。既然稿子 §6 已经在收集「与代码相反的注释」,把这条一并收进去。

---

## 第 2 轮对抗复核(opus 两路)

### 复核 1
裁定:还不能直接交 owner 拍板 —— 第一轮 14 条 blocker 里 12 条已实证解决(F0 整段撤回、:371 附近的反对意见与 deploy 级 target、board 审计事实、PR-1 拆分、integration:admin、Q5-A、归属审计+088、两支源码级守卫、F-04 与改派路由,行号我逐条核实基本全中),但反驳1·B6 只修了八条大 BOM、同族的 dry-run/apply 仍不在清单也不在谓词里(workbench-access.cjs:252/:258,http-routes.cjs:6128/:6130/:6148),且 B7 的修法引入了一句被 projectSync.ts:752-760 证伪的地基句;补这两条(基本是清单与措辞)加上归属键 vs 无租户目标表、PR-6 confirm 审计次序两处交代,再交 owner 就是一份能拍板的稿子。

#### blockers
- 【反驳1·B6 只修了八条,同族还剩两条,其中一条是主写路径】§1.3b 补齐了大 BOM 八条,但操作员步骤表一共 11 条:C:/Users/zhou/Downloads/dev/metasheet/plugins/plugin-integration-core/lib/stock-preparation-workbench-access.cjs:252 是 dry-run、:258 是 apply(legacyGate 'write'),稿子 §1.3a/§1.3b 两张表都没有它们。二者都吃调用者 parameters:路由 http-routes.cjs:63/:65,键集 :1557/:1562(均含 'parameters'),处理器 tableActionDryRun :5734 / tableActionApply :6128(门 :6130 requireTableActionAccess(...,'write'),parameters 原样下传 :6148,dryRunToken :6149)。也就是说一线可以对任意项目号 dry-run 拿 token、再 apply 把行写进 deploy 级共享目标表(在 sandboxPolicy :6171 / productionPolicy :6174 已配的部署上真写)。而 §5 的谓词只接 reconcile / confirm / expansion-start —— enforce 之后「别人项目的数据被改」这条主路径原样敞开;更矛盾的是 PR-5 把 apply 当作主认领点却不给它装门,非归属人跑 apply 时(insert-only + 23505)认领行还会静默落到原主名下,账本看起来是对的、写却发生了。修法:把 dry-run/apply 补进 §1.3、§1.4a 与谓词接线,或明确写「本设计不覆盖 apply 写面」并让 owner 知道这条免责。
- 【推荐链的地基句被代码证伪】§3 写「唯一真实、每个项目都会发生的事实是『谁跑了这个项目的 apply』」,§2 C 与 PR-5 写 apply 是「操作员必经且每个项目都会跑 / 覆盖全部项目」。反证:C:/Users/zhou/Downloads/dev/metasheet/apps/web/src/services/integration/stockPreparation/projectSync.ts:752-760 —— held 分支(即唯一会跑 reconcile 的分支,:722/:724)刻意跳过第 3 步 apply(WRITE_HELD_FOR_CONFIRMATION)并 record(4,...) 后 return。所以一次四步跑里 reconcile 与 apply 互斥:计划被扣住的项目在那一轮永远到不了 apply。三个认领点的并集(apply ∪ reconcile ∪ expansion-start)确实覆盖两条分支,机制没塌,但「apply 覆盖全部项目」这句是假的,而 owner 恰恰要按这句话在 A/C 之间选。改成「apply ∪ reconcile 才覆盖两条分支,单点都不覆盖」即可。
- 【归属表的键与被保护的写面不同量纲】§2 A/C 的表键是 (tenant_id, project_no),而谓词一旦按 §5 PR-5/PR-6 接到 expansion-start / apply 上,被保护的对象是 plm_stock_preparation_main —— 稿子自己在 §1.7 引用的 stock-preparation-project-board.cjs:585-588 明写 action.target 是 deploy 级共享配置、该表没有 tenant 列、行级唯一 scope 就是 projectNo(:603-607 的 WHAT THIS DOES NOT CLAIM 进一步声明多租户共用一个 target 时它不保证分得开)。于是同一个项目号在共享 target 上跨租户重名时,按租户键的归属行表达不了「这些行归谁」。这直接影响 Q3(唯一键形状)的答案与 C 在写面的保证强度,必须在裁决书里说清楚:归属只对账本(按租户 staging project)可证,对共享目标表不可证。
- 【PR-6 把谓词接到 confirm,与稿子自己为 PR-1b 论证过的不变式冲突,却没给答案】confirm 的 audit intent 行刻意排在写之前(http-routes.cjs:8165-8166「Record intent FIRST」、:8167-8179 append、:8180 才是 confirmConfirmationDecision),另有 :8175-8176 保证「请求畸形时审计行仍然落地」。归属谓词需要先由 decisionId 解出账本行的 projectNo(stock-preparation-confirmation-decisions.cjs:1256),这次读只有两个位置:放在 :8167 之前 = 正是 PR-1b 判定不可接受的那条(审计前插一次 multitable 读);放在 :8167 之后 = 被拒的调用者留下一条声称有意图的审计行,与稿子在 §1.7 大加引用的「拒绝前什么都没碰」(operator-pull-gate.test.cjs:1005-1007)取向相反。PR-1b 的解法(成功后追加)对拦截型检查不适用。PR-6「opus 0.7d」在这条没答之前不成立。

#### minors
- migration 088 不只是「照 086:52-55 的 DROP/ADD 形制」:086:31-34 写明 store 常量与迁移必须 SET-EQUAL,且 __tests__/stock-preparation-audit-migration.test.cjs 是按『最高编号的词表迁移』发现式解析的。所以 088 必须重列全部 15 个动作(只 ADD 新动作会把现有 14 个静默丢掉),这一句该写进 PR-3。
- §2.5 (b) 的「list 一调 scope 就自动进派生集合、立刻红」少了一个既存出口:stock-preparation-tenant-scoped-write-guard.test.cjs:468-480 有 VALUE_BEARING_READS_WITH_GATED_LEGACY_TENANT 的 carve-out(成员 tableActionDryRun,允许 gated 三元里恰好一次 resolveTenantId)。所以 Q0 的选项是三个:不收窄读面 / 先做 #5445 形状迁移 / 按 dry-run 的 gated-ternary 形制入 carve-out。
- §2.5 (a) 漏了 G9 的第二半:stock-preparation-handoff.test.cjs:2985-2987 的 marker 循环断言 scope 表头必须点名 tableActionConfirmationDecisionsReconcile。方案 B 删门时不只是把 11 改 10 + 删表头条目,还要同时改这条断言,否则表头一删它就红。
- §1.4b「list 是这一族里唯一没迁的」建议加限定词:限于确认账本一族成立;table-action 侧的 apply 同样用 resolveTenantId(http-routes.cjs:6133),同一 x-tenant-id 形状。按仓库对绝对措辞的既有纪律,这类「唯一」要么限定要么先穷举。
- 行号微漂(1–2 行,均不影响结论,但既然全稿以行号立论就一并校正):stock-preparation-operator-project-directory.cjs 的「Broadening the predicate…The real fix is a project-ownership store」实为 :370-373(稿写 :371-374);同文件「WHAT THIS DOES NOT CLAIM」起于 :378(稿写 :379-385);stock-preparation-project-board.cjs 的「WHAT THIS DOES NOT CLAIM」实为 :603-607(稿写 :602-609);stock-preparation-operator-scope.cjs 表头第 10 条枚举起于 :108(稿写 :110-126)。
- PR-1a 的取值可以更直接:reconcileProjectNo 在 http-routes.cjs:5906 已算好(稿子写的 prepared.parameters.projectNo 也成立 —— stock-preparation-table-actions.cjs:1750 的返回确实带 parameters —— 只是多绕一层)。另注:非 admin 分支才有 :5908 的 400,admin 分支传入的 projectNo 不受形状约束,而 audit-store.cjs:20-22 明写 project_id 不做 shape gate、纪律在路由侧 —— 与 export :8269 的既有做法一致,写一句「与 export 同口径」即可免除下一个读者的疑问。
- §1.4a 第 5 条(大 BOM apply)与小 apply 一样,只在 sandbox/production 策略已配的部署上真写(http-routes.cjs:6171/:6174,大 BOM 侧同一 assertStockPrepApplyAllowed,见 workbench-access.cjs:276-278 的自陈)。给 owner 估爆炸半径时值得写上这一格。

### 复核 2
裁定:还不能交 owner 拍板，但差距比第一轮小一个数量级：14 条第一轮 blocker 里 11 条已实质解决(F0 整段撤回及其四条证据、:371-374 的既有反对意见、board 审计 project_id 与 083 编号漂移、PR-1 拆 1a/1b、integration:admin 免责、Q5-A 已裁决、087+088 两支迁移、G9/派生扫描/F-04 三支守卫、改派路由 PR-6，逐条行号我都重新核过且成立)；剩下的三条都是局部修补、不动推荐链本身——把 dry-run 与 apply 两条路由补进 §1.3/§1.4a 并给出它们的门与租户底座、把「apply 每个项目都会跑」改成「reconcile 与 apply 是互斥双分支、并集完备」、把 PR-8 换判据会红掉的 P-13 armed 半边(:972-987 / :989-1012 / :1009-1011)记进 C 路线的守卫账与工时。这三处改完，§4 的 Q0-Q6 就是一份可以直接拍的问题清单。

#### blockers
- 【第一轮反驳1·B6 只解决了一半：路由清单仍缺四步流程的两条核心路由，而 PR-5 的主认领点正装在其中一条上】§1.3a/§1.3b 补齐了大 BOM 八条，但 `POST /api/integration/table-actions/:actionId/dry-run`(http-routes.cjs:63，处理器 :5734)与 `POST …/:actionId/apply`(http-routes.cjs:65，处理器 :6128-6185)一条都没进清单。apply 完全符合 §1.3 自己定的入选口径：①操作员可达(stock-preparation-workbench-access.cjs:258-261，legacyGate 'write')；②projectNo 由调用者从 body 给(VALID_TABLE_ACTION_APPLY_BODY_KEYS = {'parameters','confirm'}，http-routes.cjs:1562；:6131 解析；:6148 透传)；③它是**写客户目标表**的那一条(applyStockPreparationAction，:6146)，危害不小于 §1.4a 第 5 条的大 BOM apply，却在「按危害排序」里缺席；④它的租户推导是 `resolveTenantId(req, {})`(:6133)——正是 §1.4b 用来指责 list「底座弱一档」的同一个 helper。PR-5 把 C 的主认领点放在 apply 上，而这条路由在全稿里既没有行号、没有权限门盘点、没有租户底座评估。owner 看不到自己要在哪条路由上落认领。
- 【§2 C 与 §3 的核心论据被代码直接证伪：apply 不是「操作员必经且每个项目都会跑」】§2 C「修法」写“认领点改成 apply 成功之后(四步第 3 步，projectSync.ts:757-810，操作员必经且每个项目都会跑)”，§3 理由段进一步写“唯一真实、每个项目都会发生的事实是「谁跑了这个项目的 apply」”。反证：apps/web/src/services/integration/stockPreparation/projectSync.ts:759-766 —— `if (held) { // Deliberately NOT applying with acceptManualConfirmHold. See the module header.` 之后直接 `record(result(3,'apply','skip','WRITE_HELD_FOR_CONFIRMATION',…))` + `return done()`。而 held 正是稿子自己在 :722/:724 引来证明 reconcile 只在 held 分支跑的那个条件。所以 reconcile 与 apply 是**互斥的两个分支**，不是一前一后的两步：held 项目永远不到 apply，非 held 项目永远不跑 reconcile。三个认领点合起来覆盖面确实成立(这一点结论没错)，但支撑它的那句事实是错的，而 §3 的整段推荐、对比表「归属来源可信吗」那一格、以及 owner 判断「认领是否真能定归属」全部挂在这句上。正确表述应是「reconcile 覆盖 held 项目、apply 覆盖非 held 项目，两者互斥且并集完备，expansion-start 覆盖大 BOM 通道」。
- 【§2.5(d) 的守卫账在推荐路线(C)上记漏了一整支：PR-8 换判据会红掉 P-13 armed 半边】§2.5(d) 明写“本稿的 PR-2 不再动判据，所以这两条在 A/C 路线上保持绿；只有 B(删门)会把它们连同整个 armed 半边一起删掉”。但 §5 的 PR-8 自己写着“enforce 时**一次性**把 #5516 的 `archive_empty` 判据替换为归属谓词”。反证(plugins/plugin-integration-core/__tests__/stock-preparation-operator-pull-gate.test.cjs)：case 1 :972-987 断言「目录里有这个号 ⇒ 不被 NOT_VISIBLE 拒」——换成归属谓词后，一个目录里有、但归属人是同事的项目会被拒 ⇒ 红；case 2 :989-1012 断言「目录里没有这个号 ⇒ 403 NOT_VISIBLE」——按 C 的第 2 条放行(归属表空即认领)会放行 ⇒ 红；:1009-1011 的正例控制 `assert.ok(projectSheetLookups(refused) > 0, '…the refusal came from an actual directory read, so P-13f\'s "zero lookups" measures the flag')` —— 归属谓词读的是 SQL 归属表、不再 lookup 项目 sheet ⇒ projectSheetLookups 变 0 ⇒ 红，且这条一红，P-13f(:1144)「零目录读」的度量基准同时失效。也就是说第一轮反驳1的 minor「PR-2 会红的两条 P-13 正例」并没有随 F0 撤回而消失，它只是从 PR-2 挪到了 PR-8，而 PR-8 那一行既没有工时也没有守卫清单。C 路线的守卫账因此被系统性记少一支。

#### minors
- §1.4b 的绝对句「list 是这一族里唯一没迁的」偏强。同一份 http-routes.cjs 里 `tableActionApply` :6133 也用 `resolveTenantId(req, {})`，`tableActionDryRun` 走的是 guard 里那条 gated 形态。要么把「这一族」显式限定为账本/值面四条(list / value-entry / confirm / export)，要么承认表外还有同形状的。
- §2.5(b) 的「list 一调 scope 就进派生集合 ⇒ 立刻红 ⇒ 必须先做 #5445 形状的 scope 迁移」漏了守卫自带的例外通道：__tests__/stock-preparation-tenant-scoped-write-guard.test.cjs:468-480 的 `VALUE_BEARING_READS_WITH_GATED_LEGACY_TENANT` 允许一个 handler 在 gated 三元里调 **恰好一次** `resolveTenantId`(:475-479)，`tableActionDryRun`(:391)就是靠这条留在集合里的。所以「先做完整 scope 迁移」是更好的工程选择，但不是守卫逼出来的唯一出路。Q0 的措辞应从「必须先迁」改成「要么先迁，要么进 gated 例外并说明为什么这里也配得上那个例外」。
- §2 前置事实第 3 条与 PR-3 把 088 的风险写成「撞号」，但 086 自己记录的失败模式更狠：086:31-34 说词表迁移必须 **re-list 每一个动作**、且 `STOCK_PREP_AUDIT_ACTIONS` 与之 SET-EQUAL 的断言按「**最高编号**的那支词表迁移」由 DISCOVERY 解析；086:36-49 逐字复盘过这件事——原本编号 083 的这支文件被后合入的 085 全量 re-list 静默抹掉，「症状不是迁移失败，而是操作员一开看板就 500」。所以 PR-3 的正确提醒不是「预留号防撞」，而是「088 必须 re-list 全部 15 个动作；若期间有更高号的词表迁移合入，你的动作会被静默抹掉，先红的是那支 set-equality tripwire 而不是部署」。
- PR-1a 的「无行为变化，不带开关」措辞不准。补 `projectId` 改变的是审计表内容：reconcile 的行从 project_id NULL 变成带 projectNo，而审计 trail 有自己的读路由(`GET /api/integration/stock-preparation/audit`，http-routes.cjs:208)。这在一个把「审计行 values-free」当载荷性质写进迁移(066:22)与 086:20-25 的代码库里，应该说成「权限与拒绝语义无变化，审计行内容有变化，理由见 §1.5 末段」。
- §2 C「并发写法」引 `SELECT … FOR UPDATE` 的行号偏一格：stock-preparation-handoff-store.cjs:243 是注释，实际调用 `trx.selectOneForUpdate(HANDOFF_TABLE, where)` 在 :245。:173 那处引的是 084 复述块里的散文而非代码，也可一并标成注释。
- 两处路径/范围不精确，owner 或下一个读代码的人会找不到：(a) `StockPreparationConfirmationQueueView.vue:145-153` / `:186-192` 行号对，但全稿其他引用都带路径而这条没有，实际在 `apps/web/src/components/integration/stockPreparation/`(不在 `views/`)；(b) §1.3a 写 board 处理器 `:8941-9017`，:9017 只是那段 try/catch 的结束，同一个 handler 继续到 :9022 之后的成功审计 append——而 §1.5 恰恰要引 :9018-9021，两处范围自相矛盾。

---

## 第 3 轮对抗复核(opus 两路)

### 第三轮复核 1
裁定:第一轮两路共 7 条 blocker 逐条实证**全部已解决**(复核1·B1/复核2·B1 路由清单:dry-run :63/:5734/:5738/:5767-5774/:5785 与 apply :65/:6128-6185/:6130/:6131/:6133/:6146/:6148/:6149/:6171/:6174 及键集 :1557/:1562/:1563 我全部重核成立;B2 held 分支 projectSync.ts:722/:759-767/:768-772 成立,「reconcile 与 apply 互斥」表述正确;B3 键量纲及缓和事实 project-board.cjs:585-588/:593-600/:602-606 成立;B4 已给出可执行的 PR-6c,:8154-8155 先例、:8167-8179、:8175-8176、confirmation-decisions.cjs:1260-1266、module.exports:1574-1608 无该导出、guard :523-532 逐条为真;复核2·B3 的 P-13 守卫账 :971-987/:989-1012/:1009-1012/:1028-1055/:1141-1145 逐条为真),M-a 到 M-j 十一条 minor 的行号我也重核无误(含 086:31-34/:36-49、audit-migration.test.cjs:75、G9 十一处调用点与 :2984/:2985-2987、db.cjs:270-272、handoff-store.cjs:245、board handler :8941-9044、operator-scope.cjs:108),两条「不采纳」的反证(:371-374 与 :379-384)经原文比对也确实是稿子对、第一轮错。**只剩 1 条新引入的免责精度问题**:补上 apply/expansion-start 的 legacy 旁路层级(约十分钟的文字改动,不动结构、不动工时),即可交 owner 拍板;其余 6 条是文字与守卫账补丁,可与 PR 描述一起改,不必再开一轮复核。

#### blockers
- 【本轮新引入:免责句把保证强度写强了 —— 恰写在新增的两个守门点上】§2 前置事实 1 与 §3 免责 1 写「三案都只对 integration:admin 持有者仍开着」。这只对 reconcile 成立(legacyGate='admin',plugins/plugin-integration-core/lib/stock-preparation-workbench-access.cjs:348-353)。本轮新加的两个守门点门更宽:apply 的 legacyGate='write'(同文件 :257-262;路由门 plugins/plugin-integration-core/lib/http-routes.cjs:6130),expansion-start='read'(同文件 :281-286;门 :6192)。requireTableActionAccess 的 legacy 早返回在 http-routes.cjs:993-1000,hasPermission 对 'write' 只查 integration:write(:906)、对 'read' 查 integration:read ∪ integration:write(:903-905),都在 operator 分支之前 return;而 §2.5(a) 把 PR-5 钉成「apply 与 expansion-start 各在**操作员分支**另起一次 scope」。结果:在全稿称为「唯一直写客户目标表」的那条路由上(写在 :6146),任意 integration:write 持有者仍可对任意项目号 apply、任意 integration:read 持有者仍可对任意项目号 expansion-start,且都不落归属行。按现稿签字,owner 会以为写面残余口子只有 integration:admin。修法二选一写进 §3 免责:(i) 逐路由列出旁路层级(reconcile=integration:admin / apply=integration:write / dry-run·expansion-start=integration:read);(ii) 谓词对非 admin 的 legacy 分支也生效,并说明该分支 tenant/actor 怎么取 —— legacy 分支从不解析 operator scope,只剩 resolveTenantId(:1023-1047,自陈 :1040-1045 是 header-against-header)+ user.id,这一档的强度必须当面写明。

#### minors
- 【PR-8 的守卫账还差一条,方向与 §2.5(f) 相反】§2.5(f) 只警告「谓词若再推一个 staging project 会红」,但同一处断言要求那两种形态**必须在场**:plugins/plugin-integration-core/__tests__/stock-preparation-tenant-scoped-write-guard.test.cjs:512-515 断 calls.includes('resolveIntegrationStagingProjectId(reconcileScope.tenantId, undefined)')。PR-8 把 archive_empty 换成读 SQL 归属表后,这个字面形态从 reconcile handler 里消失 → 直接红。应并入 PR-8 的守卫清单(和 §2.5(d) 同一支 PR)。
- 【P-13 armed 半边还钉着「dry-run 与 apply 不被这道门收窄」,PR-5 正好动这两条】plugins/plugin-integration-core/__tests__/stock-preparation-operator-pull-gate.test.cjs:1079-1093 —— 注释 :1079-1080 明写 *"the OTHER two steps ... are NOT narrowed by this: only reconcile carries a projectNo whose ledger rows another person owns"*,循环 :1082 对 [DRY_RUN, APPLY] 断 assert.notEqual(code,'STOCK_PREPARATION_RECONCILE_PROJECT_NOT_VISIBLE')。两个后果:① 若归属 403 复用同一 error code(FE 文案 apps/web/src/services/integration/stockPreparation/plainLanguage.ts:652-655 只有这一条),PR-5 一装门这条就红;② 这段注释是代码自己写下的、与本稿 §1.4 把 apply 提到第 1 位相反的立场,值得在 §1.4 或 §2 C 里正面引一次并说明为什么它当时成立、现在不成立。
- 【§1.3d 的绝对句与它上面的 §1.3c 表自相矛盾】§1.3d 写「projectNo 由调用者交进来的入口只有 dry-run / apply / reconcile / expansion-start 四条,其余全部按 jobId 或 decisionId 间接绑定」。反证就在同一节:list 从 query 取 projectNo 且必填(http-routes.cjs:8063-8066),export 是按 projectNo 的**带值**导出(路由 :181,handler :8210-8294),board 从路径参数取(路由 :206,:8949),handoff advance 按 (tenant, project_no) 推进(migration 084:99-100)。应限定为「**写面**由调用者交 projectNo 的入口只有四条;读面另有 list / export / board 三条同样按调用者给的号定位、只是不写」,否则 §0 的一句话结论会让 owner 以为读面也被这四个落点覆盖(Q0/Q4 其实问到了,但结论句盖过了问题)。
- 【PR-6c 的「软查找」只覆盖 0 行/多行,没覆盖「这次读本身失败」】今天 confirm 在 multitable 不可用时仍会先落 intent 审计行(http-routes.cjs:8167-8179 排在 :8180 之前,理由 :8165-8166),把账本读插到 :8167 之前后,一次 multitable 故障会把「有审计行 + 5xx」变成「无审计行 + 5xx」—— 这正是 :8175-8176 那条不变式的同族。PR-6c 应明写第 ⑤ 条:该次读**抛错也放行**(fail-open on lookup),只有「恰好 1 行且归属他人」才 403。
- 【§2 A 的一处计数与行号对不上】「P-13 case-2 的四条『拒绝前什么都没碰』断言(:1005-1007)」—— 该范围只有三条(adapterPrincipals :1005 / loadPrincipals :1006 / auditAppends :1007);若把 :1004 的 403 状态断言也算进来,范围应写 :1004-1007。全稿以行号立论,这类数字最好一致。
- 【§2 B 的删除清单按行号执行会误删】清单写「删 P-13 armed 半边 :964-1094 ... **保留** case 5(400)」,但 case 5 在 :1057-1077、dry-run/apply 那段在 :1079-1093,都在 theOperatorReconcilesOnlyProjectsItCanSeeArmed(:968-1094)这**一个函数体**内。应写成「拆函数保留 case 5 与 :1079-1093,删 case 1-4」,否则下一个人照行号删会把要保留的回归基线一起删掉。

### 第三轮复核 2
裁定:还不能交 owner 拍板,但只差两处局部修补:第一轮七条 blocker 我逐条重核后确认全部实证解决(dry-run/apply 清单与行号、held 分支证伪、归属键量纲、PR-6c 的审计次序解法、PR-8 的 P-13 守卫账都对得上代码),新剩的两条不动推荐链本身——补上 PR-5 因 apply 进入源码派生守卫集而必须付的 carve-out 改写(tenant-scoped-write-guard.test.cjs:404-412/:444-446/:482-488 vs http-routes.cjs:6133),并把「sandbox 或 production 已配才真写」改成「canonical 目标表只有 server-config production 策略能打开(table-actions.cjs:1784-1788/:1818-1821)」外加一条 owner 问句,这两处改完即可拍板。

#### blockers
- 【PR-5 的守卫账少一支,而且落在必做 PR 上,与第二轮自己在 §2.5(b) 为 list 记的账同一类】§2.5(a) 把 apply/expansion-start 新增 scope 只当成 G9 计数 11→13,但 C:/Users/zhou/Downloads/dev/metasheet/plugins/plugin-integration-core/__tests__/stock-preparation-tenant-scoped-write-guard.test.cjs:394 的 value-bearing 集合是**源码派生**(handlersCallingOperatorValueScope,:315-325:凡 handler body 含 `resolveOperatorValueScope(` 即入集),:404-412 还断言派生集必须等于 PINNED 集。于是 PR-5 一旦在 `tableActionApply` 体内加一次 scope:①它进派生集 ⇒ :404-412 红(须显式 pin,可接受);②立刻吃到 :482-488 的通杀断言「不得出现 resolveTenantId(」,而 apply 自己 :6133 就是 `const applyTenantId = resolveTenantId(req, {})`(C:/Users/zhou/Downloads/dev/metasheet/plugins/plugin-integration-core/lib/http-routes.cjs:6133)⇒ **红**。稿子为 list 列的三条出路里,「先做 #5445 形状迁移」在 apply 上不可用——legacy `integration:write` 持有者在 :993-1000 早返回、根本不过 scope,把 apply 的租户推导整体换成 scope 会改他们的行为。现实只剩一条:把 apply 加进 :444-446 的 VALUE_BEARING_READS_WITH_GATED_LEGACY_TENANT,并按 :468-480 的形制把 :6133 逐字改写成 gated 三元(:472 断言精确源码串、:475-479 断言 resolveTenantId 恰好一次)。这不是「另起一次 scope」,是改 apply 值路径的租户推导 + 新增一条 carve-out + 两处 pin,PR-5 的 1.0d 与其描述都没有覆盖。expansion-start 同样要进 PINNED(它体内无 resolveTenantId 字面量,:482-488 可过)。
- 【把 apply 抬成危害第 1、并据此重排整个 PR 计划的那句前提,对稿子自己点名的那张表是错的】§1.4-1、§2 B「代价」、§7 M-f 三处都写「仅在配了 `sandboxPolicy`(:6171)**或** `productionPolicy`(:6174)的部署上真写」。反证:C:/Users/zhou/Downloads/dev/metasheet/plugins/plugin-integration-core/lib/stock-preparation-table-actions.cjs:1784-1788 —— 沙箱路径对 canonical objectId **无条件 403**(`STOCK_PREP_APPLY_SANDBOX_ONLY` / reason `prod_canonical`,注释自陈 *"the prod canonical target is never appliable on the sandbox path, regardless of policy"*),且 :1784 把缺 objectId 的 target 也当 canonical;而 canonical 正是 `plm_stock_preparation_main`(C:/Users/zhou/Downloads/dev/metasheet/plugins/plugin-integration-core/lib/stock-preparation-templates.cjs:652),也正是稿子引 project-board.cjs:585-588 说「deploy 级共享、无 tenant 列」的那张表。能授权它的只有 productionPolicy,而 productionPolicy 是 **server-config only、无 env 开关**(:1818-1821),校验在 :1838-1852。也就是说:在没有服务端 production 配置的部署上,一线对 canonical 目标表的 apply **今天已经是 403 fail-closed**,谓词上 apply 是纵深防御与「留认领事实」,不是堵一条敞开的主写路径;「sandbox 已配」这一半根本打不开这张表(它只能打开非 canonical 的沙箱副本,那是另一种风险量级)。owner 要在 B 与 C 之间选、要为 PR-5 的 1.0d 买单,拿到的必须是拆开 objectId 两种情形的版本,并附一条 owner 问句:目标部署(含 222)是否带 production 策略配置——只读复核无法代答。

#### minors
- §2.5(d) 逐条列了 P-13 armed 半边的 case 1/2/正控制/case 4,却漏了同一函数最后那段 :1079-1093(C:/Users/zhou/Downloads/dev/metasheet/plugins/plugin-integration-core/__tests__/stock-preparation-operator-pull-gate.test.cjs:1081-1093 对 DRY_RUN 与 APPLY 各跑一次外部项目号,断言 'P-13: dry-run and apply keep the behaviour they had',:1079-1080 的注释还写明「只有 reconcile 带着别人拥有的 projectNo」)。这恰好是 PR-5 要终结的那条不变式。即便靠新开关隔离能保持绿,也应在 PR-5 里写明「凭什么仍绿」或直接重写该段注释与断言。
- G9 计数预算只算到 13,后面两支没接:PR-6b 让 list 调 scope 会成为第 14 处调用点(handoff.test.cjs:2984 的 `assert.equal(callSites, 11)` 与 :2985-2987 的表头 marker 要再改一次),PR-6 的管理员改派路由是否也解析 scope 未交代。工时表按支给了钱,守卫账却只按 PR-5 记。
- PR-1b 与 PR-6c 互相拆台:PR-1b 的立论是 confirm 在 :8167 之前拿不到 projectNo,所以改成成功后追加一行;而 PR-6c 恰恰要在 :8167 之前做一次 decisionId→projectNo 的软查找。Q7 答「收窄」时 projectNo 已在手,PR-1b 那条附加审计行就是冗余。应写明 Q7=是 时两支合一(或 PR-1b 明确写成会被 PR-6c 取代)。
- §6 的五处注释被记了两次账:§6 末尾说「合起来是一支纯注释 PR(sonnet 0.2d),可以先合」,§5 PR-2 的 C 分支(0.1d)又写「顺带修 §6 的五处注释」;而 5.1d 合计里两个数字一个都没进。要么 PR-2 吸收并把 0.1d 提到 0.2d,要么 §6 独立成支并进合计。
- 两处引用归属不明,与本稿自己在 §6.5 立的「引用带全路径」规矩不一致:§1.2 在引 `stock-preparation-operator-scope.cjs:157-159` 之后,紧接着用裸行号 `:396-424`(assertOperatorMaySeeProject)与 `:121-134`(projectArchiveIsEmpty),读起来像同一文件,但 operator-scope.cjs 只有 393 行,这两处实际都在 stock-preparation-operator-project-directory.cjs;同样,`workbenchAccess.ts:354-357 / :245-249 / :204-209` 与 `plainLanguage.ts:642-655` 全稿未带路径,实际在 apps/web/src/services/integration/stockPreparation/ 下。
---

## 附录二:《大 BOM 后台作业 worker 化》设计稿三轮对抗复核记录

> 三轮均为只读核验;每轮作者按 blockers 修订后由两路 opus 复核。第四轮为定向修订(只改指定处),未再复核——残留意见即本附录第三轮部分。

## 第 1 轮对抗复核(opus 两路)

### 反驳 1
裁定:不能直接交 owner 拍板——推荐结论建立在一个被代码证伪的算术上(B1:450 秒是 `stock-preparation-apply-writer.cjs:540-594` 的逐行串行写,不是 HTTP 往返,所以 C 与「chunk 抬到 500」两下加起来大约只省下浏览器那 262 秒的等待,并把剩下的 450 秒变成一个无法中止的单请求),再加上两条必须先答的安全前置(B5:apply 作业无 actor 守卫、写侧弱于读侧;B6:租户门是 env 门控而非「无关」)。先把 apply 写入路径的串行成本测清楚、把这两条安全缺口写进 §4 的 owner 问题清单、并按 B2/B3/B4 修正 C 的风险栏与 PR-0/PR-A 的工作量,再拿去拍板。

#### blockers
- **B1 — 推荐所依据的算术与代码相反:C 消不掉那 450 秒。**
`applyStockPreparationPlan` 是**逐行串行**写。`plugins/plugin-integration-core/lib/stock-preparation-apply-writer.cjs:540-594` 是 `for` 循环,每条 decision 一次 `await`;`applyAddDecision`(`:369-391`)先 `findExistingRecord`(`:250-257`,一次 `queryRecords`)再一次 `createRecord`/`patchRecord`,`applyPatchDecision`(`:393-411`)同形。13151 行 = **26302 次串行 API 调用**,没有任何批量化。
反推现场实测:453.6s ÷ 13151 ≈ 34.5ms/行;100 行 × 34.5ms = 3.45s,与实测「≈3.4s/批」吻合到小数点(`docs/development/takeover-beiliao-20260821/48h-autonomous-run-record-20260906.md:52`)。**那 3.4 秒里 HTTP 往返约等于零,全部是服务端逐行写。**
于是稿子 §3 第 1 条的两个推论都不成立:
- 「C 直接消掉 131 次往返…把写入从 7-12 分钟压到分钟级」→ C 消掉的只是浏览器侧 131×2s≈262s 的等待,服务端 453.6s 一秒不减。结果是一个 ~450s 的单请求,不是分钟级。
- PR-2「chunk 100→500,132 批变 27 批」→ 批数变了,13151 次串行写没变:27×500×34.5ms ≈ 466s。**chunk 大小对总时长基本无效。**
稿子内部也自相矛盾:§3 引 `customer-delivery-guide-20260904.md:383` 说 3.4s「主要是 HTTP 往返」(可由 C 消掉),§1.8 又把它记成「3.4s 服务端」(不可消)。两者不能同真。
**真正的杠杆在整份稿子里一次都没出现**:`applyStockPreparationPlan` 的逐行串行循环,以及每行一次的存在性查询。这条不动,A/B/C 都只是把同样的 450 秒搬到不同位置。按现稿拍板,会拍出一个不解决问题的方案。
- **B2 — C 把「关掉页面」从「停手」变成「失控」,而 apply 侧根本没有 cancel。**
今天面板卸载置 `cancelled=true`(`apps/web/src/components/integration/stockPreparation/StockPreparationLargeBomPullPanel.vue:184-190`),驱动循环在 `apps/web/src/services/integration/stockPreparation/largeBomPull.ts:334` 与 `:343` 两处 `isCancelled()` 上停手——这是一线目前**唯一**的中止手段。
C 把 132 次调用合成一次之后,服务端循环没有任何客户端信号能停:apply 作业**没有 cancel 路由**(稿子 §1.1 自己列了这一条,却没接到 C 的风险栏),`STOCK_PREP_OPERATOR_PULL_STEPS`(`plugins/plugin-integration-core/lib/stock-preparation-workbench-access.cjs:250-327`)里也只有 `large-bom-expansion-cancel`。
所以 §3 第 5 条「降级路径就是今天的行为,不引入新失败模式」是错的:C 引入的新失败模式正是「生产备料表被一个无法中止的请求写满」。C 要做,必须同时给 apply 一条中止入口,而新增路由要同步改 `workbench-access.cjs:280-327` 和被 `deepEqual` 钉死的 `__tests__/stock-preparation-operator-pull-gate.test.cjs:1189-1208`——稿子把这笔账记在了「可选的 PR-5」,它应当是 PR-1 的**前置**。
- **B3 — PR-0「冲突面:0 处现有断言」是假的,现成测试正钉着它要改的那一行。**
`plugins/plugin-integration-core/__tests__/stock-preparation-large-bom-jobs.test.cjs:1994-2055`(`testCheckpointApplyRejectsConcurrentRunningChunk`)手工把作业行写成 `status:'running'` 落库(`:2038`),然后断言 `runLargeBomCheckpointApplyJobChunk` 抛 409 `LARGE_BOM_APPLY_RUN_IN_PROGRESS`(`:2040-2052`)且目标表只剩 1 行(`:2053-2055`)。PR-0 要改的正是 `lib/stock-preparation-large-bom-jobs.cjs:1269-1276` 这道拒绝:
- 规则若是「无 `runningSince` / 租约过期即接管」→ 这条手工持久行没有 `runningSince` → 变成接管 → 断言红;
- 规则若是「无 `runningSince` 仍拒」→ 今天卡在 `running` 的存量作业(正是要修的东西)一条都救不回来。
两条路都必须重写这个用例并重述其语义。(`:1961-1991` 的进程内单飞用例不受影响。)给 owner 的成本要写成「1 处,且连语义一起重述」。
- **B4 — 方案 A 的路由改写会丢掉 `largeBomJobResponse` 包装,那是一条有意的告知性守卫。**
稿子 §2-A 写的替换是 `sendOk(res, publicBackgroundExpansionJob(queuedJobWithRunning), 202)`。但这一族**八条路由的每一个返回**今天都过 `largeBomJobResponse(...)`(`lib/http-routes.cjs:6209 / 6223 / 6311 / 6377 / 6393 / 6434 / 6451`)。它的作用见 `:3980-4013` 的 FRESHNESS-DIVERGENCE NOTICE:大 BOM 路径不供 `installedFieldProperties` / `extFieldMapping`,`ext_` 列会停在旧 epoch 而周围规范列刷新到今天,「行看着新、租户列是旧的」在备料表里比缺值更糟——所以这条分歧被要求**在这一族的每个响应上显式声明**。按稿子写法照抄,202 就不带这个声明了。一份以「值面不变、守卫不动」自我描述的改造,第一处代码就动了守卫。
(顺带:大 BOM apply 路由 `:6427-6433` 确实不传 `installedFieldProperties`,而三条小 BOM 路由 `:5812 / :5979 / :6156` 都传——这条既有分歧稿子通篇未提。)
- **B5 — apply 作业没有 actor 守卫,写侧比读侧弱一层;稿子完全没提,而三个方案都会放大它。**
展开作业记 `actor`(`lib/stock-preparation-large-bom-jobs.cjs:585`、`:607`),run 前过 `assertLargeBomJobActor`(`lib/http-routes.cjs:6246`,定义 `:4422-4427`),`__tests__/stock-preparation-operator-pull-gate.test.cjs:493-530`(P-10)把「作业只由创建者驱动」钉死。
**apply 作业根本不写 `actor` 字段**(`createLargeBomCheckpointApplyJob` 只记 `principal`),apply run 路由(`http-routes.cjs:6395-6435`)没有任何创建者校验,只有 `assertApplyJobMatchesExpansion`(`:2820-2826`)——比对的是 jobId 配对,不是人。而 `large-bom-apply-run` 就在一线准入清单里(`workbench-access.cjs:316-321`)。
结论:同租户同 workspace 的另一个操作员,拿到一对 (jobId, applyJobId) 就能把别人的 checkpoint apply 用**别人作业里存的 permission** 推到底,往共享备料表写。今天的缓解只是「每次 100 行、页面一关就停」;A 让作业活得更久,C 让一次调用写完全部,PR-5 还要新增 resume 入口——三个方向都在放大它。
这条应作为独立前置 PR 排在任何 worker 化之前,并且是 §4 该问 owner 的第 6 个问题。(同类但更轻的一处:`cancel` 路由 `http-routes.cjs:6437-6451` 也没有 actor 校验,而 `workbench-access.cjs:278-279` 的注释写的是「it stops a job THIS caller started」——注释断言了代码没强制的事。)
- **B6 —「`x-tenant-id` 那个洞与本路径无关」说反了。**
稿子 §2-A 的 values-free 段这么写。事实:这一族路由的租户由 `largeBomJobScope`(`lib/http-routes.cjs:1266-1272`)→ `scopedInput` / `resolveTenantId`(`:1023-1033`)解出,与作业键的 `{tenantId}` 段(`lib/stock-preparation-large-bom-jobs.cjs:533`、`:547`)、以及 `loadTableActionSourceAdapter` 回退查绑定用的 `fallbackTenantId`(`:4466-4468`)是同一个值。
挡住它的恰恰是 `requireTableActionAccess`(`:988-1013`)里那道 `assertVerifiedTenantClaim(req)`(`:998`)——而它是 **env 门控**的(`:1313`,`MULTITABLE_STOCK_PREP_TENANT_CLAIM_REQUIRED`)。
正确说法是「**这条路径就在那道门后面,而那道门默认关**」,不是「无关」。响应体确实 values-free(`publicBackgroundExpansionJob` `:562-567` 只投影 token 与计数),但那是另一个命题。把一条 env 门控的纵深防御在给 owner 的稿子里写成「不相关」,等于把一个待决事项写没了。

#### minors
- **M1 — 对比表偏袒:B 的「做不出原子抢占」立论不成立,且漏掉了最近的先例。**
稿子以 `plugin_kv` 无 CAS(`packages/core-backend/src/plugins/plugin-durable-storage.ts:115-128` 确为无条件 upsert)推出 B「做不出抢一条队列项的原子性」。但同一个对象上就有 `consume`(`:129-141`,`DELETE … RETURNING`),那正是原子 pop。更要紧的是:仓里已有一整套现成的后台作业运行器——`plugins/plugin-elearning/lib/jobs.cjs`,`CLAIM_SQL` 用 `FOR UPDATE SKIP LOCKED` + `lease_until` + `claim_worker_id` 围栏 + attempts/死信,由 `plugins/plugin-elearning/index.cjs:73` 的 `startJobsWorker` 起 tick。稿子说 B 的先例只有 077/078 两张 lease 表,漏掉了这个最贴近的先例,于是把 B 估成 6-10 支 PR。
- **M2 — 三选一少了一个选项,且对照表两行是错的。**
由 M1 引出:「进程内」并不蕴含「不持久」——elearning 那套就是**进程内 + 持久租约 + 多实例安全**,跑在同一个后端进程里。稿子对照表的「多实例安全 A=❌ B=✅」「崩溃后恢复 A=❌ B=✅」是把进程边界与持久性混为一谈得出的。
真正的分界是「作业由带着已授权 adapter 的请求驱动」还是「worker 事后自己重建 adapter」——后者才是 `createHandlers` 闭包那笔账(`lib/http-routes.cjs:3482` 工厂、`:4457` 的 `loadTableActionSourceAdapter(req, …)` 确如稿子所述收 `req`),而这笔账对「A′ = 进程内 + 持久队列」同样成立。这个轴要写清楚,owner 才拍得动。
- **M3 — 迁移编号建议不完整。**
仓里有**两个**迁移目录:`packages/core-backend/migrations/`(编号 .sql,最高确为 086,注意 083 缺号)和 `packages/core-backend/src/db/migrations/`(时间戳 .ts)。最贴近的作业表先例 `zzzz20260826160000_create_elearning_jobs.ts` 在第二个目录里。「新表编号从 087 起」这句给 owner 之前,得先答「用哪套迁移」。
- **M4 — 文件路径写错(行号是对的)。**
`sealed-export-package-provenance.cjs` 在 origin/main 上不存在;实际路径是 `plugins/plugin-integration-core/lib/sealed-export/sealed-export-package-provenance.cjs`,`PINNED_RUNTIME_FILES` 确在 `:206-232`、`pluginHttpRoutes` 在 `:219-222`。哈希核对本身成立:`sha256(origin/main:lib/http-routes.cjs)` = `56cd0d53…7001b`,与 `lib/sealed-export/vectors/s6a-package-provenance-pins.json:73` 逐字相同。「`stock-preparation-large-bom-jobs.cjs` 不在任何 pin 列表」也复核为真(该文件十个 `PINNED_*` 列表全无 large-bom 字样)。
- **M5 — 可达性守卫被高估,两边成本都算多了。**
`__tests__/plugin-capability-reachability.test.cjs` 头注释 `:38-50` 自陈是**声明式**的:「没人加进来的能力它不检查」(false negative #2,还列出今天就有 30 个 lib 模块不可达于任何 production 根)。所以「新模块必须被 production 根按代码提到,否则判死代码」只对**已声明的 entry point** 成立。A 的新 worker 模块不加声明不会红;B 的「守卫的 production 根要扩」也不是自动会红的事。
- **M6 — 展开侧状态机描述与稿子自引冲突。**
稿子说 `paused`/`expired`「全文件仅出现在枚举和 cancel 的过滤列表(`:666`)」,但同段又引 `:922`(run 允许从 `queued/running/paused/failed` 进入)。实测 grep `lib/stock-preparation-large-bom-jobs.cjs`:`paused` 在 `:32`(枚举)、`:922`(run 允许列表)、apply 侧 `:42`/`:1277`/`:1332`;`expired` 只在 `:36`/`:47`,`:666` 那行是 `['cancelled','failed','expired']`。准确说法:**`paused` 在展开侧从不被写入但被允许作为入口态;`expired` 两侧都是纯死状态。**
- **M7 — 「C 的守卫冲突是 0」所据用例描述不准,且漏掉一处语义变化。**
`__tests__/http-routes.test.cjs:5556-5568` 不是「只有 1 条 decision」:它断言 `counts.created===1` **且** `counts.inactive===1`(`:5562-5563`),至少两条。结论(单批跑完、断言原样绿)仍成立。
但紧邻的 `:5536-5554` 钉住「沙箱门在 checkpoint 写之前开火、且这一次 run 一行都没写」。C 把门下沉进循环后,**403 的语义从「本次请求什么都没写」变成「写了 N 批之后被拒」**——`assertStockPrepApplyAllowed` 内部 `assertProductionPolicyNotExpired(policy, now)`(`lib/stock-preparation-table-actions.cjs:1840`)每轮吃新的 `now`,一个 450s 的循环里策略真的可能中途过期。这不是断言红不红的问题,是必须写进给 owner 的风险栏的语义变化,而稿子 §2-C 写的是「values-free 与租户边界:零影响」。
- **M8 — C 的并发描述不准。**
`runLargeBomCheckpointApplyJobChunk` 的单飞锁是**每 chunk 取一次、`finally` 放一次**(`lib/stock-preparation-large-bom-jobs.cjs:1257` / `:1336-1337`),循环体之间锁是松开的、库里状态是 `paused`(`:1332`)。所以反代掐断后客户端重发,通常不会像稿子说的「撞 409」,而是**开出第二条服务端循环**与第一条交替推进 checkpoint。行不会写重(`nextDecisionIndex` 单调),但「请求断了 = 没有第二条在跑」这个直觉是错的,长请求方案必须自己处理这件事。
- **M9 — A 的工作量口径两处都不准。**
(a) 数量:`expansion-jobs/:jobId/run` 在测试里的调用点是 `http-routes.test.cjs:5382 / 5398 / 5406 / 5659 / 5756`(`:5273` 是注册断言)、`b2a-trial-registry-wiring.test.cjs:878`(断言在 `:881-882`)与 `:1542`(断言在 `:1545-1547`)、`stock-preparation-operator-pull-gate.test.cjs:515` / `:524`(断言错误码,A 下仍绿)。
(b) 性质:`:5411`、`:5664`、`:5761` 断言的是 **run 响应体本身**的 `status`(`completed` / `failed`),A 之后 run 响应永远不可能带终态,只能改成「drain 之后再 GET」——那是**改断言内容**,不是插一行编排。给 owner 的成本要按「重写 3 条终态断言 + 改 3 处编排」报,不能报成「改编排不改内容」。
- **M10 — A 少记了一条会拉下整个进程的失败模式。**
今天 run 里 `storage.set` 失败会当场变成 501 返回调用者(`http-routes.test.cjs:5670-5698` 钉的是 start 路由的同类行为)。A 之后 202 已经发出,worker 里的 `storage.set` 抛出没有任何 catch——`logLargeBomJobFailure` 只在状态机两个分支里调(`lib/stock-preparation-large-bom-jobs.cjs:881` / `:972`),存储抛出会直接冒成 unhandledRejection。仓里没有全局兜底(`packages/core-backend/src/plugins/../core/PluginIsolationManager.ts:292` 那个是插件隔离用的)。默认 Node 姿态下这是整进程退出,`ecosystem.config.cjs:62` 的 `autorestart` 再拉起——一个租户的作业把全后端拉下水。A 的 worker 模块必须自带 catch,这条要写进设计。
- **M11 — B2a claim 的时序变化没写,且一处措辞过绝。**
A 把源读挪到请求之后,而 B2a 的 claim 仍在请求内取(`http-routes.cjs:6258-6280`,`runId: \`large-bom:${jobId}\``)。同 runId 重入确如稿子所说被允许(`lib/b2a-trial-registry.cjs:1091-1118`,`held=true / claimed=false` 走 `pageReads` 累加),**语义不变,但 claim 与真正的源读之间从此隔着一个不确定的排队窗口**——给 owner 的稿子里该有一句。
另:「同一个 jobId 无论**被谁**驱动 claim 语义不变」严格说不对,`assertLargeBomJobActor`(`:6246`)先把非创建者挡在 claim 之前。
- **M12 — 以下引用逐条复核为真,不用改。**
`start` 202(`http-routes.cjs:6209`)/ `run` 默认 200(`:6311`);`VALID_EMPTY_REQUEST_KEYS` 为空 Set(`:1566`)且 apply run 在 `:6398` 引用它;apply run 不传 `maxDecisionsPerChunk`(`:6427-6433`)→ 回落 `LARGE_BOM_APPLY_DEFAULT_CHUNK_SIZE = 100`(`large-bom-jobs.cjs:101`,硬顶 1000 在 `:102`);`workspace-default` 兜底(`http-routes.cjs:1266-1272`);八条路由注册在 `:66-73`、handler 在 `:6187/6212/6226/6314/6356/6379/6395/6437`;一线清单 `workbench-access.cjs:250` + `:280-327`,`deepEqual` 钉 11 个 step 在 `operator-pull-gate.test.cjs:1189-1208`;`ecosystem.config.cjs:60-61` 为 `instances:1 / fork`;`largeBomPull.ts:235/237/238/265-284/329-345` 全部对得上,模块头 `:12-23` 也确实写死了「nothing on the server advances a large-BOM job between calls」;基线祖先关系(`d636a053e` → `f26a3395c`,差 5 个提交)与「`tableActionLargeBomExpansionJobRun` 在 `:6226` 不是 `:6083`」两处订正都成立;`large-bom-drive.cjs` 确不在仓内。

### 反驳 2
裁定:不能直接交 owner 拍板——§1 的事实盘点基本可交(行号我逐条复核过,只有几处小偏差,pin 哈希实算一致),但推荐结论建立在一个没人测过的「3.4s/批 = 往返 + 落库」拆分上、并与它自己的对照表那一行 ❌ 互相矛盾,PR-0 的「0 冲突」已被两条既有断言证伪,C 的跨轮持锁与门中途抛出这两处语义还空着,对照表又缺了「离开页面还能不能跑完」这一行;补完那次实测并把 B2–B5 的语义定下来之后再交。

#### blockers
- **推荐结论与它自己的对照表互相矛盾,且唯一的收益数字没人测过。** 对照表写着 C「解决请求挂 450s ❌ 反而变成一个 450s 请求」,§3.1 却说 C「大概率把写入从 7-12 分钟压到分钟级」——两者不能同时为真。压缩量的全部依据是 `docs/development/takeover-beiliao-20260821/customer-delivery-guide-20260904.md:383` 的一句「≈3.4s/批,主要是 HTTP 往返 + 每批落库」,原文**没有**给往返/落库的拆分。而 `48h-autonomous-run-record-20260906.md:52` 的 453.6s 是仓外驱动跑的(稿子自己也这么说),那条路径里根本没有 2s 等待;稿子却把「131 次 2s 等待」算进 C 的收益,再和只存在于未实测浏览器路径的 12 分钟混成结论。若 3.4s 主要是 100 行落库,C 的地板仍是 ~450s,也就是对照表那一行的 ❌ 就是真实结果。交 owner 前必须补一次可证伪实测(同一批次:服务端 chunk 纯耗时 vs 端到端耗时,一条日志即可);Q1 的反代超时问题在这个数出来之前也无法判断。
- **PR-0 的「冲突面:0 处现有断言」是错的,而且 lease 的两种语义各撞一堵墙。** `plugins/plugin-integration-core/__tests__/stock-preparation-large-bom-jobs.test.cjs:1994-2065`(`testCheckpointApplyRejectsConcurrentRunningChunk`)把作业行直接改成 `status: 'running'` 落库(`:2023`),断言 409 `LARGE_BOM_APPLY_RUN_IN_PROGRESS`,并在 `:2063` 断言 `loaded.status === 'running'`。这行**不带任何 lease 字段**——和今天卡在 222 上的行同形。于是 PR-0 只有两种选法,稿子一种都没写:(a)「无 lease 字段 = 可接管」→ 这条测试红;(b)「无 lease 字段 = 永不接管」→ 已经卡住的行一条都救不回来,而救它们是 PR-0 的全部动机。另有 `:1947-1993`(`testCheckpointApplySingleFlightRejectsConcurrentQueuedRun`)钉住进程内 Set 锁的在飞行为。
- **PR-0 与 PR-1 合在一起会造出今天没有的并发缺陷,而 C 的服务端循环本身就把 TOCTOU 窗口放大约 130 倍;稿子把这两件事分别记成「让 C 更安全」和「多实例安全 ❌ 同今天」。** 其一:今天挡住「反代掐断→客户端重发」的正是 `stock-preparation-large-bom-jobs.cjs:1269-1276` 那道 409;PR-0 换成「超过 lease 窗口即可接管」后,一个仍在服务端跑着的 450s 循环会被第二个请求接管——除非 lease 每 chunk 续期,而稿子没写心跳。其二:`acquireCheckpointApplyRun`(`:1236-1252`)是在 `runLargeBomCheckpointApplyJobChunk`(`:1254`)**内部**取、`finally`(`:1336-1338`)释放的,每 chunk 一取一放;PR-1 在外层 `while` 循环调它,两轮之间锁是松的、状态是 `paused`(`:1332`)——恰好是「可重跑」状态,另开的浏览器 tab 或超时重试都能插进两轮之间。要保住今天的性质必须在循环外再加一层单飞(或跨轮持锁),这项改动稿子没列,且它会动到 `:1947-1993` 那条既有断言。
- **C 的循环体里两道门中途抛出时怎么办,稿子没定,而这条路径一定会走到。** `assertStockPrepApplyAllowed`(`plugins/plugin-integration-core/lib/stock-preparation-table-actions.cjs:1836-1857`)在生产策略下会过期窗口校验(`assertProductionPolicyNotExpired`),入参是 `now`。今天每个 chunk 是独立请求,策略中途过期只让下一次请求干净 4xx、checkpoint 完好;C 的循环里第 N+1 轮抛出时**前 N 轮已经写了行**,稿子没说这时是把整条请求变成 4xx(客户端收到错误但作业其实推进了一半)还是吞掉停在 `paused`。同一段还有第二个洞:`assertProductionCleanRowsWithinBound` 今天读的是路由层一次性载入的 `pendingJob.plan.decisions`(`plugins/plugin-integration-core/lib/http-routes.cjs:6421-6425`),循环第二轮起这份 `pendingJob` 已过期,`gate` 回调必须收上一轮返回的作业——稿子给的 `gate` 签名没交代这一点。
- **三案对照缺了对这次决策最关键的一行:客户端离开/断网后作业能不能跑完。** 稿子自己在 §1.5 坑 3 记下「离开页面 = applyJob 变孤儿」(`apps/web/src/components/integration/stockPreparation/StockPreparationLargeBomPullPanel.vue` 的 onMounted/onUnmounted),这对一个 7-12 分钟的操作是最贵的一条属性。A(+lease)修得掉;C 修不掉——C 只是把「浏览器挂 12 分钟转圈」换成「浏览器挂一个 450s 的请求」,关页/断网/切网照样丢。对照表里没有这一行,推荐段也没提,owner 会在看不到这项差异的情况下拍板。
- **B 被当成不可分割的整体估成 6-10 支 PR,而它真正贵的那一半不是 450s 所在的那一半——这是对比里最实的一处偏袒。** `createHandlers` 闭包抽取(`http-routes.cjs:3482` / `loadTableActionSourceAdapter` `:4457`)与「第二个持有源凭据的进程」这两条成本**只属于展开侧**,因为只有展开要载源适配器。apply 侧一行源凭据都不碰:`runLargeBomCheckpointApplyJobChunk`(`stock-preparation-large-bom-jobs.cjs:1254-1339`)只吃 `storage` + `recordsApi`,写的是多维表目标。「只做 apply worker」的 B 变体既不用抽 `createHandlers`、也不新增凭据持有进程,而 453.6s 恰恰全在 apply 侧。稿子把这个变体连同 B 一起否掉了。顺带:`packages/core-backend/src/plugins/plugin-durable-storage.ts` 除 get/set 外还有 `consume`(`:129-141`,`DELETE … RETURNING`,原子)与 `delete`(`:142-151`);稿子按 `ensureDurableJobStorage`(`stock-preparation-large-bom-jobs.cjs:487-507`)的两方法门就断言「没有 CAS…是方案 B 的全部难点所在」,漏掉了一个现成的原子领取原语。
- **PR-5 / Q4 漏了前端那份镜像清单和逐字段钉住它的断言,「不难,但要单独排一支 PR」因此低估。** 新增一条路由要同步改的不是两处而是四处:`plugins/plugin-integration-core/lib/stock-preparation-workbench-access.cjs:280-327`、`plugins/plugin-integration-core/__tests__/stock-preparation-operator-pull-gate.test.cjs:1189-1208`(deepEqual 11 个 step)、**`apps/web/src/services/integration/stockPreparation/workbenchAccess.ts:53-124`(前端同一份清单)**,以及 **`apps/web/tests/StockPreparationProjectBoard.spec.ts:331-332`——它把前端清单与后端清单逐字段 `toEqual`**。此外 `stock-preparation-operator-pull-gate.test.cjs:1473-1478` 断言「注册的 `/large-bom/` 路由条数 == 清单条数」(清单派生,改了清单会自愈,但要知道它在)。`plainLanguage.ts:1088` 也引用这份清单。

#### minors
- 路径写错:`sealed-export-package-provenance.cjs` 实际在 `plugins/plugin-integration-core/lib/sealed-export/` 下,不是 `lib/` 下;按稿子写的路径 `git show` 会报 does not exist。pin 值本身核对无误——我复算了 `sha256(origin/main:plugins/plugin-integration-core/lib/http-routes.cjs)` = `56cd0d53…`,与 `lib/sealed-export/vectors/s6a-package-provenance-pins.json:73` 逐字相同,`pluginPackageJson` 在 `:70` 也对。
- §1.3 说 `cancelled`/`expired`「只出现在『允许重跑』的过滤列表(`:1277`)」——`stock-preparation-large-bom-jobs.cjs:1277` 的列表是 `['queued','paused','failed']`,这两个状态其实哪儿都没出现,只在枚举 `:39-48` 里。
- §1.2 说 `paused`「全文件仅出现在枚举和 cancel 的过滤列表(`:666`)」——它也在 `:922` 的 run 允许列表 `['queued','running','paused','failed']` 里,稿子别处自己引了 `:922`。
- `largeBomBackgroundExpansionCaps` 的行号是 `stock-preparation-table-actions.cjs:413-434`,不是 `:413-431`(函数体到 `return caps` / `}`)。
- §2-C 说 `__tests__/http-routes.test.cjs:5556-5568`「只有 1 条 decision」——该用例断言 `counts.created === 1` **且** `counts.inactive === 1`(`:5562-5563`),至少两条决策。结论(远小于 chunk 100,单轮跑完,断言原样绿)不受影响。
- PR-3 大概率是空改动,不该单列一支:`apps/web/src/services/integration/stockPreparation/largeBomPull.ts:329-345` 的 while 循环本来就在拿到终态时退出(`:341`),服务端一次跑完后它自然只 tick 一次;而 `apps/web/tests/StockPreparationLargeBomPull.spec.ts` 全用 mock 双打(`:132` 的 `toHaveBeenCalledTimes(2)` 由 mock 序列决定),不会因服务端行为改变而红。PR-3 实际只剩面板文案。
- PR-2 的落点没定,而这决定它要不要排队:apply 作业行里**不存**动作快照/`largeBom` 配置(`stock-preparation-large-bom-jobs.cjs:1159-1194` 只落 `target`/`template`/`plan`)。所以「读动作配置」要么在 `createLargeBomCheckpointApplyJob` 里从 `sourceJob.actionSnapshot` 派生(不动路由、不重钉 pin),要么由路由传 `maxDecisionsPerChunk`(动 `http-routes.cjs` → 再重钉一次 pin,与 PR-1 排队冲突)。稿子没说是哪种。另:若 3.4s/批主要是落库,PR-2 才是收益最大的一支,现在被排在第三并挂在 Q2 上。
- 「动 pin 的 PR 单独排队合」引的 `docs/development/takeover-beiliao-20260821/stock-preparation-overall-plan-20260902.md:166` 原文写的是「改 `package.json` 测试链或 `pluginPackageJson` pin」,没写 `pluginHttpRoutes`。结论仍成立(两个 pin 同住一个 vectors 文件,任何两支都会冲),但引文与原文不逐字对应。
- 稿子引 `48h-autonomous-run-record-20260906.md:102`(第 16 条)当作本稿输入,但那一条自己写着「453.6 秒的写入循环就是在同一个请求里跑完的」——按代码这是错的:132 批 = 132 次独立 `POST …/apply-jobs/:applyJobId/run`,`http-routes.cjs:6395-6435` 每次只推进一个 chunk。既然要交 owner,顺手把那条记录纠正掉,别让两份文档互相打架。
- A 的 values-free 段说「零影响,且是三案里最强」——A 会把一个已解密的源连接句柄放进模块级队列里存活不确定时长,而 `ecosystem.config.cjs:62-64` 上有 `autorestart: true` + `max_memory_restart: '1024M'`。这不是新泄漏面,但也不该记成「零影响」。
- `plugin_kv` 的 `list()`(`plugin-durable-storage.ts:152-164`)确实只按 `plugin` 过滤、无 key 前缀;稿子说它「全表扫」——它有 `WHERE plugin = $1` 和 `ORDER BY key ASC`,措辞可以更准。不影响结论(拿待跑列表仍要全取或另建表)。

---

## 第 2 轮对抗复核(opus 两路)

### 复核 1
裁定:还不能交 owner 拍板:两路第一轮的 blocker 我逐条复核确认**全部已解决**(largeBomJobResponse 八处返回行号 6209/6223/6311/6353/6376/6392/6434/6450、pin 哈希 56cd0d53…、`:1994-2065` 与 `:1947-1992` 两条测试、`48h-…:48` 的 222 flag-on、`:52` 与 `:383` 的措辞分歧、`recordsApi` 进程内链条 `http-routes.cjs:6426`→`:3880-3886`→`plugin-scope.ts:389-413` 全部为真),但第二轮在自己新写的三处——PR-1(事实前置)、方案 D(唯一推荐)、G3/PR-4(安全与救援动机)——引入了同一类「未落地/未验证」的错误;§3 的 G1/G2 与 §7 的 owner 问题清单可以现在就单独交上去,方案栏、对照表与 §8 PR 拆分需要再过一轮。

#### blockers
- 【PR-1 落不了地,且『冲突面 0』为假 —— 全篇推荐都挂在它上面】稿子说把三个计时整数「落进 job.evidence 并由既有的 values-free 投影带出,不碰路由,冲突面 0」。三处代码反证:(1) `plugins/plugin-integration-core/lib/stock-preparation-large-bom-jobs.cjs:1221-1229` 的 `mergeApplyEvidence` 每个 chunk 用 `{resultStatuses, errorCodes, fieldCategories}` 三键**整体重建** `job.evidence`(调用点 `:1327`),写进去的累加器下一批就被抹掉;(2) `:1453-1475` 的 `summarizeLargeBomCheckpointApplyJobForEvidence` 是**闭合白名单**(`:1469-1473` 只投这三键),新键根本出不来,「由既有投影带出」为假;(3) 要扩投影就撞 `plugins/plugin-integration-core/__tests__/stock-preparation-large-bom-jobs.test.cjs:493-542` 的整对象 `assert.deepEqual`(`:521-540`)。真实成本 =「改 merge + 改投影 + 重写 1 条 deepEqual」,不是 0。这正是第二轮指控第一轮 PR-0『0 冲突』的同一类错误,只是换了一支 PR。
- 【方案 D 引的『现成批量读取器』拿不到 recordId,D 的落点不成立】`plugins/plugin-integration-core/lib/stock-preparation-table-actions.cjs:785-810` 的 `readExistingStockPreparationRows` 每行过 `unmapRecordFields`(`:774-783`):它取 `record.data` 并只回投 data 字段;而 multitable 行的形状是 `{id, sheetId, version, data, …}`(`packages/core-backend/src/multitable/query-service.ts:224-241` 的 `mapRecordRow`),所以**顶层 `id` 被丢掉**——恰恰是 `findExistingRecord` 交给 patch 用的那个值(`stock-preparation-apply-writer.cjs:377-383`、`:407-411`)。它还按 `projectNo` 过滤(`:790-792`)而非本 chunk 的 key 集,分页 1000×100(`:106-107`),所以既不是「一次批量读」,也收敛不到 chunk;按每 chunk 预读一次算,第 k 批要读回前 k-1 批自己刚写的行,132 批合计约 O(n²/2) ≈ 87 万行读。D 要么另写读取器(新守卫面 + 新测试),要么改一个规划链也在用的公共 helper——两者都不是稿子写的「照既有用法」。
- 【D 的『砍半』在被实测的那次负载上等于 0 —— 与第一轮同类的未验证算术】`docs/development/takeover-beiliao-20260821/48h-autonomous-run-record-20260906.md:52` 记的是 `add 13151` / **`existingRows 0`**:453.6 秒里那 13151 次存在性查询**全部落空**。而稿子给 D 定的安全规则是「映射未命中回退到今天的单行查询」(§4.1 与 §8 PR-2),于是在这次负载上一次查询也省不掉,收益为 0;要真省就必须把「未命中=不存在」当权威,那正是稿子自己排除的高风险变体,并且会把 `stock-preparation-apply-writer.cjs:261-265` 的 `duplicate_target_key` 唯一性守卫从写入时刻退回预读时刻。所以「唯一有代码结构支撑的收益估计……砍到约一半」只对以 update/重跑为主的负载成立,而 owner 要拍的恰恰是首次导入。对照表里 D 那一格「✅ 唯一(理论砍半)」必须按这个重写。
- 【G3 与 PR-4 的事实前提『唯一出路是手改 plugin_kv / 卡住的存量行一条都救不回来』是错的】同一个展开作业可以再建一个 apply 作业:`stock-preparation-large-bom-jobs.cjs:1128-1196` 的 `createLargeBomCheckpointApplyJob` 没有任何「已存在 apply 作业」的限制,`http-routes.cjs:2820-2826` 的 `assertApplyJobMatchesExpansion` 只比 `sourceJobId`;web 驱动每次拉取都无条件新建一个(`apps/web/src/services/integration/stockPreparation/largeBomPull.ts:316`);而 `applyAddDecision`(`stock-preparation-apply-writer.cjs:376-384`)对已写过的行走 patch。所以一条卡在 `running` 的 apply 作业的真实代价是「从 0 重跑一遍(≈450s)+ 一条永久垃圾 plugin_kv 行」,不是数据不可恢复。PR-4 的两难分支 (b)(「无租约字段 = 永不接管 → 存量行一条都救不回来,而救它们正是这支 PR 的动机」)因此不成立,G3 的紧急度、PR-4 的动机与那条『各撞一堵墙』的论证都要按这个重写。

#### minors
- PR-0 对**存量** apply 作业是 fail-open:`assertLargeBomJobActor` 在 owner 为空时静默早返回(`http-routes.cjs:4423-4424`),而已经躺在 `plugin_kv` 里的 apply 行既无 `actor` 也无顶层 `principal`(`stock-preparation-large-bom-jobs.cjs:1159-1193`)。稿子只说了「不落字段就等于没做」,没说新守卫对旧行永远不生效。便宜的补法是把回退链延到 `job.approval.principal`(`:1168-1173`),值现成。
- PR-0 的 apply-start 步骤描述与代码不符:该路由(`http-routes.cjs:6356-6377`)**从不加载展开作业**,`sourceJob` 是在库函数内部 `stock-preparation-large-bom-jobs.cjs:1133` 才载入的,所以「用已加载的 `sourceJob.actor` 校验触发者」在那个位置无从取值——要么路由自己多做一次 `storage.get`,要么把校验放进 `createLargeBomCheckpointApplyJob`。
- N5(「记录 API 只有单条操作,要批量写必须先改宿主」)说过了头:同一个 records 对象上就有 `runStockPreparationPersistUnitOfWork`(`packages/core-backend/src/multitable/plugin-scope.ts:414-452`),宿主实现是**一个数据库事务**包住 query/create/patch(`packages/core-backend/src/index.ts:2162-2211`),`plugins/plugin-integration-core/lib/stock-preparation-sync-run-persist.cjs:642` 已在用它。它确实不是批量 API(内层仍是单条),但「摊薄每行一次提交」不需要动宿主——大 BOM apply 恰恰是唯一不走这条事务的 stock-prep 写路径。代价要一并写明:UoW 路径对 sheet 归属**更严**(`index.ts:2180-2185` 未注册即抛,非 UoW 路径按 `pluginSheetScopeMode` 只告警)。这条应进 PR-1 的测量目标和 D 的备选栏。
- §1.1 的每行成本模型少算一半:`hooks.assertSheetScope` 自己就是一次 SQL(`plugin-scope.ts:176-194`,按 `sheet_id` 查 `plugin_multitable_object_registry`),所以每个记录操作 = 2 次 DB 往返,一行 `add` ≈ 4 次。整个 apply 作业的 sheetId 恒定,这 13151 次重复断言是与 D 同量级、但落点在宿主的另一条杠杆。PR-1 的计时应把它单列成第四个计数器,否则测出来的「查询 vs 写入」两半仍然解释不了 34.5ms/行。
- §2.8(a) 的守卫清单漏了一条会直接红的钉子:`__tests__/stock-preparation-large-bom-jobs.test.cjs:352-372` 用 `deepEqual` 钉死两个状态枚举(`stock-preparation-large-bom-jobs.cjs:29-37` / `:39-48`)。PR-4 / PR-6 若引入 `leased`、`stale` 之类新状态就撞它(复用既有 `cancelled`/`expired` 则不撞),该写进清单。
- 行号小偏差(不影响结论,但本稿自称逐条二次核实):PR-2 写 `applyStockPreparationPlan`「`:498` 起」,实际是 `stock-preparation-apply-writer.cjs:512`——`:507-511` 是「本函数是唯一写入咽喉、任何新调用者必须自带同一道门」的 SECURITY 注释,D 改签名时要连它一起读;`findExistingRecord` 是 `:250-267` 不是 `:250-268`;`PINNED_RUNTIME_FILES` 从 `sealed-export-package-provenance.cjs:206` 起但并不止于 `:232`(`s6aProvisioningCli` 在 `:233` 之后)。

### 复核 2
裁定:还不能整份交 owner 拍板 —— 第一轮六条 blocker(B1 算术/B2 中止手段/B3 PR-0 假 0 冲突/B4 largeBomJobResponse/B5 apply actor/B6 租户门)与第二轮七条**全部已解决且订正后的行号我逐条实读一致**,但第二轮自己新引入三条:PR-1(排第一的前置)按稿写会被两道闭合三键投影吃掉、白跑一次 222;§4.1/N5 把「一个 chunk 一个事务」这条比 D 更大的杠杆按错误前提提前关掉(`plugin-scope.ts:414-451` + `index.ts:2162-2167` 已有,`sync-run-persist.cjs:642` 已在用);§4.4 第 3 条拿一个与代码相反的机制(「重跑门要 await」)去加固对 C 的否决。改完这三处(约半天,都是文字与 PR 描述,不动代码)即可交。急的话可以**先单独交 §3 三个安全缺口 + §7 的七问**:那部分证据链完整、结论不依赖上述三条,PR-0(apply actor 守卫)可以立刻开工。

#### blockers
- 【新引入】PR-1(全稿排第一的事实前置)按稿子写会白跑一次 222,而稿子把它记成「由既有的 values-free 投影带出,冲突面 0」。apply 侧的 evidence 是**两道闭合三键白名单**,不是可扩展对象:(a) `plugins/plugin-integration-core/lib/stock-preparation-large-bom-jobs.cjs:1221-1229` 的 `mergeApplyEvidence` 每个 chunk 结束时把 `job.evidence` **整体重建**成只有 `resultStatuses`/`errorCodes`/`fieldCategories` 三个键(`:1224-1228`),任何写进 evidence 的计时整数在下一次 `:1327` 调用时就被抹掉;(b) 就算改了 (a),`summarizeLargeBomCheckpointApplyJobForEvidence`(同文件 `:1453-1475`)的投影仍是三键字面量(`:1469-1473`),不 spread `job.evidence`,所以 `publicCheckpointApplyJob`(`:569-574`)带出的响应里看不到计时,GET/run 都看不到 —— 222 上跑完只能去翻 `plugin_kv` 原始行。正确的成本口径是「改两个投影函数 + 一条新的 values-free 断言」(该文件的 `__tests__/stock-preparation-large-bom-jobs.test.cjs:2064` 有 `assertValuesFree(publicCheckpointApplyJob(loaded))`,新键必须过它),不是「0」。这支是后面所有分岔的依据,写错了就是一次假绿实测。
- 【新引入】§4.1 结尾与 N5「批量写入不能,不在这一层……要批量写必须先在宿主侧加接口」的前提被代码证伪,而它正是把 D 的天花板钉死在「砍半」的那句话。稿子引的 `packages/core-backend/src/multitable/plugin-scope.ts:389-413` 恰好在 `:414` 前一行截断:同一个 `records` 对象上第七个方法就是 `runStockPreparationPersistUnitOfWork`(`:414-451`),宿主实现是 `packages/core-backend/src/index.ts:2162-2167` 的 `poolManager.get().transaction(...)`,回调里给的是**事务内**的 `queryRecords`/`createRecord`/`patchRecord`(`:2188-2200`),并在事务里先取 canonical sheet fence + 两把 advisory lock(`packages/core-backend/src/multitable/stock-preparation-persist-unit-of-work.ts:76-95`)。这条路今天就有活的调用方:`plugins/plugin-integration-core/lib/stock-preparation-sync-run-persist.cjs:642-656`(把 `createTargetScopedRecordsApi` 绑在事务 API 上,`bindRecordsApi(transactionRecordsApi)`)。诚实的结论应是:直接复用**不行**,因为 `validateStockPreparationPersistUnitOfWorkInput` 硬钉 `sheetIds.length !== 4` 就抛(`stock-preparation-persist-unit-of-work.ts:38`)且要求 project/batch 两把键(`:41-48`)—— 但那是**改一个校验器的入参形状**,不是「宿主没有这个接口」。差别对 owner 是实的两点:①「一个 chunk 一个事务」这条比 D 更大的杠杆被稿子提前关掉了;② D 自称的「全部技术难点」(预读映射与写入之间的窗口可能建重复行,§4.1 风险段)在仓里已有现成答案(同一事务 + advisory lock),不必自己论证一遍。
- 【新引入】§4.4 第 3 条(以及 §9 反驳 2 第 3 行自称「这比『通常会开出第二条循环』更准确」)的机制说反了。原文:「C 自己要求每轮重跑两道门,而**重跑门是要 await 的**,窗口因此被 C 自己拉宽」。两道门全是同步函数,循环里重跑它们**一个 await 都不产生**:`plugins/plugin-integration-core/lib/stock-preparation-table-actions.cjs:1836` `function assertStockPrepApplyAllowed`(非 async)、`:1802` `resolveStockPrepApplySandboxPolicy`、`:1822` `resolveStockPrepApplyProductionPolicy`、`:1861` `assertProductionCleanRowsWithinBound`,路由处也确实没 await(`lib/http-routes.cjs:6412`、`:6423`)。反向还有一层:今天两次 chunk 之间锁根本是**松着整整一个客户端 tick**(`apps/web/src/services/integration/stockPreparation/largeBomPull.ts:344` 的 `wait(2000)` 加一次网络往返),所以「窗口今天很窄(一个微任务 tick)」也不成立,C 的窗口只会**更窄**。结论(可能开出第二条服务端循环交替推进 checkpoint)靠 `:1257`/`:1332`/`:1336-1338` 本身就成立,但这句话必须删掉或改写 —— 一份以「行号逐条核实、措辞按代码收紧」自我标榜的稿子,拿一个与代码相反的机制去加固结论,是给 owner 的证据链上的洞。

#### minors
- 【新引入,§8 PR-3】「落点已定,不必再问:从 `sourceJob.actionSnapshot.largeBom` 派生」不成立 —— 今天 `action.largeBom` 根本装不下 chunk 大小这个键:`stock-preparation-table-actions.cjs:361-370` 的 `normalizeActionLargeBomCaps` 对任何不在 `LARGE_BOM_BACKGROUND_CAP_FIELDS`(`:336`,只有 maxRows/maxPages/maxReadCount/maxElapsedMs)里的键当场 422 `unsupported largeBom cap`(`:367-368`)。要走这条落点得同时动 `:336` 的字段表、`:345` 的 ceiling 表和 `:361-394` 的校验器,而动作配置是被快照+哈希的(`:356-359`、`:461-463` 自陈)。要么改成「需要新增一个配置键」,要么承认它仍是待答项。
- 【新引入,§1.2/§1.3】成本结构漏了每次记录操作**自带一次独立 DB 查询**:`plugin-scope.ts:391/395/399/407` 每个方法先 `await hooks.assertSheetScope(...)`,而该 hook 在宿主侧是一次自己的池查询(`packages/core-backend/src/index.ts:2131-2161`,`assertPluginOwnsSheet(txQuery, …)` `:2148`)。所以 26302 次操作实际约 52604 次 DB 往返。方向对 D 有利(删掉一半存在性查询同时删掉它们的 scope 查询),但「唯一有代码结构支撑的收益估计=砍半」的算术口径要按这个更新,并且「每 chunk 把 scope 断言提到前面做一次」本身就是第三个杠杆,稿子里没有。
- 【新引入,§4.2】终态断言数少算一条。表里五行中有**四**行断言的是 run 响应体的终态:`__tests__/http-routes.test.cjs:5410-5414`、`:5663-5667`、`:5760-5770`,外加 `__tests__/b2a-trial-registry-wiring.test.cjs:1545-1547`(`assert.equal(ran.statusCode, 200)` + `status === 'failed'` + `authoritative === false`)。只有 `:881-882` 不是终态断言,但它断的是 `okSource.reads.length > 0`,A′ 之后源读挪进 worker,不 drain 同样红。给 owner 的成本行应写「重写 4 条终态断言 + 改 5 处编排 + 新增 `drainLargeBomWorker()`」。稿子刚在 §9 因同类低估点名反驳 1,自己又少算一条。
- 【新引入,§8 PR-2】`applyStockPreparationPlan` 的起始行写成 `:498`,实际是 `plugins/plugin-integration-core/lib/stock-preparation-apply-writer.cjs:512`(`:498` 落在 `summarizeErrors` 的 `.sort(...)` 里)。顺带:该函数头上 `:507-511` 有一段 SECURITY 注释,明写「这是 stock-prep apply 的唯一写入 chokepoint,任何新调用方必须套同一道门」,PR-2 改这个函数的签名时该引它。
- 【残留,§2.3】「`cancelled` 与 `expired` 除枚举外哪儿都不出现」是绝对句,`cancelled` 在 `stock-preparation-large-bom-jobs.cjs:666-667`(展开侧 cancel)就出现并被赋值。原意(apply 侧从不赋值)是对的,但得写成「在 apply 侧」,否则第一句就能被一行 grep 打掉。
- 【核对通过,备查】以下第二轮新增/订正逐条实读为真,不必再动:`largeBomJobResponse` 八处 `:6209/6223/6311/6353/6376/6392/6434/6450`(B4 订正正确);NOTICE `:3980-4005`、函数 `:4006-4015`、条件挂载 `:4007`;八条路由 handler `:6187/6212/6226/6314/6356/6379/6395/6437` 与 read/write 网关全对;G1(apply 作业 `:1159-1193` 无 actor 无顶层 principal、apply-start `:6356-6377` 与 apply-run `:6395-6435` 均无校验)、N3(`:4423-4424` 静默 no-op)、G2(`:6437-6451` vs `workbench-access.cjs:278-279`,`lbj:647` 仅形状检查)、G3 全部成立;N4(`PluginIsolationManager.ts:285-297` 在模板字符串里、闭合反引号 `:298`,全仓宿主进程无全局兜底)成立;pin 实算 `56cd0d53…7001b` 与 `s6a-package-provenance-pins.json:73` 一致,且 provenance 文件全文无 large-bom/apply-writer;`plan`/`target` 跨 chunk 不变(`:1326-1334` 只改五个字段)—— 对反驳 2 第 4 条后半的驳回是对的;两个迁移目录(sql 最高 086、083 缺号;ts 340 个)成立;PR-2 的新测试落在**已存在**的 `__tests__/stock-preparation-apply-writer.test.cjs`(package.json:64 已在册),所以不碰测试链、不重钉 `pluginPackageJson`,这条「可并行」成立。

---

## 第 3 轮对抗复核(opus 两路)

### 第三轮复核 1
裁定:不能整份交 owner 拍板:第一轮两路六条 blocker 我逐条实读确认**全部已解决**(PR-1 的三键闭合投影、readExistingStockPreparationRows 丢顶层 id、D 在 `existingRows 0` 上收益为 0、G3 可靠重建 apply 作业救回、UoW 接口已存在、四道门全同步无 await —— 行号与代码一致),但第二轮在自己新写的成本模型与 L3 章节里引入了五条同类错误(每行一次事务被漏、条数当时间用、每 chunk 全量 plan 四次 I/O 被漏、canonical fence 结论说反、宿主改动被缩成长度判断);**可以现在就单独交上去的是 §3(G1/G2/G3 三个安全缺口)、§7 的 Q1/Q2 与 §8 的 PR-0**,这部分证据链我逐条核实为真且不依赖任何性能模型;§1、§4.1–4.3、§5.1、§6 的②③④与 PR-2/3/4/5 必须再过一轮。

#### blockers
- 【新引入,§1.2/§1.3/§6③ —— 成本模型漏掉“每行一次事务”,直接推翻本稿的“唯一”与排序】十条往返的清单里没有 BEGIN/COMMIT,也没有连接取用。宿主的 records 门面是**每次调用自开一个事务**:`packages/core-backend/src/index.ts:1021-1022`(`createRecord: async ({sheetId,data}) => { return poolManager.get().transaction(...)`)、`:1059-1060`(patchRecord 同形);只有 `queryRecords`(`:999`)/`getRecord`(`:1041`)走 `poolManager.get().query` 不开事务。于是一行 add 的真实形状是 4 条(查)+ BEGIN + 6 条(写)+ COMMIT,13151 次**持久化提交**。§1.3 写“**这是本稿唯一有结构支撑、且与负载无关的削减点**”为假:L3(一 chunk 一事务)消掉的是每行一次 COMMIT,`index.ts:1021-1022` 是无条件的,同样有结构支撑、同样与负载无关、同样与 add/update 无关。§5.1 把 L1 记成“省 6/10”而把 L3 记成“未知,待测”,§6③ 由此得出“L1 是唯一值得先做的性能工作”——这个排序没有依据,而一次 fsync 提交的单位成本通常比一条 PK SELECT 高一到两个数量级,方向很可能相反。
- 【新引入,§1.6 —— 预测表只测语句条数,结论却用时间份额;且 453.6 s 的构成未经核实】(a) 六行预测全是“语句数”,而 §1.6 的结论写“预测成立 → 六成的时间花在读同一行元数据上”“不需要再讨论 worker”。条数比不是时间比:COMMIT(fsync)、TOAST 过的 jsonb 写、`meta_sheets` 主键 SELECT 三者单位成本差一到两个数量级,预测全中也推不出时间归属。修法是取 `pg_stat_statements` 的 `total_exec_time/mean_exec_time`(不只 `calls`),`log_statement` 计数法给不出这个数。(b) “3.4 ms/次对本机 PG 是合理量级”是本稿自己下的未测断言;若实测是 0.3 ms/语句,131k 语句只值 ~40 s,剩下 ~410 s 全在别处,整条推荐链塌。(c) 453.6 s 是驱动脚本的墙钟:`48h-autonomous-run-record-20260906.md:52` 记“写入循环 453.6s(≈3.4s/批)……驱动总 472.8s”,而该驱动脚本不在仓里(`git ls-tree origin/main | grep large-bom` 与 `git grep -l apply-jobs -- scripts` 均无命中),仓里唯一的同形驱动 `apps/web/src/services/integration/stockPreparation/largeBomPull.ts:344` 每批 `wait(2000)`——若 222 的脚本同样按 tick 睡,132×2 s = 264 s 就是墙钟的 58%,“每行十次往返 → 3.4 ms/次”当场失效。稿子必须先说清 453.6 s 里有多少是客户端固定等待,否则这是本稿第三次“拿没实测的算术当推荐依据”。
- 【新引入,§1.4/§5.1/§8 PR-5 —— 漏掉每 chunk 把整份 plan 读两遍写两遍的 O(N) 固定开销,而它正是 chunk 大小这条杠杆的真身】每推进一个 chunk:路由 `plugins/plugin-integration-core/lib/http-routes.cjs:6402-6407` 的 `loadLargeBomCheckpointApplyJob` 一次 `storage.get`;runner `plugins/plugin-integration-core/lib/stock-preparation-large-bom-jobs.cjs:1259` 又一次 `storage.get`;`:1304-1307` 与 `:1334` 两次 `storage.set` 各把**整份 job 回写**,而 job 里装着 `plan.decisions` 全量(13151 条,每条带 `record` 行载荷——见 `createLargeBomCheckpointApplyJob` 的 job 形状 `:1179`、`:1159-1193`)。存储原语是整值读/整值 upsert:`packages/core-backend/src/plugins/plugin-durable-storage.ts:101-114`(`SELECT value FROM plugin_kv`)、`:115-128`(`INSERT … value = $3::jsonb … ON CONFLICT DO UPDATE`),多 MB 的 jsonb 必然 TOAST,于是每批 2 次 detoast + 2 次整份解析/压缩/重写,132 批 = 528 次全量往返,外加 JS 侧 `cloneJson`(`:1268`/`:1302`/`:1335`)。这项开销随批数线性变化,正是“每批固定开销”。§1.4 的“chunk 100→500……批数变了,每行十次往返没变;仍然基本无效”、§5.1 的 0 收益格、§8 PR-5 的“收益按 §1.4 基本为零”,三处都建立在“没有可观的每批固定开销”这个未验证前提上。更糟的是 §1.6 的预测表**一行 `plugin_kv` 都没有**,那次测量跑完既证实不了也证伪不了它(且条数只会显示每批 4 条语句,看不出每条几 MB)。
- 【新引入,§4.3 与新发现 N4 —— 锁的结论说反了,而它是 Q6 给 owner 的裁决依据】稿子写“**非 UoW 的 `createRecord`(`records.ts:660-677`)只取自动编号锁、不取 canonical fence**”,并据此把 N4 列为本轮新增发现、把 L3 对 L2(b) 的作用限定为“只收窄到非 UoW 并发写者”。代码相反:`packages/core-backend/src/multitable/auto-number-service.ts:22-27` 的 `acquireAutoNumberSheetWriteLock` 是 `acquireCanonicalSheetFence` 的 deprecated 别名(注释自陈“the sheet-write lock KEY is unchanged”),两条路发的是同一条 `SELECT pg_advisory_xact_lock(hashtext($1))`、同一把 `canonicalSheetFenceKey(sheetId)`(`canonical-sheet-fence.ts:81-83`);UoW 侧 `stock-preparation-persist-unit-of-work.ts:81` 的 `acquireCanonicalSheetFencesInOrder` 调的就是同一个函数。所以非 UoW 的 create 与 UoW **互斥**,L3 对“并发 create 建重复行”的保护比稿子说的强。稿子自己在 §1.2 #6 引的正是这条链(`records.ts:675 → auto-number-service.ts:23-28 → canonical-sheet-fence.ts:81-83`),两处自相矛盾。真实的残余窗口另在别处:非 UoW 的 `patchRecord`(`records.ts:516-539` 走 `fenceWriterEntry`,不取 canonical fence)与纯读者。Q6 的 (a)/(b) 表述要按这个重写。
- 【新引入,§4.3/§8 PR-3/§7 Q5 —— 把宿主侧改动缩成“放宽一个长度判断”,owner 会按错误的规模拍板】稿子说 L3 只需“放宽 `stock-preparation-persist-unit-of-work.ts:38` 的 `sheetIds.length !== 4`或给一个单表变体”。同一个校验器 `:41-48` 还硬要 `project.projectId` 与 `batch.snapshotBatchId` 两个非空串,且两个 sheetId 必须落在 `sheetIds` 集合里;`:87-94` 的两把 advisory lock 的键就是从这两组值算出来的(`stockPreparationProjectLockKey` `:57-63`、`stockPreparationBatchLockKey` `:65-71`)。而大 BOM checkpoint apply 作业里只有单张 `target` 与 `plan`,**没有 snapshotBatchId**(job 形状 `stock-preparation-large-bom-jobs.cjs:1159-1193`)。所以“单表变体”= 为一条新写路径重新设计 fail-closed 形状与锁键集合,不是改一个长度判断。另有一条没写的代价:UoW 的 canonical fence 是事务作用域,一个 chunk 的整段时间(今天约 3.4 s)里**同表的任何 create(含 UI 手工新增)都排队等锁**,而今天是每行一取一放。这两点都要进 Q5,否则 owner 是在一个被缩小的问题上点头。

#### minors
- §3-G3 把代价写成“一条永久垃圾 `plugin_kv` 行”,低估了留存面:那一行装的是整份 plan(13151 条 decision,每条带行载荷)。`stock-preparation-large-bom-jobs.cjs` 全文没有 `storage.delete` / `consume` / TTL / 过期回收(`expired` 在 apply 侧从不赋值,稿子 §2.3 自己也这么说),而 web 驱动每次拉取无条件新建一个 apply 作业(`largeBomPull.ts:316`)。于是反复拉取同一个项目会按 MB 级持续堆积客户原始值的副本,永不回收。给 owner 的代价一栏应写“一份永不过期的整表载荷副本”,并把“作业行的清理/TTL”加成一个待答项。
- §8 PR-2 缺“调用链作用域”的落地机制,规模按现在的写法报低了。`createPluginScopedMultitableApi` 在 `packages/core-backend/src/index.ts:2050` 每个插件只构造一次,`plugin-scope.ts:389-413` 的闭包里没有 chunk / 请求边界可绑;而 `loadSheetAndFields` 是模块私有函数(`query-service.ts:168`、`records.ts:440`),cache 要从 `index.ts:979-1100` 的 records 门面一路穿进去。所以 PR-2 不是“给 `loadSheetRow` 加可选参数 + 两处透传”,而是改插件可见的记录 API 入参形状(或引入 AsyncLocalStorage 之类的隐式上下文)——评审面与 Q4 的措辞都要按这个更新。
- §1.2 #6 说 advisory lock “**无标志门,无条件**”,措辞过头:它在 `records.ts:669-677` 的 `else` 分支里(`linkWriterFencePlan` 为 null 时才走),flag on 且有 link 计划时改走 `enterLinkWriterFencePlan`。结论(每行至少取一把 sheet 级 fence)不变,但本稿的自我标准是“措辞按代码收紧”,这一句该改成“两条分支都取 fence,flag 关时走自动编号别名那条”。
- §1.2 的“≈3.4 ms/次”与 §1.5 保留的 “≈3.4s/批” 是两个毫无关系的 3.4,同一份稿子里相邻出现,给 owner 读极易串。建议把前者换成一个不撞车的表述或直接删掉(反正它按 blocker 2 已不可用)。

### 第三轮复核 2
裁定:第一轮 blocker 逐条裁定:复核1 B1(PR-1「冲突面 0」为假)= 已解决——§8 PR-1 按 `mergeApplyEvidence`(large-bom-jobs.cjs:1221-1229,三键重建 `:1224-1228`,调用点 `:1327`)+ 投影三键字面量(`:1469-1473`)+ 测试整对象 deepEqual(`__tests__/…large-bom-jobs.test.cjs:521-540`,后接 `:541`)重报成本,并给出 §1.6 零代码替代,我逐条实读一致;复核1 B2(批量读取器拿不到 recordId)= 已解决——§4.2/PR-4 明写必须自写 chunk 级读取器,`unmapRecordFields:774-783` 丢顶层 id、`query-service.ts:231` 的 id、`apply-writer:377-383` 用 id 三处属实;复核1 B3(D 在 existingRows 0 上收益为 0)= 已解决——`48h-…:52` 与 `customer-delivery-guide-…:383` 均记 existingRows 0,D 已降级、表 5.1 单列实测收益行;复核1 B4(G3「救不回来」为错)= 事实层已解决(`createLargeBomCheckpointApplyJob:1128-1196` 无「已存在 apply 作业」限制、`assertApplyJobMatchesExpansion` http-routes.cjs:2820-2826 只比 sourceJobId、add 走 patch `:377-383` 全部为真),但**结论层未解决**,见 blocker 4:该路径在产品里够不着;复核2 B1 = 同复核1 B1,已解决;复核2 B2(UoW 已存在)= 已解决(plugin-scope.ts:414-452、index.ts:2162-2211、校验器 `:38`/`:41-48`、活调用方 sync-run-persist.cjs:642-656 全部核实),但新写的 L3 引入了 blocker 1 与 blocker 2;复核2 B3(「重跑门要 await」说反)= 已解决——四道门确为同步 `function`(table-actions.cjs:1802/1822/1836/1861),路由 `:6412`/`:6423` 无 await,已删除该机制且方向反转正确。另外我把稿子的守卫清单、八条路由与八处 `largeBomJobResponse`(全仓恰九处命中)、pin 实算 `56cd0d53…7001b` 与 `s6a-package-provenance-pins.json:73`、`PINNED_RUNTIME_FILES:206` 起/`pluginHttpRoutes:219-222`、两个迁移目录(sql 最高 086、083 缺号;ts 340)、elearning 租约协议、ecosystem `:60-64`、`PluginIsolationManager.ts:285-297`+`:298` 全部第三次实读,均为真。能否交 owner 拍板:**安全那一块能,性能与 worker 化那两栏不能**——§3(G1/G2/G3 的事实部分)、§7 的 Q1/Q2、§8 的 PR-0 证据链完整可以立刻单独交并开工(PR-0「冲突面 0」我也核过:`publicCheckpointApplyJob:569-574` 只投 summarize 结果,新增 `actor` 不进响应、不撞既有断言);§1.2–§1.6、§4.1–§4.3、§5.1、§6 的 ③④、§8 的 PR-2/PR-3/PR-4、以及 G3/PR-6 那条必须按上面四条 blocker 再过一轮(都是口径与文字改动、不动代码,约半天)才谈得上整份拍板。

#### blockers
- 【新引入,§4.3 + §9.3 N4】「非 UoW 的 createRecord 不取 canonical fence」被本稿自己的证据链证伪,L3 的「诚实边界①」与 Q6 的风险框定要重写。稿子 §1.2 #6 自己写的链条就是 `packages/core-backend/src/multitable/records.ts:675` `acquireAutoNumberSheetWriteLock` → `packages/core-backend/src/multitable/auto-number-service.ts:23-27`(函数体只有一行 `await acquireCanonicalSheetFence(query, sheetId)`)→ `packages/core-backend/src/multitable/canonical-sheet-fence.ts:81-83`——自动编号锁**就是** canonical fence,同一个 helper、同一把 key(`canonical-sheet-fence.ts:17-19` 明写「the lock KEY string 是 auto-number 早就用的那一个,NEVER renamed」)。`canonical-sheet-fence.ts:23-24` 更直接点名:flag OFF 时 `acquireCanonicalSheetFence` 的调用方是「REST/plugin create, form submit, auto-number backfill」这些**无条件**call site;UI/REST 建行路径 `packages/core-backend/src/multitable/record-service.ts:573` 也在 else 分支里无条件取同一把。而 UoW 侧取的是 `packages/core-backend/src/multitable/stock-preparation-persist-unit-of-work.ts:81` 的 `acquireCanonicalSheetFencesInOrder`,内部逐张调 `canonical-sheet-fence.ts:97` 的同一个函数。所以「L3 把 L2(b) 的窗口收窄到『非 UoW 并发写者』,没有关闭它」这句的机制说反了:所有**建行**路径取的是同一把 sheet 级 fence(不取它的是 `records.ts:537` 的 patch 路径 `fenceWriterEntry`,而 patch 建不出重复行)。§4.3 那段、§9.3 的 N4 行、Q6 里「叠加 L3 只能收窄不能关闭」的说法都要按这个重新推导——这正是给 owner 拍 (a)/(b) 的那条风险依据。
- 【新引入,§4.3/§8 PR-3】L3 把一个 chunk 包进一个事务会改掉写侧的失败语义,并把 G3 从「罕见崩溃」抬成「任何一次库错误」,而 PR-3 现在按「宿主放宽校验器 + 插件包一层、冲突面 0」报成本。证据:`plugins/plugin-integration-core/lib/stock-preparation-apply-writer.cjs:569-593` 是**逐行 catch**——单行失败只 `counts.failed += 1` 并继续,`:596-601` 据此算 `applyStatus`,再由 `stock-preparation-large-bom-jobs.cjs:1231-1234` 的 `terminalApplyStatus` 把 `failed>0` 折成 `partial`。一个 chunk 一个 PG 事务之后,第一行真正来自库的错误会 abort 整个事务(后续语句一律 25P02),除非每行加 SAVEPOINT——「100 行里坏 1 行仍写进 99 行」这条今天的契约就没了。更硬的一层:`runLargeBomCheckpointApplyJobChunk`(`:1254-1339`)在写 `running` 落库(`:1304-1307`)与写 `paused`/终态(`:1332-1334`)之间**只有 finally 放锁(`:1336-1338`),没有 catch**,所以 UoW 一抛,作业就永久停在 `running`,之后每次 run 都在 `:1269-1276` 抛 409——即稿子 §3-G3 描述的那个死锁态。于是「G3 该修但不阻塞任何东西」(§6 理由 4)与「PR-3 = 两个文件」都不成立:要么 PR-3 里同时给 chunk 运行器补 catch/失败落库,要么 PR-3 必须排在 G3 修复之后。
- 【新引入,§1.6 + Q3 + §6③】被推为「第一位事实前置」的那次测量,按稿子写的口径答不了它自己提的问题。(1) 预测表只要**语句数**,而 2N/2N/2N 这三行是代码结构直接决定的(`plugin-scope.ts:395` 与 `:399` 每次记录调用各发一条 registry 查询;`query-service.ts:258` 与 `records.ts:680` 各自再走一遍 `loadSheetAndFields` → `loaders.ts:41-58` + `:60-76`),读代码就已确定,几乎不可能被证伪——它测不出新信息。(2) 稿子却由它直接跳到「**六成的时间**花在读同一行元数据上」(§1.6)与「这个数出来之前 L1/L2/L3 没法排序」(Q3):**调用次数不是时间占比**,六次小主键 SELECT 与一次 INSERT + 一次修订写 + 一把 advisory lock 的单价完全不同。要让 ③ 可判,同一次采集必须带上每条语句的 `total_exec_time`/`mean_exec_time`(pg_stat_statements 本来就有)与一个 chunk 的墙钟,否则 owner 批的是一次答不了排序问题的 ops 动作。(3) 附带:§1.2 的「453.6 s / 131,510 ≈ 3.4 ms/次,这个模型能解释」默认了 r11 驱动脚本两批之间不 sleep,而产品侧驱动每批之间明确 `await wait(2000)`(`apps/web/src/services/integration/stockPreparation/largeBomPull.ts:344`);若 r11 脚本同形,131×2 s ≈ 262 s 是纯等待,每语句就是 ~1.4 ms 而不是 3.4 ms。驱动脚本不在仓里(仓内只有 `docs/operations/data-factory-large-bom-c3c4-entity-machine-validation-runbook-20260608.md` 提到 apply-jobs),这个前提要么写明要么把这句「算术对账」删掉——一份以「不拿未实测算术当依据」为主张的稿子,不该再留第三处。
- 【新引入,§3-G3 / §9.1-B4 / §8 PR-6】G3 降级所依赖的救援路径在产品里够不着,「代价 = 从 0 重跑 ≈450 s + 一条垃圾行」低估了。稿子的救援动作是「对同一个展开作业再建一个 apply 作业」,并引 `apps/web/src/services/integration/stockPreparation/largeBomPull.ts:316` 作证——但那一行是在**同一次拉取内**、紧跟一次全新展开之后发生的:唯一的客户端在 `largeBomPull.ts:253` 无条件 `api.startExpansion(projectNo)` 开**新的展开作业**,面板 `StockPreparationLargeBomPullPanel.vue:161-182` 挂载即跑这条整链,前端没有任何「对既有 jobId 再建一个 apply 作业」的入口(要用得直接手打 `POST …/expansion-jobs/:jobId/apply-jobs`)。一线的真实恢复动作是整条重拉:源重读 + 重规划 + 450 s,而且新展开作业 = 新 jobId = 新 Run 身份 `large-bom:${jobId}`(`plugins/plugin-integration-core/lib/http-routes.cjs:6278`),B2a 注册表**武装时**同一张注册会以 `operation_already_consumed` 拒掉不同 Run(`plugins/plugin-integration-core/lib/b2a-trial-registry.cjs:1103-1108`,只有同 runId 才走 `:1110-1118` 的重入),即还需要 owner 侧补一张新注册——而 222 是否武装正是本稿自己挂着的 Q9。§3-G3 的代价行、§6 理由 4(「本轮给 owner 减负最实的一处」)与 PR-6「建议选 (b)」的论证都要按这个口径重述。

#### minors
- §1.1「整条写入路径上 HTTP 往返为零」与本稿 §1.5/§2.1 自己写的「132 批 = 132 次独立 `POST …/apply-jobs/:applyJobId/run`」互相打架;据此把 `customer-delivery-guide-20260904.md:383` 的「主要是 HTTP 往返」判成「错的」也过头了。准确的订正是「不是每行一次 HTTP,是每行多次库内往返;每批仍有一次 HTTP」。
- §1.2 #6 说自动编号锁「无标志门,无条件」,但 `records.ts:675` 位于 `if (linkWriterFencePlan) … else` 的 else 支(`:673-677`),只有在 writer-fence 标志关(即 Q7 那个未知数)时才必然走这一支;flag 开时走 `enterLinkWriterFencePlan`,语句更多。改成「flag 关时无条件」即可。
- §1.2 的每行清单漏了一对:宿主的 `createRecord` 每行**自带一个事务**——`packages/core-backend/src/index.ts:1022-1040` 是 `poolManager.get().transaction(...)` 包住 `createMultitableRecord`,所以每行还多一对 BEGIN/COMMIT(查询侧 `:1000-1020` 则是裸 `poolManager.get().query`)。这条只会加强 L3(「摊薄每行提交」)的理由,该进 §1.2 的表而不是留在表外。
- §4.1 代价第 1 条「所有插件、所有 UI 的记录操作都走 `loadSheetAndFields`」是个能被 grep 打掉的绝对句:`loadSheetAndFields` 在 `query-service.ts:168` 与 `records.ts:440` 各有一份**未导出**的私有实现,UI/REST 建行走的是 `record-service.ts`(自己查字段,`:580` 起),不经过它们。这对 owner 是好消息(L1 的爆炸半径比稿子写的小),但绝对句要收紧成「`records.ts` / `query-service.ts` 的调用方」。
- 行号小偏差(不影响结论):路由条数守卫实际在 `__tests__/stock-preparation-operator-pull-gate.test.cjs:1475-1479`(filter 在 `:1475`,assert 在 `:1476-1479`),稿子写 `:1473-1478`;`plugin-durable-storage.ts` 的 `list` 收在 `:165`(稿子写 `:152-164`);`normalizeActionLargeBomCaps` 在 §0.2 写 `:361-370`、在 §8 PR-5 写 `:361-394`,同一份稿子里两个口径,统一到函数真实范围。
