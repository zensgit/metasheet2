REVIEW-BASE: 81b2c1905

# O1′ 裁决记录(owner,2026-08-29)

> 送达方式:owner 于 A 机通道对《O1′ conflict matrix(修正版,同目录)》裁决卡答复「按你建议执行」,依《跨机开发公约》规则 6 当日落库。本文即该指令的正式跨机送达。values-free。

## 裁决:五问全部取 A

| 问 | 裁定 | 含义 |
|---|---|---|
| Q1 已有 canonical 行人填哪些列 | **A** | 开放全部 16 human 列(template 8 列 + 客户包 `ext_` 人列);T-HUMAN-WALL 继续拦机器,人直接编辑不触墙 |
| Q2 新 held 行值填在哪 | **A** | 填 ledger `resolvedValue`/`resolvedAuxValue`(schema 已预留);解除 `CONFIRMATION_DECISION_VALUE_ENTRY_UNIMPLEMENTED` 拒绝并定义消费规则;**无 schema 迁移** |
| Q3 system-only 占位行 | **A** | 允许(即现状);配套要求:T-2 对账口径须显式区分「未填」与「差异」 |
| Q4 值/decision/指纹绑定 | **A** | 全绑 ledger 行(已实现);须为 3 类匿名 hold 补身份(见矩阵 §3 脚注) |
| Q5 指纹变化 supersede | **A** | 沿用现有语义 + 4(a) 修复(**已合入 main:PR #5312 reopen-on-return**);复活行清空旧 resolutionAction、人须重确认(W-4 保守默认,owner 予以确认) |

## 随裁决生效的边界(不变)

- 机器永不写 human band(结构墙不动);production Apply 保持关闭;K3 外部写永久禁令(G-4)不受影响。
- ledger 从「决定账本」扩为「值账本」后,values-free 证据面须重新界定:**值列内容不得进入任何证据/日志/报告**,证据只允许计数、指纹、状态与列 id。
- v1.2 :349「同 subject 新 generation」的配套二选一,按与 Q5-A 一致的方向(原子 supersede)**建议** Codex 落入 Decision Register;该 register 属 Codex 工作区,正式生效以其入册为准。

## 解锁的开发(O1′ 实装波,规格另出)

1. 解除值录入拒绝:ledger 值列消费规则 + confirm 面接受 `resolvedValue`(值列 values-free 边界照上);
2. `accept_current` / `manual_hold` 语义实装(退出 `CONFIRMATION_DECISION_ACTION_UNIMPLEMENTED`);
3. 3 类匿名 hold(`missing_expanded_idempotency_key` / `missing_existing_idempotency_key` / `c2_row_error` 族)身份定义——注意矩阵 E2 校正:`c2_row_error` 是 ~10 种 expander 类型的伞名,身份方案须按真实类型族设计,不得按单一类型设计;
4. 人工列值经确认后的消费投影(Q1/Q3 组合:人直接编辑 canonical human 列即产权行为,无需机器代写)。

前置依赖照旧:实装波基于 W 列车合并后的 main;E2 伞名拆解先出短 spec 再动代码。

## 附:同日第二项裁决——`/stock-prep` 工作台(owner,2026-08-29)

**采纳,定位收窄为「确认队列工作台」**:作为 O1′ 人工确认闭环的操作入口(待确认队列、confirm/accept_current/manual_hold 操作、值录入面),不再承担泛化 MVP 演示职责;其余 MVP 面(快照页等)不随采纳复活,按各自既有裁决。随之 **O2(R-11 前后端权限对齐)的对象收敛为该页**:页面能力与 admin 路由词表对齐前,该页保持 admin-scoped。悬置两周的三选一至此关闭。
