# 备料 48 小时自主开发计划(2026-09-05 22:45 → 2026-09-07 22:45)

> owner 离开电脑 48 小时。本计划由我自主执行:通知驱动推进,每 3 小时巡检一次(session cron `a5c30e34`)。进度页面(同一链接持续更新)见交付总结;本文是计划正文,进度不在这里改。

## 0. 不变的护栏

- **凭据**:PLM 测试库/生产库账号密码永不经我手、不进任何文件;令牌只用 `JWT_SECRET` 现签短期票,唯一例外是 222 上定时任务用的服务账号令牌(专用账号、登录禁用、7 天有效、文件 ACL 仅管理员,见 W2)。
- **范围**:只碰 222 与本仓库;钉钉待办、宜搭不做;客户生产库不连。
- **子代理**:只在自己的 worktree 干活;仓库外一律"只报告不动手";不删任何不是自己创建的东西。
- **合并**:CI 全绿 + 保证型 PR 必过对抗审查(opus 多路 → 逐条反驳)才合;用 zensgit 合完切回 skiyakubali0-cloud。
- **owner 决策**:能用"默认关闭 + 可回滚"绕开的,记假设后照做;绕不开的(比如改客户可见的语义)记进待拍板清单,不越权。
- **222 变更**:先备份(app.env / pg_dump),改完按升级脚本第 7 步重载 env 再重启,并用一次试算核对生效。

## 1. 模型分派(按难度,不按顺手)

| 难度 | 典型任务 | 模型 |
|---|---|---|
| 机械 | 文档逐行改、配置 JSON、登记测试链、改文案 | haiku |
| 常规 | 按规格实现与配套测试、只读调研、运维脚本、夹具生成 | sonnet |
| 高 | 安全边界设计、对抗审查、跨模块语义、provenance 变更 | opus |
| 裁定 | 综合、取舍、222 操作、对客户可见的表述 | 我(fable) |

对抗审查一律 opus 多路 + 逐条反驳;实现者与审查者永不是同一个代理。

## 2. 波次

### W1 收口(T+0 → T+4h)

**目标**:上一波三件在飞的东西全部落地。

| 事项 | 动作 | 模型 | 完成判据 |
|---|---|---|---|
| #5492 子树桥接(草稿) | 工作流:safety/semantics/regression 三路审 → 每条 finding 一个反驳者 → 确认项发回原实现者修 → 转 ready → 合 | opus 审 / opus 修 / 我裁 | CI 全绿、无 blocker、七处偏离逐条有结论 |
| 定时拉取脚本 + 看板"上次同步" | 等 sonnet 交 PR → opus 两路审(脚本安全:令牌不落盘不打印、租户声明检查;lastPulledAt bounded 语义)→ 修 → 合 | opus 审 / sonnet 修 | 同上 |
| #5490 设计记录、#5491 读预算与 pm2 写法 | CI 绿即合 | 我 | 已合 |

**风险**:对抗审查可能推翻实现者的偏离 (3)(子树根的 expandChildren 无二次过滤)。若被判 blocker,W1 内只加最小的 matchesByField 后置过滤(不重构),否则移到 W4。

### W2 上 222 实证(T+4h → T+12h)

**目标**:把"更好的方案"在客户测试 PLM 上跑通,而不是停在单元测试。

1. CI 打包(main 合后 SHA,tag `r10`)→ scp → 备份 → 就地升级脚本 → 健康检查。
2. 动作 JSON 加 `projectSubtree`(`parentIdField=Parent_OBJ_ID`、`pathIdField=path_id`、深度 1、节点/根上限默认),`declaredBridge` 不改(预检那条 blocker 与找根轴无关,已写明)。按第 7 步写法重载 env 重启。
3. 对项目 `2-20231625` 试算:期望子树发现 6 张表头、6 个根件出行、缺子件的行进确认队列并显示"去源系统补"提示;导出走一遍。证据(values-free)记入 `222-rehearsal-full-run-20260904.md` §10。
4. 注册 222 每日 06:00 **只试算**计划任务:建专用管理员服务账号(登录禁用、租户绑定),签 7 天令牌写入仅管理员可读的文件,脚本从环境变量读;跑一次核对日志 values-free、退出码正确。
5. runbook 与交付说明加"子树桥接怎么开、看什么"。

**模型**:打包/升级/配置(我,机械部分 haiku 校对文档);证据整理 haiku;说明文字我写。
**回退**:升级脚本自带逐路径恢复命令;配置回退 = 恢复 `app.env.pre-*` 备份 + 重载重启。

### W3 客户会撞的第二层问题(T+12h → T+26h)

三件并行,各自 worktree:

**W3a 缺件清单**(sonnet 调研 → opus 设计 → sonnet 实现 → opus 审)。客户在真实数据上第一件事就是撞 `missing_component`;现在只告诉他"去源系统补",不告诉补哪个。目标:试算面板列出缺件(零件号、所在 BOM、父件、层级),可复制、可导出 CSV。完成判据:前端 spec 登记进 `run-required-web-tests.sh`,守卫测试过;222 上对 `2-20231625` 看到 45 个缺件条目(此前实测数)。

**W3b 源绑定解析器 workspace 回退**(sonnet 实现 → opus 审)。第三处 workspace 洞:reconcile 从 principal 推 `workspace=null`,UI 用 `default`,两边读不到同一条绑定,现在靠"两处各写一条"顶着。调研发现方向与 #5471 **相反**(UI 写在 `'default'`,后台用 null 读),照抄修不了。裁定方向 B:null 调用者在租户下恰有一条非 null 绑定时回退取用,≥2 条拒绝(fail-closed),精确命中优先,非 null hint 未命中仍不回退(保住既有测试)。完成判据:只写 UI 那一处两边都读到;222 上线后删掉 null 那条绑定,拉取与对账仍通。根因(Web 把租户写进 workspaceId)记为 owner 决策。

**W3c 大 BOM 分批路径实测**(sonnet 生成脚本,我操作 222)。在 `synth_plm` 生成 12000 行合成 BOM,临时切绑定到合成源 → 试算应转 `large_bom_bounded` → 后台分批 apply → 校验行数、导出 → 切回客户 PLM。完成判据:交付说明 §7.1 那句"未实测"改成实测结论(通过或发现的问题)。

### W4 安全纵深(T+26h → T+38h)

**opus 设计 + 实现,opus×3 对抗,我裁定**。两处:

1. **读取二次过滤**:`expandChildren` 对 bomHead/bomDetail 在 `filtersApplied:false` 源下无后置过滤(订单路径与子树路径同样)。在 `readAll` 调用处统一 `matchesByField`,行为只在无过滤源下改变;夹具加一个 `filtersApplied:false` 适配器用例钉死。
2. **x-tenant-id 洞**:`requireTableActionAccess` 对持 legacyGate 的用户直接放行、跳过 operator-scope 租户校验;12 条管理员路由对无租户声明的 token 应 403(套 #5445 的 operator-scope)。

完成判据:合后打 `r11` 上 222,重跑管理员链 + 操作员链回归(现有脚本),全部与 W2 前一致;定时任务用的租户绑定令牌不受影响。
**风险**:第 2 项可能锁住某个我没想到的管理员路径 → 默认加 feature flag(env 键,222 开、代码默认关),回退只需关 flag。

### W5 收尾(T+38h → T+48h)

- 交付说明 v2:子树桥接、定时拉取、缺件清单三节;runbook 同步;交付包目录重建(`r11` zip + sha + 脚本 + 说明)。
- 全新安装路径:若能在 222 隔离目录 + 独立数据库验证就验证,否则如实标注"未验证"及原因。
- 48 小时报告:做了什么、在哪(PR、222 证据、文档)、待 owner 拍板项;发布为页面;写 memory。

## 3. 我替 owner 假设的决策(全部可回滚)

| 决策 | 我的假设 | 回滚方式 |
|---|---|---|
| 子树桥接是正式功能还是测试手段 | 正式功能,代码默认关闭,222 开 | 配置删块 |
| 子树根件数量按 1 | 接受(没有订单就没有数量来源) | 无需回滚,是事实 |
| 定时拉取自动写入吗 | 不写,只试算;写入由人点 | 加 `--apply` |
| 定时用哪个账号 | 222 专用管理员服务账号,登录禁用,令牌 7 天 | 删账号、删令牌文件 |
| 大 BOM 实测要切绑定 | 切到合成源再切回,期间客户 PLM 拉取不可用约 1 小时 | 切回即恢复 |
| W4 管理员路由收紧 | 上 feature flag,222 开 | 关 flag |

## 4. 巡检与失败处理

- 每 3 小时 cron:看任务表、后台代理/工作流是否停滞、CI、222 健康;停滞的重派;推进下一波;更新进度页面与 memory。
- 任一波 blocker 超过 2 次修复循环仍红:该波停在"已知问题"状态,写清现象与证据,不硬合,继续下一波。
- 222 健康检查失败:先按升级脚本的恢复命令回退到上一版,再排查;不在坏掉的状态上继续实证。

## 5. 回来后先看什么

1. 进度页面(链接在交付总结里):每波状态、PR 列表、待拍板项。
2. `docs/development/takeover-beiliao-20260821/222-rehearsal-full-run-20260904.md` §9 起:222 上每次变更与证据。
3. `gh pr list --author @me`:哪些已合、哪些卡着、为什么。
4. memory `beiliao-24h-program-status-20260903.md` 末尾:最新状态与教训。
