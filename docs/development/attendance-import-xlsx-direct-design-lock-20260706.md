# 考勤导入 xlsx 直导（R3）design-lock — 2026-07-06

> **Status: RATIFIED（2026-07-06：owner /goal「余下的开发作为目标」+ 代理/主循环双推荐档采纳——
> D1=A 前端转换 / D2=v1 仅 `.xlsx` 首个非空 sheet / D3=a **已由 #3737 落地**（SheetJS 0.20.3，
> 两 CVE 关闭，且勘误证实升级为生产可达漏洞的就地修复）/ D4=默认档（原文件 ≤10MB、失败可操作报错）。
> X1 就此解锁；X2/X3 仍各自独立 opt-in。）**
> 需求链：客户把钉钉默认导出的 `.xlsx` 传进 CSV-only 导入口（#3694 根因）→ 已落两刀缓解
> （选文件拦截+另存为指引 #3694；勾选生成模板 #3708；列识别回显 #3718）→ 本锁 = 终局方案：
> **去掉"另存为 CSV"这一步**，xlsx 直接可导。

## 1. 核心架构决策 D1：解析位置 = 前端转换（推荐 A 档）

| 档 | 方案 | 评估 |
|---|---|---|
| **A（推荐）** | **前端转换**：选中 `.xlsx` 时浏览器内 SheetJS（`xlsx` 已在 `apps/web` 依赖树，动态 import 按需加载不进主包）读首个工作表 → `sheet_to_csv` 转成 CSV 文本 → **喂进既有 csvText/upload 管线，后端逐字节零改动** | 复用全部既有闸门（预览/提交/校验/审计/异步 lane）；无 wire 形状变更；列识别回显天然生效（转换后表头直接进 #3718 面板）；xlsx 护栏从"拦截"变"转换"，`.xls/.xlsm/.xlsb` 维持拦截+指引 |
| B | 后端原生解析：upload 接受 xlsx，服务端 SheetJS | 新增内容类型/zip-bomb/内存/异步 lane 全套面；仅当 A 的转换上限（见 D4）被真实客户命中才考虑（列为 X3） |

## 2. 决策点

- **D2 范围**：v1 仅 `.xlsx`（钉钉默认导出格式）；`.xls`（OLE2 老格式）/`.xlsm`（宏）/`.xlsb` 维持现有拦截+另存为指引。多工作表：v1 取**首个非空 sheet** + 状态提示「已读取工作表 <名>；如需其他表请另存为 CSV」；sheet 选择器 = X2。
- **D3 SheetJS 版本姿态（安全门）**：~~仓内 `xlsx@^0.18.5` 仅测试代码使用~~（**勘误 2026-07-06 对抗审阅 #3737-P2-1**：静态 import 确为零，但存在 4 个**动态 `import('xlsx')` 生产消费方**——`MetaImportModal.vue:587` 前端解析用户上传、`MultitableWorkbench.vue:3617`/`IntegrationWorkbenchView.vue:3441` 导出、`univer-meta.ts:4612` 后端 `POST /import-xlsx` 服务端解析用户上传——**CVE 在升级前即生产可达**，D3-a 属就地修复而非前置预防）。R3 让考勤线也开始解析**用户提供的文件**。0.18.5 有已知 CVE（CVE-2023-30533 原型污染 <0.19.3；CVE-2024-22363 ReDoS <0.20.2），且 npm 渠道版本停更（官方发布已迁自有渠道）。三选一：
  - D3-a 升级到官方渠道 ≥0.20.2（改 package.json source + lockfile，多维表测试同步验证）——**推荐**；
  - D3-b 换 `exceljs`（活跃维护，但 API/体积不同，多一颗依赖）；
  - D3-c 维持 0.18.5 + 转换侧缓解（`sheet_to_csv` 输出为纯文本、不消费解析对象图，原型污染面收窄但非零）——不推荐长期。
- **D4 上限与失败 UX**：xlsx 原文件 ≤10MB（超限 → 报错走另存为指引，不尝试转换）；转换后 CSV 文本走既有 5MB inline/upload 分流；转换失败（加密工作簿/损坏 zip/空表）→ 可操作报错（「无法读取该 Excel：<原因>；请另存为 CSV 后重试」），**绝不静默**。日期/数字单元格用格式化文本输出（`sheet_to_csv` raw:false），与钉钉导出的文本习惯一致；发现歧义以预览为准（既有预览闸门兜底）。

## 3. 阶梯（每 rung 独立 opt-in）

| Rung | 内容 | 量级 |
|---|---|---|
| **X1 前端转换** | 动态加载 SheetJS → 首 sheet 转 CSV → 既有管线；护栏改道（xlsx→转换，其余拦截不变）；识别面板/勾选模板/预览全链自然复用；测试含真 xlsx fixture 端到端（转换→识别→预览 payload 形状）+ 失败态（加密/损坏/超限/空表）+ mutation | ~3-5 pd（D3 裁决后） |
| X2 sheet 选择器 + 行数提示 | 多表工作簿 UI | gated |
| X3 后端原生 xlsx | 仅当 A 档上限被真实命中 | gated |

## 4. 边界

- 公式求值结果取缓存值（SheetJS 默认），不实时计算；样式/合并单元格不解释（合并区首格值，其余空——预览可见）。
- 后端 `plugins/plugin-attendance` 在 X1 零改动；X1 不动 wire 形状（csvText/csvFileId 语义不变）。
- 加密/密码工作簿：明确报错不支持（v1）。

## 5. 完成口径

owner 裁决 D1-D4（尤其 D3 安全门）→ 改 RATIFIED → X1 实现 → opus 对抗审阅 0 P1/P2 → 三红线 → 验证 MD。
