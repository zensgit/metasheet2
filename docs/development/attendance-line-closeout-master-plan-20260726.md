# 考勤整线收尾总计划（vNext Wave 5 + issue #4556 W4-W8）

> Status: **PLAN**（docs-only）。**不授予授权、不改任何 runtime、不构成开工或合并。**
>
> Date: 2026-07-26 · 对象：当前所有在飞的考勤交付面 + 至 issue #4556 关闭的全部剩余工作
>
> 本文回应 owner 指令「规划及计划……按代码难度选择模型……完成整条考勤开发线收尾」。
> **owner 闸位以 🔒 标注——那些步骤本车道不得自行推进。**
>
> **Erratum（follow-up，2026-07-26）**：本文原始版本随 #4615 合入 main（`9fdf68fa5c34d2224fbe6bd0d71b14ca78263502`）。外部复核在已合版本上发现 2 处 P2：①§0 标题「闭集票据表」及其「机器验证」结论对「考勤线整体」过度声明——本次改为 query-scoped 表述并如实列出盲区；②§0.1/§4 的 #4615/#4582/#4613 状态在合入时已漂（写 OPEN/待决，实为已合）——本次刷新为当前事实。此后又经两轮同日追加复核：③盲区清单里两处剩余「未触发」「闭集」类残留绝对表述被指出并改正（见 §0）；④标题正则漏掉的 **W4-PRE-1 子链**（`#4521`/`#4526`/`#4530`/`#4534`，均 MERGED）此前只当例证引用、未收进候选票据清单本身，且 §0.2「Wave 0-4 全部票据」一行本身列了其设计锁 `#4522` 却漏列这四张实现票——本次已把四张补为 §0.2 新行并改正该行标题。**本 erratum 本身范围克制**：只动上述四类问题触及的行 + 顺带按 API 现势刷新 #4612/#4617 的 head（§0.1）、刷新标题正则/路径两条查询的命中计数（55/63 → 56/64，因本 follow-up PR 自身 `#4626` 进窗），未重新审阅或改动其余内容。
>
> **第五轮独立门审（同日追加，2026-07-26，对上一提交 `16d404db28d0e1d479f2ab74ccbe236ad6bc9f19` 的复核，判定 3×P1/1×P2/1×P3/3×NIT）指出的问题，本次逐条改正**：① 交叉校验用的 `gh pr list --json files` 走 GraphQL，单 PR 只返回前 100 个改动文件，此前未披露；窗口内 3 张 PR 的 `files` 长度卡在 100（`#4540`/`#4525`/`#4482`），绕开截断复核后仅 `#4525` 有真实 attendance 路径命中被吃掉——计数订正 64→**65**、9→**10**、5→**6**，均以「实跑命令 + 输出 + 完整成员名单」形态给出，盲区清单新增第 4 条；② W4-PRE-1 子链此前把「四张」写成新的闭集计数，被 `#4545`（MERGED `ee39a13eb9db27d01c89cb19f0644b546c711347`）证伪——它对标题正则、路径交叉查询同时不可见（both-miss），是与前四张不同的第三类查询盲区；本次改为**开放式已知成员列表**并补入 `#4545`，盲区清单新增第 5 条「both-miss」；③ 删除上一提交在 §0.2 标题勘误段新引入的假陈述「#4584/#4585/#4586 同样是人工补列而非查询命中」——三者标题实测均命中现行正则，该半句既与实测相反、对结论也无必要，PR body 同句一并删除；④ 本轮 SWEEP v3 扩维（新增数量词 pattern，且绝对词维度补收 `均已`/`不静默`/`每一行` 等）对全部累计 delta 重扫，逐处补充范围限定或改写为条件陈述；⑤ 「命中集 − 提及集 = 空」的机器验证结论补上校验时刻锚；⑥ 剩余未裁定残留（`#4427`/`#4442`/`#4444`/`#4524`/`#4602`，加本轮经盲区 4 找回的 `#4525`，共 6 张）逐张一行裁定（本线/非本线 + 理由），见 §0.2。
> **NIT 一并披露（不改写历史，仅在此勘误）**：commit `322348e14` 的 message 称 #4530「4 files」不实——`gh api repos/zensgit/metasheet2/pulls/4530 --jq .changed_files` = **9**，「4」是其中 attendance 路径命中的文件数，二者被该 commit message 混为一谈。

## 0. 一页现状 —— **查询范围内的候选票据清单**

> **本节曾标题「闭集票据表」，经外部复核（2026-07-26，随 #4615 落 main 后的 follow-up）指出为过度声明，已降级为下述表述——机制不变，只改结论强度，理由见下。**
>
> **枚举方法（写成规则；方法只对其自身查询范围可证闭合，不对「考勤线全部改动」可证闭合——后者的盲区见下）**
> 本表由 **API 查询**生成（`gh pr list --state all --limit 200`，标题正则 `attendance|考勤|4556|W4C|W5-|Wave 5`，**最新 200 张 PR 窗口内**命中 **56** 张——该窗口已跑满，不是「全量」，详见下方盲区 2），**再逐张分类**——不是「列出我正在推进的票」。
> **静默缺席不是本文档的做法**：任何命中**该查询**的票若未出现在本表，仍视为**缺陷**（这条约束不变，但只约束「命中该标题正则查询」的票，不约束该查询之外的票——见下方盲区）。
> **本表两次因方法不闭合而被外部复核抓到**，如实留痕：①初版用「我在推进的票」⇒ 漏 #4584/#4585/#4586/#4608；②二版只**补上被发现的那四张**、方法未改 ⇒ 仍漏 **Wave 5 的四张 runtime PR**（#4557/#4562/#4564/#4576），而文档标题恰恰写着「vNext Wave 5 + …」。**「补上发现的」不等于用查询兜底枚举**——二版之后改由查询驱动。
> **第三次外部复核（本次）指出**：查询驱动解决了「人工遗漏」，**但没有解决「查询本身的边界」**——见下方盲区清单，这是本次修正的核心改动。
> **第四次外部复核（同日追加，2026-07-26）指出**：上一版盲区清单虽已具名列出 `#4521`/`#4526`/`#4530`/`#4534`（W4-PRE-1 子链），**但只当例子引用，没把它们收进候选票据清单本身**；且 §0.2「Wave 0-4 全部票据」一行本身用词与内容矛盾——它列了 W4 设计锁 `#4522`，却漏列 `#4522` 的四张实现票。这不是「范围外」，是「全部」不是全部。本次已把四张补为 §0.2 新行（MERGED + merge SHA + 范围说明），并把该行标题的「全部」改为有界表述——见 §0.2。
>
> **已知盲区（如实列出边界，不再用「补一张」当作已闭合）**
> 1. **标题正则字面是硬边界**：`attendance|考勤|4556|W4C|W5-|Wave 5` 只匹配 PR **标题**文本。改动了考勤相关文件、但标题未含上述字面的 PR，对本查询不可见。**具体例证**（2026-07-26 用按改动路径的交叉查询证实，见下，共 **10** 张——第 5 轮复核找回被盲区 4 的 100-file 截断吃掉的 `#4525` 后，由 9 张订正为 10 张）：`#4521`/`#4526`/`#4530`/`#4534`（标题为 `W4-PRE-1[b/c/d]` / `user_orgs`，未含正则字面，但均改动 `packages/core-backend/tests/integration/attendance-w4pre1*.db.test.ts`）——**这四张不再只是例证**：它们是 W4 设计锁 `#4522` 的实现票，`#4522` 本身已在 §0.2「Wave 0-4」一行里，四张实现票却缺席，是该行「全部票据」用词与内容自相矛盾的直接证据（第四次外部复核抓到，见上）。本次已把四张补为 §0.2「W4-PRE-1 子链」新行（MERGED + merge SHA + 范围说明），并改正了「Wave 0-4」行标题——**不再只是被引用的例子**。`#4602`（标题 `Phase A harden DingTalk corp-scoped identity matching`）、`#4427`/`#4442`/`#4444`/`#4524`（ops 脚本 `scripts/ops/attendance-*` / `staging-attendance-*`）、`#4525`（同为 `scripts/ops/attendance-run-gate-contract-case.sh`，第 5 轮经盲区 4 找回，见下）——**这 6 张本次逐张裁定完毕，均判非本线**，一行一理由见 §0.2「路径命中但非本线」新行；不再是「留作残留」的未裁定状态。
> 2. **`--limit 200` 是分页硬界，且本次两个查询都已触发它**——这条曾在本节初稿被写错为「未触发」，如实纠正：`gh pr list --state all --limit 200 --json number --jq 'length'` 返回 **200**（窗口已跑满，不是「命中数低于上限」），窗口地板为 `#4422`。**两个查询看到的从来不是「全部 PR」，而是「最新 200 张里符合条件的那些」**；`#4422` 以下、真正属于考勤线但更早的票一律不可见——具体例证：`#3487`（`ci(attendance): wire AE-3 request-center parity spec into web guard`，MERGED，标题字面命中正则但票号在窗口地板之下），两条查询命中的 **56** 与 **64/65**（**64** = `gh --json files` 原始 GraphQL 输出；**65** = 绕开盲区 4 的 100-file 截断复核后的实际数，见下方交叉校验）都应读作「最新 200 张 PR 窗口内」的命中数，不是「仓库全部历史」的命中数。本节下方各处数字同样适用这条限定，未逐处重复贴（行号会随本文档后续编辑漂移，不钉死引用）。**窗口活着的现场证据（历史记录，非本轮实测）**：上一轮重跑较其前一版的 55/63 各多 1——多出的是 `#4626`（本 follow-up PR 自身，标题与改动路径都含 `attendance` 字面）进窗后被两条查询同时命中；那一轮的差集是 9 张仅路径命中 + 1 张仅标题命中。**本轮（第 5 轮）差集已订正为 10 张仅路径命中**（找回被 100-file 截断吃掉的 `#4525`，见下方盲区 4、交叉校验），说明窗口平移与截断绕开是两类独立增量，不要混同。这也意味着本行数字在下次编辑时大概率又会不同——复算者应自己重跑，不要相信本行写死的数。
> 3. **分类仍是人工判断**：查询只保证「命中集」可复算，把每张票分到 §0.1（关键路径）还是 §0.2（不在线，附理由）仍是人工判断，未被机器验证——机器验证只覆盖「命中集 − 本文提及集 = 空」这一步。
> 4. **`gh pr list --json files` 走 GraphQL，单 PR 只返回前 100 个改动文件**（第 5 轮外部复核指出，此前未披露）：`changed_files ≥ 100` 的 PR，其超出前 100 个的路径对交叉校验查询静默不可见。**实测**：本窗口内 3 张 PR 的 `files` 数组长度卡在 100 —— `#4540`（`changed_files=120`）、`#4525`（`changed_files=138`）、`#4482`（`changed_files=125`）。**绕开命令**：`gh api repos/zensgit/metasheet2/pulls/<N>/files --paginate --jq '.[].filename'`。复核结果：`#4540`/`#4482` 无 attendance 路径（真阴性）；**`#4525` 有 1 个 —— `scripts/ops/attendance-run-gate-contract-case.sh`**（真阳性，此前被截断吃掉）。下方交叉校验的路径命中计数（64→**65**）与本节盲区 1 的仅路径命中计数（9→**10**）均已按此订正。
> 5. **both-miss：两条查询可以同时不可见同一张票**（第 5 轮外部复核指出）：盲区 1（标题不含正则字面）与盲区 4（路径被 100-file 截断）各自独立，但存在第三类票——标题不含正则字面，**且**改动的文件本身也不含 `attendance` 路径字样，两条查询都命中不到。**具名例证**：`#4545`（MERGED，merge SHA `ee39a13eb9db27d01c89cb19f0644b546c711347`，标题 `chore(test): w4pre1d departure db test — 补 vitest.config.ts exclude`，唯一改动文件 `packages/core-backend/vitest.config.ts`）。它是 W4-PRE-1 子链 `#4534` 真库用例的第二接线点：`#4534` 合入时该用例已写入 `plugin-tests.yml` 真库步（`git show 3d1b6cfaacad6bf833f31eb9f6ccaf99217e8c72:.github/workflows/plugin-tests.yml | grep -c w4pre1d-departure-candidate-split` = 1），但 `vitest.config.ts` 的 no-DB exclude 漏了它（同一 commit 同一 grep = 0）——期间若 no-DB job 先收集到该文件会 skip-green 其 `describeIfDatabase` 块（真库 job 本身一直在跑，故非零覆盖，是假绿风险窗口，`#4545` 补上 exclude 关闭了它）。本次已把 `#4545` 补入 §0.2 W4-PRE-1 行（开放式已知成员列表，不写闭集数词）。**已知边界**：探测 both-miss 类目用的是 `git log origin/main --oneline -G'attendance' -- ':!*attendance*'` 型探针，只覆盖最近若干条含 `attendance` 文本但未改动 attendance 命名文件的提交，不是全仓穷尽扫描；本条目只证明该类目存在且已有 1 个考勤线实例，不承诺闭集。
> **状态新鲜度（此段本身被外部复核抓到过两次，故写成可自查的而非口号）**
> - **已合条目**钉不变的 merge SHA，永不过期。
> - **在飞条目**的 head 会因 rebase 变化；**本文自身那一行不钉 SHA**——它每次刷新后 head 必变，钉了必错。（**#4615 现已 MERGED，见 §0.1：它不再是「本文自身那一行」——merge SHA 永不过期，钉死是对的；当前意义上的「本文自身」是本 follow-up PR，它没有自己的表格行，故这条规则暂无需应用之处，不是被违反。**）
> - 「以 PR 现势为准」是纪律，**但不能替代合入前刷新**：本表已因此漂过两次（#4613 合入后仍写 Draft；#4582 合入后仍写 OPEN）。
> - **读者不必依赖本快照判断在飞状态**，可自行复算「该查询在最新 200 张 PR 窗口内的命中集」与现势（**这不是考勤线闭集**，见上方盲区 1、2）：
>   ```bash
>   gh pr list --state all --limit 200 --json number,title,state,isDraft,headRefOid,mergeCommit \
>     --jq '.[]|select(.title|test("attendance|考勤|4556|W4C|W5-|Wave 5";"i"))
>            |"\(.number) \(.state) draft=\(.isDraft) \(.mergeCommit.oid // .headRefOid)"' | sort -n
>   ```
>   **校验时刻锚（第 5 轮外部复核指出上一版此结论已随窗口漂移过期，本次改为钉时刻而非再加免责话术）**：`@ 2026-07-26T18:27 PDT（2026-07-27T01:27Z），窗口 #4423–#4627`，该查询命中 **56** 张，「命中集 − 本文提及集 = 空」在**此锚定时刻**成立——**含 `#4627`**（`docs(attendance): W4C-2 QA 交付包…`，OPEN，创建于本次校验窗口内，标题命中正则）：它只以本条边界说明的方式被**提及**（与 `#3487` 的处理方式相同），**不是** §0.1/§0.2 候选票据清单的成员——它是另一张独立的 W4C-2 QA 交付文档，与本收尾计划无从属关系，不补入本表。**该锚定本身会随窗口继续平移而过期**——复算者应自己重跑上方命令并预期得到不同的具体成员，不要相信本行写死的数（**这只证明「该查询在该窗口内、按文本提及计的命中集闭合」，不证明「考勤线全部改动」闭合，也不代表 §0.1/§0.2 候选票据清单本身闭集**——见上方盲区 1、2、4、5）。
>
> **交叉校验（按改动路径的正交查询，2026-07-26 第 5 轮随本次修正重跑；做了，非声称）**：
> ```bash
> gh pr list --state all --limit 200 --json number,title,files \
>   --jq '.[] | select(.files | any(.path | test("attendance"; "i"))) | .number' | sort -n
> # ⚠️ 走 GraphQL，单 PR 只回前 100 个文件（见上方盲区 4）；changed_files ≥ 100 的 PR 须另用
> # gh api repos/zensgit/metasheet2/pulls/<N>/files --paginate 绕开，否则会静默漏计
> ```
> 上述命令原始输出命中 **64** 张；按盲区 4 绕开 100-file 截断复核后，补回 `#4525`，**实际命中 65 张**（vs 标题正则 56 张，**同样是最新 200 张 PR 窗口内**——两个查询用的是同一个窗口，差集比较在窗口一致的前提下有效，但都不覆盖窗口外的历史票，如 `#3487`）。逐一比对两个命中集：
> - 仅路径命中、标题未命中：**10** 张（`#4427`/`#4442`/`#4444`/`#4521`/`#4524`/`#4525`/`#4526`/`#4530`/`#4534`/`#4602`；`#4525` 为本轮经盲区 4 补回，此前的 9 张列表漏了它）——改动路径见上方盲区 1、4 逐张所列。其中 4 张（`#4521`/`#4526`/`#4530`/`#4534`，W4-PRE-1 子链）已裁定并补入 §0.2 新行；**其余 6 张（`#4427`/`#4442`/`#4444`/`#4524`/`#4525`/`#4602`）本次逐张裁定完毕**，均判非本线，理由见 §0.2「路径命中但非本线」新行——差集不再有未裁定残留。
> - 仅标题命中、路径未命中：1 张（`#4549`，标题含「Attendance」但改动的是 `docs/development/dingtalk-hardening-real-uat-evidence-pack-20260713.md`，已在 §0.2 列出）。
> - 两个查询互不包含：标题正则漏了 10 张真实改动考勤路径的票，路径查询漏了 1 张标题命中但路径未命中的票（因该票只改文档、路径不含 "attendance" 字样）；**此外还存在第三类两条查询都不可见的票（both-miss，见上方盲区 5，具名例证 `#4545`）**。**这证明两个口径都是近似，互为交叉校验，谁也不是权威闭集**，并集本身也不构成「考勤线全部改动」的闭集。
> **本次交叉校验做到了「报告差集」，并把差集里全部能裁定归属的票都处理完**：4 张（W4-PRE-1 子链）补入 §0.2 W4-PRE-1 行；其余 6 张（含本轮经盲区 4 补回的 `#4525`）逐张裁定为非本线，见 §0.2「路径命中但非本线」新行。**唯一仍不在本表处置范围内的是 both-miss 类（盲区 5）——该类目按定义不进入标题/路径两条查询的命中集，不属于「本次交叉校验的差集」，是另一种性质的盲区，已在盲区 5 单独记录**，不与本段的「10 张仅路径命中」混同计数。

### 0.1 在关键路径上

| 面 | 票 | SHA | 状态 |
| --- | --- | --- | --- |
| **vNext Wave 5 — W5-0 六只读端点** | #4557 MERGED | `beef6c13402a24a4ced30dfa4a2f607144d1167b` | 已合 |
| **W5-7 前置小票（comp_time 参数化）** | #4562 MERGED | `ebe798d47c666ec32077e18ea7136ea9d457ed6d` | 已合 |
| **W5-1 决策轨迹双面** | #4564 MERGED | `9d6ab3e1c3195ac40ee9bf37321da28920af2a29` | 已合 |
| **W5-2 上下文帮助** | #4576 MERGED | `e10816380eb26d63b196ba076477b230804aaaee` | 已合 |
| **Wave 5 收官验证 MD** | #4582 **MERGED** | `1f93c462f9202851c7223500314573ce2e5de6c4` | **已合 ⇒ vNext Wave 5 四片 + 收官 MD 全在 main，该线收官** |
| W4 锁 PROPOSED 合入 | #4588 MERGED | `a3e5765727ca608e8c49c7a44a025e6e4aae5d40` | 已合 |
| W4 锁 RATIFY 状态持久化 | #4592 MERGED | `d6ac495b947c0b42ed7bee66d9531fbe25a486ca` | 已合 |
| W4C-0 identity 修订 | #4595 MERGED | `3fa1ae3421744fcec9a18c4f87153281c59ec6b2` | 已合（owner RATIFY 于 07-26，**不倒签**） |
| 修订状态持久化 | #4600 MERGED | `b5ff168e9411c556ff5eb055ce559859f9aeba8b` | 已合 |
| **W4C-0 contracts & durable storage** | #4606 MERGED | `d4dc12d8a8cde38c8f04f1952b3ba0b8b317265f` | 已合（**授权门瑕疵已记录**，见 #4613） |
| **W4C-1 pure calculator** | #4607 MERGED | `aebac4f8bef344b3ff3443ee045439c789a569a1` | 已合（同上） |
| **W4C-2 live/scheduled shadow** | #4612 OPEN | head `f0fe4a4a649a96842a154d35a3cda9d6e8a60fc9`（API 直取，2026-07-26 本次 follow-up 刷新；在飞条目仍会因后续 rebase 再变——**以 PR 现势为准**） | **Draft + ⛔HOLD**（`gh pr view` 复核 `isDraft=true`, `state=OPEN`）；fingerprint 半边未完成、P1-2 被 #4617 阻挡 |
| **(b2) §7.1a amendment** | #4617 OPEN | head `9ce0467e131a5911abaa0627461ce6a0f93e6fb1`（API 直取，2026-07-26 本次 follow-up 刷新；在飞条目仍会因后续 rebase 再变——**以 PR 现势为准**） | **Draft/PROPOSED**（`gh pr view` 复核 `isDraft=true`, `state=OPEN`）；四镜审 **5 P1 / 4 P2 发现完成、修订未完成**（此定性未重新核验，沿用原文，不在本次 follow-up 范围内） |
| 授权 provenance 勘误 | #4613 **MERGED** | `df610db9ab6c403da6233a9c5dae2579941a6275` | **已合** |
| W4 记录 + 计划（**4 文件**，本文原始版本） | #4615 **MERGED** | `9fdf68fa5c34d2224fbe6bd0d71b14ca78263502` | **已合**（本文现版本由随后的 follow-up PR 修正 P2-1/P2-2/P2-3 而来，见文末交叉引用；**该 follow-up PR 才是当前意义上的「自指」，其自身的行不钉 SHA，理由同上**） |

### 0.2 不在关键路径（含标题正则命中项 + 路径交叉校验补入项；本表内每行逐张标注理由，表外残留见 §0 盲区清单）

> **标题勘误（第四次外部复核抓到，与本节新增的 W4-PRE-1 行同源）**：本节标题原写「命中查询但不在关键路径」，隐含「本节每一行都命中标题正则查询」——但 W4-PRE-1 子链（下方新行）**正是**因为标题不命中查询才被本查询遗漏，是靠路径交叉校验才补入的。标题已改为不作此隐含声明。
> **第 5 轮外部复核纠正**：上一版本段紧接着写「本节其余几行（如 #4584/#4585/#4586）同样是人工补列而非查询命中」——**该半句是假陈述**，独立重跑标题正则复核，`#4584`/`#4585`/`#4586` 三者标题均命中现行正则 `attendance|考勤|4556|W4C|W5-|Wave 5`（`gh api repos/zensgit/metasheet2/pulls/{4584,4585,4586} --jq .title` 逐条核对），**不是**查询未命中的样本；本条勘误的支撑证据只需 W4-PRE-1 子链这一个例子即可成立，故本次已删去这半句，不用「历史成因是人工补列」的读法保留——那与本段论证「标题隐含每行都命中查询」的对象是**集合成员资格**，不是**行的来源**，用「来源」读法保留等于绕开被指出的问题。

| 组 | 票号 | 不在线的理由 |
| --- | --- | --- |
| **#4556 W0-W3 切片本身** | #4558 · #4560 · #4561 · #4563 · #4566 · #4567 · #4568 · #4569 · #4570 · #4571 | 本文标题范围是 **W4-W8**；W0-W3 已于 2026-07-23/24 由 Codex 车道落地并各自记录 |
| **W1-W3 合并后审计的修复** | #4584 `78b4133bac153ba39a3dff682d137bfdc26ae947` · #4585 `e0defbe26d7f2e1747e74aa908ca710422812bf7` · #4586 `c81b3bc39202fe347d18ab58520671af3c706def` | 修的是 W1-W3 的遗留（flag-OFF 遏制击穿 / guard 检查名稳定化 / 时间线守卫）；**#4585 是 `attendance-web-guard` 升 required 的前置，该治理动作已完成** |
| **W4C-0 的 test-infra 修复** | #4608 `d75d3b8285cba2b69a2442c1a8b5cf184c5266a4` | test-infra；解掉了连续两次打断 W4C-1 的「753 全绿却 exit 1」57P01 flake |
| **vNext Wave 0-4 主链票据**（原标题「全部票据」——第四次外部复核抓到自相矛盾：本行列了 W4 设计锁 `#4522`，却漏列 `#4522` 的四张实现票，「全部」不成立，已改为有界表述；四张实现票另起一行见下） | #4488 · #4492 · #4494 · #4501 · #4502 · #4504 · #4508 · #4509 · #4513 · #4514(CLOSED) · #4522 · #4541 · #4542 · #4543 · #4544 · #4546 · #4548 · #4554 | Wave 0-4 主链已各自收档；本文范围自 **Wave 5** 起（不含下一行的 W4-PRE-1 子链——那是 W4C 的前置基础设施，非 Wave 0-4 主链本身，但同样已收档不在本文范围） |
| **W4-PRE-1 子链**（`#4522` W4 设计锁的实现票 + 其接线补丁；**开放式已知成员列表——不写闭集数词，随发现更新**，标题不含标题正则字面，见 §0 盲区 1；第 5 轮外部复核指出上一版把「四张」写成闭集计数，被 `#4545` 证伪，已改为本表述） | #4521 MERGED `e20371b1a353d97b4fd5feb89044f9c5220bb6ca` · #4526 MERGED `3727cd92ea5d1ab4440b005ba40a1392abeeb6d0` · #4530 MERGED `1a209a5ccc67c362beb991c5e5d439c3528c9bfd` · #4534 MERGED `3d1b6cfaacad6bf833f31eb9f6ccaf99217e8c72` · #4545 MERGED `ee39a13eb9db27d01c89cb19f0644b546c711347` | user_orgs 生产维护前置基础设施，先于 §0.1 的 W4C-0/1/2 三片落地：写路径（#4521）→ 全生命周期 bind/unbind/backfill（#4526）→ 受控离职 deactivation（#4530）→ deprovision candidacy 拆分（#4534）→ `#4534` 真库用例的 `vitest.config.ts` exclude 第二接线点（#4545，见 §0 盲区 5：two-point wiring 缺失的第二点，此前对标题正则、路径查询同时不可见）；本行所列各票逐张核过 `merge_commit_sha` 均为 MERGED，不在当前 owner 闸位序列内 |
| **路径命中但非本线**（第 5 轮外部复核指出的差集残留，本次逐张裁定；见 §0 交叉校验） | #4427 · #4442 · #4444 · #4524 · #4525 · #4602 | 逐张理由：#4427（`fix(ops): make branch policy checker Bash 3 compatible`，OPEN，改 `attendance-check-branch-protection.sh`）——分支保护检查器的 Bash 兼容性修复，考勤只是该脚本命名沿用的历史前缀，非考勤业务代码；#4442/#4444（`W0 L7`/`L8 — exact-anchor recovery`，均 **CLOSED 未合**，`merged=false`，stacked on #4417）——改 `staging-attendance-report-digest-rd45-smoke.mjs`，是另一条 exact-anchor 恢复线借用考勤 fixture 做 smoke，且从未合入 main；#4524（`feat(approval): add secure record-link form fields`，MERGED 2026-07-23）——approval 记录链接字段功能，考勤路径命中仅因改了 `scripts/ops/attendance-run-gate-contract-case.sh` 门禁契约用例脚本；#4525（`fix(approval): preserve date-only calendar values`，OPEN，第 5 轮经 §0 盲区 4 的 100-file 截断绕开复核找回）——同 #4524 一桶，同一脚本、同一 approval 线；#4602（`fix(directory): Phase A harden DingTalk corp-scoped identity matching`，MERGED 2026-07-26）——DingTalk 目录准入线的身份匹配加固，改动只触及 `attendance-w4pre1{,b}-*.db.test.ts` 作为受影响回归面，非该线本身范围变更。**六张结论均为非本线** |
| **S7 动态审批人线** | #4453 · #4471 · #4476 · #4480 · #4481 · #4483 | 不同交付线（S7），与本文范围无依赖 |
| **考勤运维 / 其它** | #4422 · #4549 | RD-4/5 smoke 与 DingTalk UAT 前置，不属本线 |

## 1. 关键路径（唯一合法顺序）

```
【A 轨：Wave 5，已到终点】
  #4582 ──🔒owner 裁收档──▶ Wave 5 CLOSED

【B 轨：#4556 W4，串行】
  W4C-2 修 P1(时区错配) + P2-1(resolved golden) + P2-2 处置
        └─ 与 (b2) 无依赖，可立即做 ✅已授权
  #4617 ──🔒owner RATIFY──▶ 实现 P1-2（(b2) run 身份 + outbox union）
        └─ 二者汇合 ──▶ 新 exact-head 独立门审 ──🔒owner 裁合并──▶ W4C-2 合入
  ──▶ W4C-3a ──▶ W4C-3b ──▶ W4C-3c ──▶ W4C-4     （每片：fresh-main PR + 独立门 0P1/P2 + 🔒合并裁决）
  ──▶ 🔒W4C-5 staging soak（单独授权 + ≥7 日历日）
  ──▶ W5 flex / 🔒W6（备料亦需授权）/ W7 cutover / W8 收口
  ──▶ 🔒issue #4556 关闭（锁 §14-10 另行终裁）
```

**并行窗口（仅两处）**：① W4C-2 的非 P1-2 修复 ∥ #4617 的 owner 审阅；② W4C-5 的 7 日历日 soak ∥ W5/W6 开发（锁允许，各自门）。**其余一律串行**——锁 §12 逐字要求「前序先合」。

## 2. 逐步任务与模型分配

模型判据（owner 指令「按代码难度」+ 外部复核修正）：
**Sonnet 5** = 正确性风险最高、需稳定长链推理（canonical boundary / 身份授权 / 事务与 outbox 语义 / rollback 语义）；
**Fable 5** = 机械面广、体量大（collector / fixture / CI 接线 / 读面与 OpenAPI / 文档同步）；
**Opus 5** = 门禁与裁量（每片独立对抗审、设计锁审、语义漂移深读、跨车道影响判定）。

| # | 任务 | 模型 | 前置 | 备注 |
| --- | --- | --- | --- | --- |
| B1 | **W4C-2 P1**：时区错配（`deriveLegacyLivePunchAttributionV1` 从 `args.timezone` 重算，而路由已把它覆写为班次 tz）。**先写零并发复现腿并跑红，再修** | **Sonnet** | 无 ✅ | 客户端一跳可达；修法二选一须读码定论 |
| B2 | **W4C-2 P2-1**：补 `resolved` 姿态 golden fixture；**复跑门审 M10 验必红** | **Fable** | B1 不冲突 | 现两份 fixture 都是 `unresolved`，红线从未覆盖有排班用户 |
| B3 | **W4C-2 P2-2 处置**：caller 枚举**以双语法静态 grep + 人工通读**得出「唯一入口且已守」（**方法有界：非覆盖率插桩**，实现方已自报此限）；**🔒 是否补事务内 RBAC 复核 = owner 裁** | Sonnet（若裁定补） | 无 | 已披露：事务内复核函数从不查权限表 |
| B4 | 重写 P1-3 不可达论证文本（错误前提已被门审证伪） | **Fable** | 无 | 正确理由是路由先行 throw，非 SERIALIZABLE 快照 |
| B5 | **🔒 #4617 RATIFY** → 实现 P1-2（scheduled-run row / 最终化事务 / outbox union / 两事件入闭集 / 迁移回滚） | **Sonnet** | 🔒 | 全线最难实现项 |
| B6 | W4C-2 新 exact-head 独立门审 | **Opus** | B1-B5 | 旧判定不延展 |
| B7 | 🔒 owner 裁 W4C-2 合并 | — | B6 | |
| B8 | **W4C-3a** import/integration/rollback | **Sonnet** 实现 + **Fable** fixture + **Opus** 门 | B7 | 数据面最险：rollback 语义 / 恰 5000-5001 / 三 transport 等价 / 导入指标禁覆盖 canonical 段结果 |
| B9 | **W4C-3b** approval/correction/outdoor/cancel | **Sonnet** + **Opus** | B8 | 跨插件权限泄漏最险：P17/P26 + 携 `published_definition_id` 的对抗实例 |
| B10 | **W4C-3c** manual/recompute/operator/最终清单 | **Sonnet** + **Fable**（collector 硬执行） + **Opus** | B9 | 收口片：「最终债务集为空」硬门会暴露前片遗留 |
| B11 | **W4C-4** shadow ledger / dual-host detail / OpenAPI | **Fable** + **Opus** | B10 | 触 vNext Wave 5 解释面 ⇒ **不得单方面改其合同**，须在 Wave 5 车道另修 posture 上限 |
| B12 | 🔒 **W4C-5** staging soak | Fable（runbook/helpers） + **Opus**（门） | 🔒 单独授权 | ≥7 日历日；预期差异清单必须带上 W4C-1 的 `correction-applied ⇒ adjusted` |
| B13 | W5 flex / 🔒W6 workspace / W7 cutover / W8 收口 | 按各片性质同上 | 逐片 | W6 备料亦需 owner 授权 |
| B14 | 🔒 issue #4556 关闭终裁 | — | B13 | 锁 §14-10 |

## 3. 每片不变的门禁（锁 §12 + 本线累积房规）

**锁 §12 逐字**：独立 fresh-main PR · 前序先合 · 独立对抗审 **0 P1/P2** · exact-head 测试与 mutation · 不启用任何组织。cut-over 片额外须证 `legacy_projection_only` 下**字节不变**。

**本线累积房规（每片任务书都要带）**：
1. 负例 stub 的失败点必须落在**被测守卫之内**（W4C-0 P2-1：stub 在 try 之外爆、catch 从未进入）；
2. 点名「fails independently」逐条**独立判别腿**；
3. **多门互相掩护**要分别 neuter 求排他失败（共享代码变体亦算）；
4. **fixture 自然形状**（W4C-1 P2-1：三值全同刻 ⇒ 断言名声称的行为不可观测）；
5. 「断言不发生」必配**正控**；
6. **TOCTOU 必须构造真并发**（双连接、双 commit 序）；
7. **mutate 后核对命中真代码行**（本片曾误中注释致 5/5 假绿）；
8. 真库一律**自建全新库**（残留库两次造成假红）；
9. 测试文件**禁裸控制字节**（NUL 使 grep 判 binary，破坏审计）；
10. SHA 一律 `rev-parse` 直贴，**严禁手工补全缩写**；
11. **合并前回读授权来源本身**——授权活在授权发生的位置，不在被授权动作的产物里；
12. **合同欠账 ≠ 分类勘误**：门审引锁原文判「未实现」时，禁用「重读锁文」覆盖；实现侧闭集漏项默认是 inventory 缺陷，不是合同意图。

> 第 11、12 两条是本线两次实质失误的产物（越过 HOLD 合入两片；(c)-plus 与 Wave 5 `grounded` 两次「重读覆盖原文」）。它们是本计划里**最不可省**的部分。

## 4. owner 闸位清单（🔒，共 8 处；**3/8 已裁决，刷新于本次 follow-up**）

1. **#4582** Wave 5 收档裁决 —— **已裁：MERGED** `1f93c462f9202851c7223500314573ce2e5de6c4`
2. **#4617** (b2) amendment RATIFY —— P1-2 的硬前置；**仍待裁**（PR 现势为 OPEN/Draft，见 §0.1）
3. **#4613** provenance 勘误是否合入 —— **已裁：MERGED** `df610db9ab6c403da6233a9c5dae2579941a6275`
4. **#4615** W4 开发及验证记录（**4 文件**——此行原写「3 文件」，与 §0.1 行不一致，本次顺带核对 `gh pr view 4615 --json files` 更正为 4）是否合入 —— **已裁：MERGED** `9fdf68fa5c34d2224fbe6bd0d71b14ca78263502`
5. **B3** 是否补事务内 RBAC 复核 —— 仍待裁
6. **B7** W4C-2 合并裁决 —— 仍待裁
7. **B12** W4C-5 staging soak 单独授权（+ **W6 备料授权**）—— 仍待裁
8. **B14** issue #4556 关闭终裁 —— 仍待裁

**另有逐片合并裁决**（B8-B11、B13 各自一次）——按 owner 07-26 的口径，恢复授权**不含**后续切片，每片合并均需单独裁。

## 5. 风险登记（本车道已知，非推测）

| 风险 | 载体 | 缓解 |
| --- | --- | --- |
| W4C-0/W4C-1 授权门瑕疵 | 已在 main | provenance 勘误 #4613 如实记录；不倒签；保留≠洗白 |
| W4C-2 的 P1 时区错配 | PR #4612 | B1 先写腿跑红再修 |
| `resolved` 分支从未被字节红线覆盖 | PR #4612 | B2 补 fixture + 复跑 M10 |
| 事务内复核不查权限表 | `w4c0-authorization.ts:284-327`（已改名 `recheckAttendanceActorLivenessInTransactionV1`，消除假不变量命名） | 已披露；路由门是**目前已知的**唯一屏障，其承重性已由「删 `withPermission` ⇒ 负例翻红」证明；**但唯一性的证据是静态枚举而非插桩** ⇒ W4C-3b 扩 caller 时须重估；🔒 B3 |
| W4 快照落地后 Wave 5 posture 上限过期 | 跨车道 | 已写入 W4 锁 §10.3 follow-up；B11 时在 Wave 5 车道修订，**不单方面改** |
| `HANDOFF-W4C0.md` 混入 main | 已在 main（随 #4606） | 独立清理票（未开，本计划记录） |
| **strict required checks 下 `main` 每次前进都会让在飞 PR 变 BEHIND** | 全部在飞 PR | **这是复发性条件，不是一次性事实**：每次合并前须重新 rebase + 等 fresh checks。已在本线多次触发（#4582 连续两次、#4612/#4615 各一次）。**收窄记录而非撤回**——「本次已刷」不等于「此条已消」 |

## 6. 交叉引用

- W4 锁 `attendance-issue-4556-w4-segment-calculation-design-lock-20260724.md`（§12 串行合同 / §14 执行序）
- identity 修订 `…-w4c0-identity-proof-amendment-20260725.md`
- (b2) amendment `…-w4c2-scheduled-run-identity-amendment-20260726.md`（PR #4617）
- provenance 勘误 `…-w4-authorization-provenance-erratum-20260726.md`（PR #4613）
- 开发及验证记录 + W4C-2 修复设计 + 剩余切片计划（PR #4615 三文件）
- Wave 5 锁 `attendance-vnext-wave5-explainability-data-contract-lock-20260722.md`（§10 已含 owner 07-24 窄 amendment）
