# Approval Parity Execution Ledger (2026-08-17)

**Status:** LIVING - planning ledger only; no new runtime phase is authorized by this file
**Master design:** `approval-parity-master-design-lock-20260817.md`
**Verification record:** `approval-parity-final-verification-20260817.md`
**Initial baseline:** `origin/main@d33a6a0fa120452b721ea76d449dfa1463727463` (superseding the
2026-08-17 planning-audit baseline `b84c30db97`, which lost exact-head qualification when main advanced)

## 0. Ledger rules

1. Record source SHA, review SHA, landing SHA, and merged-main verification SHA separately.
2. `CI green`, `review approved`, `merged`, `deployed`, `UAT passed`, and `flag enabled` are distinct.
3. A review is valid only for its recorded SHA. Rebase or conflict resolution requires requalification.
4. A Draft/PROPOSED row is not authorized runtime work unless the owner-decision column names the
   ratified lock or explicit implementation authorization.
5. Flags remain OFF unless a row records environment, approver, time, evidence, and rollback result.
6. Do not store secrets, tenant data, form values, user identities, or private security detail here.

## 1. Existing foundations

| Foundation | State | Evidence / boundary |
|---|---|---|
| Canvas V2 interaction lock | RATIFIED | `approval-canvas-v2-interaction-design-lock-20260721.md`; O3 deferred |
| Canvas engineering stack | on main | default OFF; tenant S1-S12 not yet recorded complete |
| Version diff and restore | on main | immutable published version; restore creates a new draft |
| Durable approval event delivery | on main | independent activation history; do not infer current environment flag |
| Attachment runtime | on main | independent flag and UAT |
| FWB create/update/decision values | on main | independent flag and UAT; number mapping unavailable |
| Form-builder delta #4866 @ `80d33cbefa` | PR-local Draft / stale baseline | not present on main; range-diff and refresh required before ratification or implementation |
| Form command substrate | on main, not mounted | pure insert-after/append, identity, and reference commands plus required unit tests exist; production view does not call them and exact start/between/end slots are still P0 work; there is NO retype/update command in the substrate — it is new P0 command work (PR #4866 F3), not a mount |
| Current three-region form UI | on main, incomplete | palette/preview/inspector and drag affordance exist; palette drop remains append-only, structural field-list operations have history, and direct inspector property edits have none |
| Approval detail/center | on main, partial | preserve shipped timeline, summary, aging, batch approve/reject, and retry; only residual chrome belongs in P5-C |

## 2. Phase ledger

Use one row per PR. Split a row before implementation if it contains more than one independently
reviewable behavior.

| ID | Phase | Slice | Lock / decision | PR | Head SHA | State | Required evidence | Owner gate |
|---|---|---|---|---|---|---|---|---|
| P0-A | P0 | Refresh form-builder delta against current shell | #4866 revision | #4866 | `80d33cbefa...` old | NEEDS-REFRESH | range-diff, current file anchors, adversarial design review | ratify revised delta |
| P0-B1 | P0 | Extract flag-OFF fallback | revised #4866 | — | — | NOT STARTED | byte-equivalent mounted tests | implementation authorization |
| P0-B2 | P0 | Mount existing command substrate + authoritative opaque identity/reference providers | revised #4866 | — | — | NOT STARTED | production import/call proof; duplicate-ID, incomplete-history, and collision mutations | same |
| P0-B3 | P0 | Replace append-only drop with exact slots + typed drag codec | revised #4866 | — | — | NOT STARTED | start/middle/end, cancel/dragend/Escape/navigation/read-only, malformed/stale payload, browser geometry | same |
| P0-B4 | P0 | Property commits in one history + dependency-safe delete + NEW typed retype command (not a mount) | revised #4866 | — | — | NOT STARTED | one-command history; complete visibility/condition/permission/graph/mapping/detail reference negatives; retype ID-preservation and refusal negatives | same |
| P0-B5 | P0 | Production mount behind Canvas flag | revised #4866 | — | — | NOT STARTED | flag-OFF equivalence, flag-ON browser tests | same |
| P1-A0 | P1 | Basic-information typed controls + live validation count | Lock-0 | — | — | NOT DRAFTED | no raw comma-separated IDs; browser/a11y; old draft round-trip | ratify delta |
| P1-A | P1 | D0 inspector presentation cleanup | Lock-0 | — | — | NOT DRAFTED | named presentations; immediate commands; no scrim/Save transaction; readonly honesty copy retained | ratify delta |
| P1-B | P1 | Existing multi-source inspector | existing runtime + D0 delta | — | — | NOT STARTED | remove single-source lock; shipped union exact-set; all sources round-trip; no flatten | implementation authorization |
| P1-C | P1 | Shipped timeout + threshold frontend compatibility | D3-p/G1-p bounded owner decision — P2-3 CLOSED per lock text, see notes | #4972 | `fa8e92c9b6` (second fix-round on `6c2d61c419`, rebased onto `385a433821`; executes master lock §P1-C's flatten-branch-deletion clause verbatim per governance adjudication from the orchestrating session — GOAL-SET PROVENANCE, not an owner-authored ratification, and REVERSIBLE if an owner later amends the lock text itself) — prior head `6c2d61c419` (fix-round on `bd5930a9ae`, rebased onto `f9fc980772`; adversarial gate 0 P1/3 P2/2 P3/2 NIT — P2-1 jump-target validation + P2-2 setter/handler guard tests closed, P2-3 flatten-branch-deletion routed OPEN at this same D3-p/G1-p gate) | IMPLEMENTED — PR OPEN, NOT MERGED | both allowlists no-flatten (48+10 tests post-fix-round, mutation-probed, LINEAR-side malformed-timeout/invalid-N coverage added); linear+complex round-trip; linear-only fail-closed reuses the existing `collectParallelRegionNodeKeys` port + positive control; jump-target existence/type check (new); M6 dynamic-M honesty (no invented static bound); M7 every control wired incl. both parallel-region setter guards (new, real-view mount); M8 stale-comment correction; P2-3: the `: 'single'` fallback is DELETED — `unsupportedTemplateAuthoringReason` is now the single door for an out-of-union `approvalMode`, hydration preserves the raw value verbatim (2 new tests, mutation-proven: restoring the fallback reds exactly the negative test); no engine change | review + merge; **P2-3 CLOSED per lock text — not a self-ratified substitute; still subject to the pending owner ratification of the PR as a whole (D3-p/G1-p gate) alongside every other P1-C item** |
| P1-D | P1 | Canvas residual + editor version entry | D0 delta | #4951 | `5fe9489a0f` reviewed; fix round head `1edded1fa9` requalified CLEAR (gate MD `/tmp/pr4951-requalification-claude-20260817.md`); post-requalify residual round applied on top (banned-mechanic copy replaced, paint-tuple guard widened) | REQUALIFIED CLEAR + RESIDUAL ROUND | migrate shipped type ribbons to the ratified flat-card grammar; browser/a11y (NOT discharged this slice — jsdom source-scan + jsdom computed-style only, carried to P7-B real-Chrome tenant acceptance); no graph semantic delta (strengthened by the fix round: the branch-delete UI mount was dropped entirely, so zero topology-mutation surface is reachable from this slice's UI, not merely byte-identical command files) | implementation authorization |
| P2-A | P2 | New enterprise assignee semantics | Lock-1 (PROPOSED, `approval-lock1-enterprise-assignees-20260817.md`) | #4940 | `7fb53514d7...` docs-only | LOCK PROPOSED — NOT RATIFIED | directory/corp/empty-resolution matrix; §3 gates G-1..G-20; OD-L1-1..7 owner decisions | ratify lock |
| P2-B | P2 | Department/contact fields and routing | Lock-2 | — | — | LOCK RATIFIED — implementation NOT STARTED | field schema plus routing, snapshot/live, corp, authz tests | implementation authorization |
| P3-A | P3 | Missing flow policies only; preserve shipped merge flags | Lock-4 | — | — | NOT DRAFTED | server-enforced fallback/automatic-decision/departure-policy mutations plus merge non-regression | ratify lock |
| P3-B | P3 | More settings first functional group | D0 delta + Lock-4 + selected Lock-6 subset | — | — | BLOCKED ON P3-A + GLOBAL POLICY | no inert controls, browser tests | selected global capability landed |
| P4-A | P4 | Handler node | Lock-3 | — | — | NOT DRAFTED | all graph walks, txn rollback, version tests | ratify lock |
| P4-B | P4 | Runtime readonly/editable enforcement | Lock-7 + named edit surface | — | — | LOCK RATIFIED — impl BLOCKED ON HANDLER EDIT SURFACE (Lock-3 P4-A) | HTTP bypass negatives, transaction tests | implementation authorization + edit surface |
| P5-A | P5 | Per-node operation policies | Lock-5 | — | — | NOT DRAFTED | UI/server agreement, role matrix | ratify lock |
| P5-B | P5 | Requester/global policies | Lock-6 | — | — | NOT DRAFTED | direct API negatives, time bounds | ratify lock |
| P5-C | P5 | Shared dialogs, detail table/tabs, center master-detail | landed policy subsets | — | — | NOT STARTED | existing timeline/summary/aging/batch/retry do not regress; mounted/browser/mobile/a11y | matching capability landed |
| P6-A | P6 | Additional bounded form controls excluding department/contact | Lock-8 subsets | — | — | NOT DRAFTED | schema/runtime/round-trip per field | ratify each subset |
| P7-A | P7 | Exact merged-main full verification | all landed phases | — | — | BLOCKED | verification document complete | owner review |
| P7-B | P7 | Canvas tenant acceptance | P0-P5 merged + P7-A | — | — | BLOCKED | S1-S12 on exact deployed SHA | owner UAT |
| P7-C | P7 | FWB UAT | durable delivery + FWB line + P7-B | — | — | BLOCKED | durable and FWB flags recorded; create/update/decision, net-effect-once | owner UAT |
| P7-D | P7 | Attachment UAT | attachment line + P7-C | — | — | BLOCKED | authz, bind, download, GC | owner UAT |
| P7-E | P7 | Staged flag enablement | owner handoff + P7-D | — | — | BLOCKED ON P7-D | Canvas, durable prerequisite, FWB, then attachment rollout/rollback observations | owner operation |

## 3. Decision ledger

| Decision | Current value | Authority / evidence | Reopen condition |
|---|---|---|---|
| Renderer | bespoke deterministic canvas | RATIFIED D0; O3 DEFER | measured 100+ node failure or owner decision |
| Free-form wiring | not supported | graph safety boundary | new graph-model lock |
| Accessible list fallback | retained | S12 equivalence not proven | S12 + owner fallback window |
| Form identity | opaque allocator for new builder | proposed #4866 | owner ratification |
| Identity/reference provider | unresolved between PR #4866 proposal and current command API | current source audit | revised #4866 ratification |
| Multi-source runtime | existing union + identity dedup | resolver and backend normalization | runtime defect evidence |
| Timeout/threshold | backend persists/executes; frontend over-locks affected graphs and can flatten unsupported mode in linear hydration | current source contract | P1-C completion |
| More settings | not mounted empty | master M7 | first functional policy group |
| Handler | missing | requires Lock-3 | ratify + implement |
| Readonly/editable | runtime-inert | existing type comments | handler/edit path + Lock-7 |
| Action attachments | not part of dialog-only work | master M9 | separate capability lock |
| Number FWB | fail-closed | existing FWB contract | independent exact-number decision |
| Flag order | Canvas, durable prerequisite plus FWB, attachments | owner handoff plus production FWB dependency | explicit owner change |
| Private release prerequisites | tracked outside public repository artifacts | disclosure discipline | owner records closure out of band |
| explanation (说明) palette group (Lock-8 L8-A §2.6) | 其他 group, in both `ApprovalFormPalette.vue` (F2) and `TemplateAuthoringView.vue`'s own copy | reversible presentation choice, goal-set instruction (2026-08-17) executing this slice — Lock-8 §2.6 records the group as needing an owner decision, not yet ratified as an OD-L8-3 group | owner picks a different group; a data-only change to both palette arrays |

## 4. Evidence ledger template

Append one row for every review or verification event. Never edit an old verdict to cover a new SHA.

| Date | Slice / PR | Exact SHA | Evidence type | Command / probe | Result | Reviewer | Residual |
|---|---|---|---|---|---|---|---|
| 2026-08-17 | historical plan input | `38ac9178f4...` | reported prior evidence | form/node/history 35; resolver 27 | NOT RE-RUN; historical only | prior review | does not qualify current baseline |
| 2026-08-17 | current planning-source audit | `b84c30db97...` | full proposal + source + cross-lock + CI-file review | production entrypoints, pure command substrate, backend contracts, Feishu offline corpus | PLAN CORRECTED; no runtime/browser qualification | Codex + independent Grok | P0/P1 scope corrected; private release details remain out of band |
| 2026-08-17 | docs REV-2: rebase + owner review fixes | this commit @ `d33a6a0fa1` base | owner REQUEST-CHANGES (2 P2 + 2 P3) + independent claim verification (V1-V8: 6 confirmed, 2 nuanced) | rebase to current main; M3/P0-B4 retype split; P1-C flatten-reachability wording; §0.2 provenance; baseline refresh | APPLIED; docs-only, no runtime change | Claude (fix round) after Codex + Claude cross-review | ratify remains owner-only on the new single-commit SHA |
| 2026-08-17 | master lock ratification | reviewed `217b56137e`; merged `5b31cb4349` (#4935, blob-identical) | owner decision record | §9 filled; Status PROPOSED → RATIFIED; required checks 14/14 green on the reviewed head; owner merged #4935 personally | RATIFIED (design program only) | owner (explicit in-session instruction); recorded by Claude | runtime/UAT/deployment/flags remain individually gated; next actionable = P0-A refresh of PR #4866 |
| 2026-08-17 | Lock-0 D0 interaction delta ratification | drafted `88ff0ff037` + record commit | independent fable review of opus draft; owner goal-instruction provenance | six deltas L0-1..L0-6 accepted with dispositions D1-D6; master §3 Lock-0 row + §4 UI-0 row updated in the same PR | RATIFIED (P1-A design only) | Claude after opus draft | P1-A0/A/B/D unblocked pending P0 hot-file handoff; route-preview debt tracked on UI-0 |
| 2026-08-17 | Lock-8 field-vocabulary ratification | drafted `cbf1014a65` + record commit | independent fable review of opus draft (spot-verified executor fail-open default + two task-premise corrections); owner goal-instruction provenance | L8-A 说明/L8-B date_range/L8-C formatted-number-as-props/L8-D formula deferred; nine ODs per recommendation; no-print-substrate exclusion; census-not-checklist doctrine | RATIFIED (design only) | Claude after opus draft | P6-A slices unblocked after P0 completes; L8-C rides the existing number type |
| 2026-08-17 | Lock-6 requester/global-policy ratification | drafted `1653922682` + record commit | independent fable review of opus draft (spot-verified L6-P1 shipped defect: policy carrier destroyed on republish); owner goal-instruction provenance | L6-A dedup-tier = first global policy + fifth-step activator; v1 = L6-A + L6-P1 prerequisite; FWB revoke seam = reject-when-fired; ten ODs per recommendation | RATIFIED (design only) | Claude after opus draft | L6-P1 is a shipped-defect prerequisite (API-set policy.autoApproval destroyed by editor publish); L6-E moved to Lock-5 |
| 2026-08-17 | Lock-2 org-controls/field-routing ratification | drafted `a30970af13` + review round `a1a932ddc3` + record commit | independent adversarial review (REQUEST-CHANGES: 1 P1 Lock-1 §K4 disposition re-open, 1 P2 OD-L2-8(a) cost omission, 1 P3, 4 NITs — all closed in review round; 15 load-bearing claim groups spot-verified, zero code claims refuted); owner goal-instruction provenance | dept value = local uuid; props allowlist on shipped `user`; UNION + publish maxSelections pin; required-pin retrofit at next save/publish; no-visibilityRule pin + create-time 422; cc rows deferred; multi-selection lands with prop or publish rejects; dept check on every field vs create anchor with unlinked-requester cost accepted | RATIFIED (design only) | Claude (opus draft, fable review) | Lock-1 §K4 erratum candidate recorded (citation imprecision only, posture binding); form_field_user publish pin checks neither required nor visibility — closure is Lock-2's own §2.7 finding |
| 2026-08-17 | OD-L4-10(a) boundary correction + L6-A prerequisite fixes (PR #4965/#4966 stack) | round-scoping `fd2932104d` (PR-1) + publish-sequencing (PR-1.5) | two live pre-existing bugs surfaced by P3-B verify-against-main and adversarially gated (opus, 2 rounds + requalify): (1) return-nullification — the dedup cascade read FULL unfiltered history so a return/backward-jump re-entered node could be wrongly re-auto-approved by a prior-round approval (Lock-4 F4-D's API-reachable trace, live-reproduced incl. via a backward TIMEOUT-JUMP); (2) publish-sequencing — confirmPublish read policy AFTER persistDraft rebuilt the draft, silently discarding in-session policy edits (corroborates the L6-P1 gate's F3 allowRevoke finding) | fix (1) = to_version floor scoped to BACKWARD RE-ENTRY (manual return OR backward timeout-jump via the visited-path predicate; adminJump structurally forward-only; forward jumps unaffected — positive control); fix (2) = snapshot buildPublishPolicy(draft) BEFORE persistDraft | **OD-L4-10(a) ERRATUM CANDIDATE (owner)**: the ratified wording says round-scope via 'the existing nodeEntryEpoch machinery' and names only 'return' — the faithful boundary is **backward re-entry (return OR backward jump)** and the mechanism landed as a to_version floor (adjudicated FAITHFUL to the OD's intent: cross-node dedup needs an instance-scoped floor, nodeEntryEpoch is per-node); goal-set provenance, reversible | Claude (impl + opus gates) | gate A-7 discharged with floor-consumption discriminators for BOTH the return floor and the backward-jump floor (immediate []-seed tests alone were adjudicated theater) |
| 2026-08-17 | Lock-5 node-operation-policy ratification | drafted `799a3a6efa` + record commit | independent fable review of opus draft (spot-verified add-sign placebo + rejectCommentRequired double-hardcode); owner goal-instruction provenance | eleven ODs per recommendation (nodeOperationPolicy object / absent≡allowed / after-sign = deferred same-node round / unified commentRequired / policy_denied row) | RATIFIED (design only) | Claude after opus draft | 前加签 honesty fix (B-1/B-2) is a P5 early slice; signaturePolicy stays declared-inert with named owner slice |
| 2026-08-17 | Lock-1 enterprise-assignee semantics ratification | drafted `02e80020c2` + record commit | independent fable review of opus draft; owner goal-instruction provenance | K1-K6 contracts; seven ODs recorded per recommendation (EAGER/curated-binding/latest-epoch/empty-policy/level-first/termination-field/cc-widen); G-1..G-20 gate table | RATIFIED (design only) | Claude after opus draft | P2 K-slices unblocked serially; K6 FE after P1-C; group endpoint deferred pending K1 landing |
| 2026-08-17 | Lock-4 flow-policies ratification | drafted `a56882775c` + record commit | independent fable review of opus draft; owner goal-instruction provenance | F4-A..E contracts; ten ODs per recommendation; return-nullification hazard locked (OD-L4-10) with D-3 gate; auto_reject deferred as recorded residual | RATIFIED (design only) | Claude after opus draft | P3-A slices unblocked (Lock-1 landed via #4940; K3 seam condition satisfied); P3-B nominee = template-level dedup tier |
| 2026-08-17 | Lock-3 handler-node ratification | drafted `2e850553c1` + record commit | independent fable review of opus draft (spot-verified org-detector keying + partial-index correction); owner goal-instruction provenance | handler node contract + 25-row blast radius + Lock-7 seam; seven ODs per recommendation (linear-only/no-fallback-key/opinion-optional/metrics-split/handler-ordinal/7-of-8/submit-only) | RATIFIED (design only) | Claude after opus draft | P4-A slice unblocked after P0-P1 hot files free; field-write key 422 until Lock-7 |
| 2026-08-17 | Lock-7 field-edit/visibility enforcement ratification | drafted `aab26f740e` + disclosure redaction `5c8eae9ec5` + review round `70ce8773b3` + record commit | independent adversarial review (REQUEST-CHANGES at 5c8eae9ec5: 2 P2 [OD-L7-11 named nodeEntryEpoch reuse that cannot fire on same-round edits → re-specified as a NEW per-edit marker; G-13 routing-hint-correction premise inverted under recommended OD-L7-8(a) → scoped to readonly strings + arm-conditional] + anchor/citation P3/NITs, all closed; D-1 and OD-L7-6/8 adjudications confirmed sound); owner goal-instruction provenance | twelve ODs per recommendation (shipped enum carrier; three-state kept; handler-only write v1; approval+handler node types; one derivation instance-scoped read + actor-node write; in-place form_snapshot UPDATE in Lock-3's txn + append-only revisions; values-free audit; routing-driver publish-pin; legacy absent≡editable; actor-scoped per-field access DTO; 内容变更 via NEW per-edit marker not epoch reuse; detail sub-columns excluded) | RATIFIED (design only) | Claude (opus draft, sonnet review) | D-1 = confirmed shipped defect (fieldPermissions silently discarded on cc/start/end/condition/parallel nodes, ApprovalProductService.ts:1858-1943) logged for a separate fix slice; D-5 read-scope stays OPEN owner question; disclosure redaction moved D-3/D-5 exact anchors to private inventory |
| 2026-08-17 | P1-A gate review adjudication (#4944, Lock-0 L0-1/L0-2/L0-6) | reviewed `347c8035ef` (pre-fix-round head) | independent adversarial gate (refute-first); 14 mutation/instrumentation probes + real-Chrome mechanism probe; gate MD `/tmp/pr4944-review-claude-20260817.md` | full gate report §4 "Adjudication of the four self-reported deviations" plus the A-12/A-1 gate-verdict rows in §2 | REQUEST-CHANGES at reviewed head (1 P1, 3 P2); five deviations adjudicated and recorded here per the gate's item 7 requirement: (1) A-8 "exactly one undo entry" literal is UNMET at this baseline — ACCEPTABLE-WITH-LEDGER; named follow-up = "route node-config commits through the canvas authoring history" (touches `TemplateAuthoringView.vue` + the commands module, a distinct slice from P1-A); (2) A-4's anti-flatten guard (`complexNodeConfigHasBackendDrop`/`unsupportedTemplateAuthoringReason`) is template-wide, not node-scoped — ACCEPTABLE-WITH-LEDGER, component-only scope directly verified (all 8 radios `disabled===true` in the unknown-kind fixture via the pre-existing upstream guard); named follow-up = node-scoped read-only degradation; (3) A-12 "the toolbar owns Left/Right within itself" is FALSE (no toolbar keydown handler exists) — recorded PARTIAL, not PASS; corrected widget census for the panel = THREE arrow-key-relevant controls (tablist roving-tabindex, the L0-2 roster `role=radiogroup`, and the toolbar whose claimed-but-absent arrow handling the lock text overstated), not the two the parent lock's A-12 wording implied; (4) A-1 "always present on approval nodes" is narrowed to approval nodes WITH editable config (`hasEditableApprovalConfig`, `ApprovalCanvasNodeInspector.vue`) — a legacy approval node with no seeded edit keeps the pre-L0-1 flat read-only summary instead of an empty tab strip; recorded as a Lock-0 §-note (contract-legitimate per the lock's own anti-"empty tab theater" clause), not left as undocumented implementation lore; (5) the shared assignee-source picker component change (L0-2 radio grid + D1 labels superseding the committed `el-select`) reaches the `辅助编辑模式` (accessible list/flat) surface too, not only the canvas tabbed presentation — L0-2's "one picker component" contract clause authorizes exactly this shared change, but the PR body must state it, not imply the flat surface untouched. The P1-1 (destructive roster kind-switch)/P2-1 (D5 false remediation)/P2-2 (unpinned D1 labels)/P2-3 (untested flat-mode visibility + tab content-membership)/A-9 (substring hint pin) blocking findings from the same review were fixed in this PR's gate fix round (see PR body "Gate fix round" section for the new head SHA and probe re-run results) | Claude (Opus 5), independent adversarial gate; fix round by Claude (Sonnet 5) | owner ratification of this adjudication (and of the fix round) remains outstanding; A-8's route-through-history fix and A-4's node-scoped degradation remain unscheduled follow-up slices, not this PR's scope |
| 2026-08-17 | P1-D gate review adjudication + fix round (#4951, Canvas residual + editor version entry) | reviewed `5fe9489a0f` (pre-fix-round head); fix round applied on rebased head, exact SHA in the PR body | independent adversarial gate (refute-first); full-file reads at PR ref, disposable worktree, real vitest runs, 5 mutation probes + 2 constructed-input probes, `git merge-tree` conflict check; gate MD `/tmp/pr4951-review-claude-20260817.md` | full gate report §2 findings P1-1/P1-2/P2-1..P2-6/P3-1..P3-6; §4 gate-by-gate table | CHANGES-REQUESTED at reviewed head (2 P1, 6 P2); fix round applied per an in-session adjudicated decision set (not an owner-authored ratification — recorded here so this is never mistaken for owner sign-off): (1) P1-1 unauthorized destructive capability — resolved by the gate's option (a): the branch-delete affordance (`removeConditionBranch`/`canRemoveConditionBranch` template button, view-layer handlers, and both `ApprovalNodeConfigEditorApi` members) is DROPPED from this PR in its entirety, not authorized; the command layer (`graphTopologyEdit.ts`) stays untouched and uncalled from any approval UI; a future slice may reintroduce delete with its own owner authorization and ledger row; (2) P1-2 default-branch-card false routing claim — fixed by gating the `未满足其他条件时进入默认流程` copy on a non-empty `defaultEdgeKey`, with an honest empty-state line for the no-default case matching real `ApprovalGraphExecutor.resolveConditionTarget` first-edge fallback; negative + positive-control tests added; (3) P2-6 mandated D0 §4.1 priority-direction/header-hint copy — added verbatim ("优先级 1 最高" on the priority-1 chip; "分支按优先级从上到下依次判断，全部不满足时走默认分支。" once, in the condition inspector header), pinned verbatim in tests; self-caught during the fix round, NOT itemized by the original gate: rendering the header hint unconditionally reintroduced the exact P1-2 false-routing claim ("...全部不满足时走默认分支" is false with no default configured) — fixed by gating the header hint's VISIBILITY on the same non-empty-`defaultEdgeKey` predicate as the default card (the verbatim string itself is unchanged, so D0's "lives once" stays satisfied at count 0 or 1); both the positive-control and negative P1-2 tests extended to assert the header-hint count alongside the card copy; (4) P2-1 defeatable ribbon pin — the source-text pin was broadened from a `canvas-node-kind`-literal match to a mechanism-level `[data-node-type=...] { ... background` match (selector-shape-independent) PLUS a new jsdom computed-style test (raw `<style scoped>` block injected into the test DOM, hand-built per-type node/kind-bar fixtures, `getComputedStyle(...).background` compared across types) — both proven RED against the gate's exact MUTATION-A restatement and the literal MUTATION-A2 positive control, both proven GREEN at the clean head, restored byte-identical via `cp` + sha256 verification (never `git checkout --`); (5) P2-3 reserved-channel color — `parallel`'s left-border accent moved off `--el-color-danger` (the destructive/error token) to `--el-color-info` (already used for `parallel` elsewhere in this same component, the edge-insert icon); the false `:466` "validation accent" comment (no per-node validation marker exists in this component) was corrected. Considered trade-off, recorded not silent: `--el-color-info` is already `start`/`end`'s left-border token, so `parallel` now renders the SAME accent color as those two types — three of six types share one color post-fix. No D0 clause requires six mutually distinct type colors (text stays the required carrier); the alternative (an unused-elsewhere token) would restore distinctness but fails the "existing token used for informational accents elsewhere in the approval UI" constraint. Left as-is; an owner or a later slice may pick a dedicated token if distinctness across all six types is wanted; (6) P2-4 `TARGET_FILES` conflict — resolved as a UNION on rebase onto `origin/main`, keeping all four entries (`ApprovalFormPalette.vue`/`ApprovalFormBuilder.vue` from main + `ApprovalFlowCanvas.vue`/`ApprovalGraphNodeConfigEditor.vue` from this PR), verified present post-rebase; the PR body's "clean pass, not a migration" claim is corrected in-place (main carried a `rgb(0 0 0 / 4%)` box-shadow literal in `ApprovalFlowCanvas.vue` that this PR's flat-card change deleted — a real migration for that file); (7) P3 mechanical NITs fixed: dead `.is-selected`/`.is-moving` left-border override under equal-specificity cascade (per-type rules moved before the selection-state rules so the later, equal-specificity selection rule wins), the no-op `border-style: dashed` on the default card (already inherited from the sibling `.template-authoring__condition-branch` class), and the corrected `:466` comment (folded into item 5). NOT discharged in this fix round: P2-5's named "browser" evidence — this remains jsdom source-scan + jsdom computed-style only; a real-Chrome verification of the flat-card grammar (V-8 restraint audit, V-6 focus-ring contrast) is carried forward to the P7-B tenant-acceptance browser matrix (S1-S12), not claimed here. Owner ratification of P1-D's implementation authorization (per §2's owner-gate column) also remains outstanding — a gate fix round is not that authorization | Claude (adversarial gate, model unspecified in the MD); fix round by Claude (Sonnet 5), executing an in-session adjudicated-decision set supplied by the orchestrating session, NOT an owner-authored ratification | owner ratification of P1-D's implementation authorization and of this fix round remain outstanding; re-review is required at the post-fix-round, post-rebase head per ledger rule 3; P7-B carries the deferred browser/a11y evidence |
| 2026-08-17 | P1-D requalification + residual round (#4951) | requalified CLEAR at `1edded1fa9`; residual round on top (SHA in PR) | same gate agent re-verified every closure (blob-level byte-identity for the dropped delete affordance; PROBE-D-B/PROBE-POS both directions; MUTATION-A/A2 both RED; full lane 363 files/4433 tests exit 0) | NEW-2 (P2): the fix round's empty-state copy leaked the banned mechanic word 出边 (D0 §13.1) — replaced with 流程走向不确定，请指定默认分支 phrasing, pinned; NEW-3 (P3): both flat-card guards were property-scoped to background — an inset box-shadow ribbon stayed green — computed-style guard widened to the full paint tuple (background/backgroundImage/boxShadow/border/borderLeft/outline/filter/opacity); NEW-1 (P3, INHERITED CONSTRAINT): this PR introduces per-type card accents, so the future D0 §4.2 fork/join slice MUST either make the pair accent override the type accent or re-ratify the type-accent mechanism against §4.2's same-accent-border requirement — recorded here so the §4.2 slice inherits it | RESIDUAL ROUND APPLIED | Claude (gate + orchestrating session) | P2-3 adjudicated: shared --el-color-info accent acceptable (no ratified clause requires per-type color distinctness; type is text-carried per D0 §3.2) |
| 2026-08-17 | F3 gate adjudication + fix round (#4954, delta §5 F3 / P0-B4) | reviewed `deaaeea983` (pre-fix-round head); fix round on the rebased head recorded in the PR | independent adversarial gate (refute-first, 55 mutations, 18 probes; gate MD `/tmp/pr4954-f3-review-20260817.md`) + fix round | FIX-ROUND (narrow): 0 P1, 3 P2 (false M4/M5-RED claim → retracted retraction-first then made true via per-gate tests; 11 undiscriminated disjunct arms → per-arm single-property fixtures; duplicated option-value-preservation rule → one shared `applyOptionLabelEdits` + settle-path pins) | P2s closed in the fix round; P3-1/P3-2/P3-4 command hardening applied same round | Claude Opus 5 (gate); Claude Fable 5 (fix round) | **DEFERRALS with named carrier, per delta §9.1 (owner may instead remove by decision):** (1) delta §3.4 "duplicate" field command and option/detail-row REORDER are not in F3 — **carrier = F4** (the gate adjudicated F3 was the §3.4 carrier, so this deferral needs this ledger row, not a PR-body bullet); (2) the v1 conservative retype floor (ANY inbound current-draft reference blocks ANY type change) defers the finer per-(from,to) compatibility matrix — loosening is a lock decision, **carrier = F4-or-later owner decision**, not an implementation convenience; (3) record-link base/sheet typed pickers are excluded from the F3 inspector (catalog is parent-owned per the F0 gate) — **carrier = F4 mount**, which must wire them so retype-into-record-link has no configuration dead-end; (4) the three redundant drop-path read-only gates: `onSlotDrop`'s early return alone remains unpinned redundant shielding (deeper gates individually pinned in the fix round); full read-only behavior proof rides F4's B8 browser arm |
| 2026-08-17 | P6-amount gate adjudication + P3 fix round (#4959, Lock-8 L8-C formatted-number props on `number`) | reviewed `4853afeda2` (pre-fix-round head); P3 fix round + rebase onto `origin/main` (P4-A #4956 + L6-P1 #4957) on head `215385c390` (recorded in the PR) | independent adversarial gate (refute-first, cp-backup + sha256-verified mutation restores throughout, real vitest + real Postgres runs; gate MD `/tmp/pr4959-p6amount-gate-20260817.md`) + fix round | VERDICT at reviewed head: MERGE-CLEAN, 0 P1, 0 P2 (all L8-C gates — M10/M-1/M-2/OD-L8-6/OD-L8-7/C-1/C-2/C-4/M7/X-1 — PASS; every mutation probe load-bearing) | 2 P3 found, both closed in the fix round: **P3-1** (`ApprovalNewView.vue`'s refactored `amountWordsFor` auto-sum branch was untested by mutation M3 — neutering it to `return ''` reds no test, base also had zero coverage) closed by one new positive-render test in `approvalNewView.spec.ts`, mutation-reproven (neuter → red; cp-backup + sha256-verified restore, never `git checkout --`); **P3-2** — see the dedicated OD-L8-7(a) adjudication line below | Claude (Opus, gate); Claude (Sonnet, fix round + rebase) | **OD-L8-7(a) production-real-template-corpus sweep — recorded here as an explicit OWNER PRE-DEPLOY PRECONDITION, goal-set 2026-08-17, NOT a merge blocker and NOT complete.** Adjudication (gate MD, "OD-L8-7(a) production-corpus-sweep — ADJUDICATION"): the `number`-props allowlist enforcement (`normalizeFormField`, `ApprovalProductService.ts`) is server-side and NOT flag-gated / NOT opt-in — it validates every `number` field publish/restore/clone once this code is **deployed**; the DISPLAY feature (`currencySymbol`/`thousandsSeparator`/`uppercaseCny`) is the opt-in half (confirmed by test: a plain number field with none of the three props renders neither caption), the allowlist enforcement is not. Two sweep components: (1) shipped-preset/repo-source sweep — PERFORMED and independently re-verified by the gate: every `number`-field `props` key across `commonTemplatePresets.ts`, `numberFieldProps.ts`, `lineDerivation.ts` = exactly `{min, max, step, precision, derivedFrom}`, matching the shipped allowlist floor — **complete**. (2) real production-template-corpus sweep required by OD-L8-7(a) ("SWEEPING shipped presets **and a real template corpus**") — **NOT performed; no access to a production template corpus from any code-agent environment; unbounded and not performable by any code agent.** Mechanism verified (gate Mutation 4): dropping `derivedFrom` from the allowlist reds the census exact-set test, the C-2 detail-column publish test, AND the C-2 `restoreTemplateVersion` revalidation test — i.e. an existing production `number` field carrying a props key outside `{min, max, step, precision, derivedFrom, currencySymbol, thousandsSeparator, uppercaseCny}` **would be rejected at its next publish AND at restore**, once this code is deployed and validates real production templates. Merging into `main` with the display feature's flags OFF and no deploy changes ZERO production behavior — the rejection risk materializes only at deploy time. **Before this code reaches a production publish path, the owner must either (a) run the real-template-corpus sweep and confirm/extend the allowlist, or (b) explicitly accept that an out-of-allowlist production `number` field will be rejected at next publish/restore.** Neither has happened; this is owed to the owner, not to this ledger. |
| 2026-08-18 | D-1 fix — Lock-7 §2.7 D-1 read-axis closure (PR #4979) | reviewed `2a17d8358e...` (first head); fix round applied on the rebased head, exact SHA in the PR body | independent adversarial gate (refute-first; dispatch-path-reachability P1 hypothesis EMPIRICALLY REFUTED via a 4-node-type mid-flight tamper + full action battery, all 200; mutation-tested every conjunct of the guard; gate MD `/tmp/pr4979-d1-gate-20260818.md`) + fix round | FIX-ROUND: 0 P1, 1 P2 (empty-array carve-out's positive control silently omitted the key it claimed to test — Mutation A left 19/19 green; fixed to send `fieldPermissions: []` verbatim, Mutation A now reds exactly that assertion), 3 P3 (P3-1 a non-array `fieldPermissions` shape on the five types was STILL silently dropped — closed, guard widened to `rawPermissions !== undefined && !isTolerableEmptyArray`, `!==undefined` not `'in'` per the gate's `publishTemplate` re-normalize-of-normalized-graph note, 3 new negatives + mutation-verified; P3-2 the production comment's FE-compat justification for tolerating `[]` was wrong — corrected to state the true rationale [conservative default; no live surface sends the shape on these five types, since the FE `fieldPermissions: []` step-draft default is for APPROVAL steps, a type this guard exempts]; P3-3 = this row) | P2-1 and P3-1/P3-2 closed in code + tests, mutation-reverified (cp-backup + sha256 restore throughout, never `git checkout --`); P3-3 recorded here per the gate's request | Claude (Opus, independent gate); Claude (Sonnet, fix round) | **P3-3 — the read-axis 400 is a REVERSIBLE public-API narrowing beyond OD-L7-4(a)'s literal ratified text, adjudicated under goal-set provenance, not a lock re-interpretation.** OD-L7-4(a)'s ratified wording scopes the 400 to `editable` only ("`editable` elsewhere is a 400", lock §L7-B pin 2 / OD-L7-4 line 423-424); this PR's D-1 closure additionally 400s `readonly`/`hidden`/non-array `fieldPermissions` shapes on cc/start/end/condition/parallel — beyond the ratified text, in the SAME direction the lock's own NON-ratified sibling option OD-L7-4(c) contemplates going further still ("read-only (`hidden`) support on cc only, per C-3's read-only asymmetry — the cheapest corpus-aligned widening"; corpus rows C-1/C-3/C-5 record the benchmark product offers 表单权限 on 发起/抄写/结束 nodes). Adjudicated choice, recorded not silent: silent-drop → typed 400 is the fail-closed default (an author who configures it now gets an explicit error instead of invisibly losing the config), NOT the corpus-aligned widening (honoring `hidden`/`readonly` on these types instead of rejecting them). The OD-L7-4(c) widening direction remains available as a future owner-ratified slice — it is not foreclosed: no template has EVER been able to persist this shape on these five node types (the per-type switch has never copied `fieldPermissions` into its output config, before or after this PR — gate NIT-2's sweep), so nothing stored breaks either way, and flipping the default from reject to honor later is a pure widening with zero migration cost. Full analysis: gate MD `/tmp/pr4979-d1-gate-20260818.md` §3 Finding P3-3. Owner ratification of "reject" vs. "OD-L7-4(c) widen" as the read-axis's PERMANENT resolution remains outstanding — this PR's choice is the reversible default, not a closure of that decision. |
| 2026-08-20 | Lock-7b node-level required-field tier ratification (docs PR #5025) | drafted `b85987d3ed` + review-fold/record commit on top | independent opus refute-first review of the opus draft (REQUEST-CHANGES at `b85987d3ed`: 2 P1 [enforcement point could not reach the frozen form schema on the dominant no-`fieldWrites` payload — a one-key client bypass, since `fieldWrites` is detected by key PRESENCE; FE round-trip consequence derived from a call site `required` can never reach, with an unsatisfiable positive control], 4 P2 [missed ninth census site — the author-facing enum message; four tracked GENERATED enum copies outside the declared `…/src` sweep, CI-enforced by `plugin-tests.yml:721-722`; an absolute "re-opens none of Lock-7's twelve ODs" over an unswept domain; the obligation was discardable by the actor it binds via a visibility driver], 5 P3, 4 NIT — all closed or dispositioned in-doc, with one correction made TO the review [its withdrawn 409 sub-argument assumed a NOT NULL `template_version_id`; the column is nullable, which does not overturn the finding but makes the arm choice load-bearing]); owner goal-instruction provenance | thirteen ODs per recommendation (fourth enum member as carrier, so 必填 × hidden is unrepresentable by the shipped one-state-per-field dedup guard; per-node independent masks; handler-only satisfiability with a values-free publish rejection on approval nodes; routing-driver publish rejection through the SHARED helper; submit-time enforcement after the write merge reusing `isEmptyValue` verbatim; `hidden` ⊄ writable restated; canvas-only fourth option, absent not greyed; no DDL/verb/bootstrap/flag; type-unsatisfiable rejection with a reopen condition; one mechanical enumeration plus a nine-site census; frozen-schema load hoisted out of the `fieldWrites` key-presence guard and re-gated on the node's required candidates; visibility skip evaluated on the pre ∪ post union; per-SUBMIT binding under 会签) | RATIFIED (design only) | Claude (opus draft, opus independent review, opus fold-and-record) | Implementation LANDED: PR #5026, merged `5feca2291b` (2026-08-20) — the required tier as a 4th `NodeFieldAccess` member (required × hidden unrepresentable-by-construction via the shipped one-state-per-field dedup guard), publish chokes on an unsatisfiable placement (values-free 400) and on a routing-driver field through the SHARED `collectRoutingDriverFieldIds` derivation, handler-submit enforcement (named 422), the 4th authoring state on handler nodes only, and legacy byte-identity; `generate:sdk` was re-run in the same PR (OpenAPI/SDK artifacts updated, discharging the prior forward requirement). Final pre-merge verdict: MERGE-CLEAN bound to pre-squash head `a96ab8ae2b` (requalification #4, superseding four prior FIX-ROUND verdicts across the census-evasion chain). **Squash discipline:** verdicts bind pre-squash heads; ancestry from `a96ab8ae2b` to the merge commit is NOT establishable by SHA — record this as "verdict bound to pre-squash head `a96ab8ae2b`, landed as squash `5feca2291b`", never an ancestry claim. One further commit, `29b28b1f50` (the sealed-export pnpm-lock revert + inline script extractor), landed UNREVIEWED inside that squash. A POST-MERGE requalification at the landed SHA `5feca2291b` returned CLEAN-at-landed-SHA on behaviour, with three P3 claim-honesty findings — fixed by PR #5033, merged `c5a4a94f7f`. **G-14 — OPEN OWNER DECISION, not ruled here:** through the evasion rounds, the census G-14 names was honestly DOWNGRADED to a best-effort backstop, with the COMPILER as the primary gate (a fifth `NodeFieldAccess` member reds both `tsc` and `vue-tsc`, mutation-proven); whether that downgraded shape still satisfies G-14 AS RATIFIED is unresolved. Two residuals remain DISCLOSED, not closed: a hand-edited stored graph carrying an unsatisfiable `required` yields a 422 the actor cannot clear (fix = republish), and the 409 `APPROVAL_FROZEN_SCHEMA_NOT_FOUND` is reachable on a no-`fieldWrites` submit for opted-in templates whose `template_version_id` is NULL (the column permits NULL; whether any live instance carries one is UNSWEPT). OD-L7B-2's safety is borrowed from Lock-3 OD-L3-1(a) and must be re-adjudicated if Lock-3 arm (b) ever ships |
| 2026-08-21 | Lock-9 approver-side process-attachments ratification (docs PR #5011) | drafted `a201b83a44` + independent-review fold `b7858709da` (the pre-rebase SHAs the review bound to), both rebased onto `origin/main@c473a079b5` for the ratification commit that lands on top; on the branch as pushed the same two commits are `b854271441` + `028a8f1adc`, and the pre-rebase SHAs are no longer reachable from the ref | independent opus refute-first review of the opus draft (REQUEST-CHANGES at `a201b83a44`: 4 P2 [upload ownership stated as a REUSE of `actorCanAct`, which is a local `const` with no callable seat check on the shipped upload route; `/refs` bound mode ignored, whose OWN inline hidden gate does not route through `authorizeAttachmentDownload` and for a NULL `field_id` passes by accident and echoes `fileName`; the `field_id` relaxation understated as additive when it is a mutation of the named shipped constraint `approval_att_field_nonblank`, with a deploy-ordering and one-way-rollback hazard; post-commit read scope settled silently by omission], 2 P3, 2 NIT — all folded into §1-§3 and dispositioned in §5, with ONE correction made TO the review's favourable prose [a shipped `router.delete` retracts an uploader's own unbound row; it cannot reach a bound row, so post-commit immutability is unaffected]); owner directed execution BY REFERENCE to the executing session's enumerated recommendation list (2026-08-21, owner-authored reply 「按建议执行」) | fourteen ODs per recommendation (standalone audit-tuple binding with `form_snapshot` untouched and APS `:10507` NOT unfenced; minimal reuse of `approval_attachments` plus a `field_id` relaxation recorded as a constraint mutation, a deploy precondition and one-way once ON; upload authority = acting seat at the active node, bind-time REUSE plus upload-time NEW code, scope `approvals:act`; hidden-field gate DROPPED by an explicit `bind_kind` branch on BOTH read surfaces; `staged_instance_id` at upload with `instance_id` NULL until commit; immutable after commit with orphans to the existing 168 h sweep; a new bind function mirroring the rowCount-equality rollback; a process-scoped per-action budget whose SHAPE is locked and whose numbers are not; validators reused verbatim; NO new action verb; the shipped `APPROVAL_ATTACHMENTS_ENABLED` gate with no sub-flag; values-free scoped to error and audit surfaces; `isInstanceParticipant` reused unchanged, RATIFIED AS AMENDED with an attachment-only scope clause; post-commit read scope = all instance participants with the requester/CC exposure and the act-upload vs read-readback asymmetry both stated and accepted) | RATIFIED (design only) | Claude (opus draft, opus independent review, fold-and-record) | **One wording correction made AT ratification:** OD-L9-13(a) and the matching §L9-C bullet now carry a scope clause confining the "org-pin EXISTS is self-satisfied by the process row" argument to the ATTACHMENT surfaces — a probe executed by the separate comments-reuse analysis (2026-08-21, reported to the lock and NOT re-run by the ratifying session) shows the same predicate returns false for EVERYONE on a zero-attachment instance, so the OD must not be quoted as authorizing reuse on comment or other TEXT surfaces; the per-instance readability predicate is being ruled separately in the S1 lock resolving Lock-7 §2.7 D-5, which Lock-9's attachment reads may adopt once it lands. **Count erratum:** the enumerated recommendation said "13 ODs", counted against the pre-fold draft; the review fold added OD-L9-14, so the ratified document carries FOURTEEN. **Baseline staleness DISCLOSED:** the document's anchors were read at `origin/main@2a3b8033f5` and were NOT re-verified at the rebase head — every implementing slice must re-verify its own anchors at its own head. Implementation NOT STARTED: no runtime code, no migration, no flag change, no UAT, no deployment is authorized; the `field_id` relaxation is a named deploy precondition before the flag may go ON, and instance-detail read scope (Lock-7 §2.7 D-5) remains external. |

## 5. Required evidence by change class

| Change class | Minimum evidence |
|---|---|
| Docs/lock | cross-lock contradiction scan; current-main source anchors; disclosure scan; owner status unchanged |
| Pure form/graph command | focused unit tests; positive controls; mutation or equivalent discriminating negative |
| Mounted Vue editor | mounted spec added to `apps/web/scripts/run-required-web-tests.sh` and therefore the always-on `.github/workflows/web-tests.yml`; if approval guard scope changes, both PR/push filters and its exact Vitest command; a11y assertions |
| Drag-and-drop editor | mounted test plus real Chromium DataTransfer/pointer geometry; pure function or source-scan tests are insufficient |
| Browser geometry | real Chromium at 1440, 1024, and 390; screenshots; overlap/focus/scroll checks |
| Assignee/directory | real-DB or production-equivalent directory fixture; corp isolation; empty and duplicate resolution |
| Approval runtime | real DB; transaction rollback; deterministic interleaving for races; direct API negative |
| Version/migration | old-schema upgrade path, not fresh-only; restore conflict and immutable version proof |
| FWB/attachments | crash windows, authorization, idempotency, GC/bind races, flag-OFF positive control |

## 6. Parallel execution register

Before starting parallel work, add a row and name the file lock. Empty ownership means work must stay
serial.

| Lane | Allowed scope | Excluded hot files | Depends on | Active owner/session |
|---|---|---|---|---|
| Docs | three master documents and bounded child locks | runtime source | current source audit | — |
| Form extraction | new form components + one integration owner | node inspector, backend runtime | P0-A | — |
| Node inspector | extracted inspector/registry after ownership handoff | form builder hot section | P0 extraction + P1-A | — |
| Browser/a11y | tests and evidence only | product code unless finding accepted | fixed target SHA | — |
| Runtime capability | one lock-defined backend slice | unrelated UI/runtime families | ratified lock | — |

## 7. Flag and environment ledger

Initial values are policy assertions, not live-environment observations. Fill environment evidence only
during an authorized UAT or rollout.

| Capability | Code default | Staging observed | Production observed | Enable authorization | Rollback verified |
|---|---|---|---|---|---|
| Canvas V2 | OFF | NOT RECORDED | NOT RECORDED | NO | NOT RUN |
| Durable delivery | explicit env gate | NOT RECORDED | NOT RECORDED | NO | NOT RUN |
| Class A action ledger | explicit env gate | NOT RECORDED | NOT RECORDED | NO | NOT RUN |
| Class B action ledger | explicit env gate | NOT RECORDED | NOT RECORDED | NO | NOT RUN |
| FWB | OFF | NOT RECORDED | NOT RECORDED | NO | NOT RUN |
| Attachments | OFF | NOT RECORDED | NOT RECORDED | NO | NOT RUN |

## 8. Closeout rule

This ledger may say `ENGINEERING COMPLETE` only when implementation rows P0-P6 are merged and P7-A is
green. `CORE-PARITY` additionally requires P7-B, Canvas-relevant private prerequisites recorded closed
outside this public ledger, and owner sign-off.
`DATA-CLOSURE` additionally requires the P6 exact DATA matrix, P7-C/P7-D, their capability-relevant
private prerequisites, and owner sign-off. It may
say `PRODUCT FINAL` only after P7-E records staged enablement, rollback evidence, accepted residuals,
and explicit owner sign-off.

## 9. Errata

| Date | Lock / clause | Correction | Provenance |
|---|---|---|---|
| 2026-08-18 | Lock-5 §2.1 (choke placement) | §2.1 places the operation-policy choke "immediately after [the authorization gate] and **before the first verb branch**". Taken literally that is AFTER the DingTalk card-delivery block, which takes `FOR UPDATE` on `dingtalk_approval_card_deliveries` and CLAIMS the card (`SET card_state='acted'`) in the SAME transaction — so §1.4's records-only `COMMIT` would carry that claim and permanently consume a live card for an operation the server REFUSED. The implementation therefore places the choke ABOVE that block (still after the authorization gate and after Lock-3's handler verb gate), where the transaction has written nothing. Reproduced and pinned: `approval-node-operation-policy.db.test.ts` "§2.1 placement: a DENIED card-channel action does NOT consume the DingTalk card"; mutation M10 (choke moved to the literal position) reds exactly that test — independently reproduced by the adversarial gate (`card_state:'acted'` vs expected `'sent'`). The lock's wording is unsafe as written and should be amended to "before the card-delivery block". | goal-set instruction (2026-08-18); implementation finding on PR #4980, upheld by the independent gate (finding D-1) |
| 2026-08-18 | Lock-5 §1.6 + OD-L5-7(a) (cost basis) | §1.6 states the `opinionRequired` → `commentRequired` rename "costs an edit to an unmerged document, not a migration" **because Lock-3 is not on main**. That premise is now FALSE: Lock-3 landed and `opinionRequired` is SHIPPED end to end — declared on `HandlerNodeConfig`, normalized and persisted, enforced at runtime (422 `APPROVAL_HANDLER_OPINION_REQUIRED`), and carried in the FE `BACKEND_HANDLER_CONFIG_KEYS` allowlist. OD-L5-7(a)'s "incl. Lock-3 adoption" clause is therefore a SHIPPED-CONTRACT change, not a document edit. NOT executed; escalated for an owner decision. | goal-set instruction (2026-08-18); implementation finding on PR #4980 |
| 2026-08-18 | Lock-5 gate A-1 (arithmetic) | A-1 requires `ACTION_POLICY_KEYS` to partition `APPROVAL_ACTION_TYPES` "into **4 policy-gated and 4** by-decision-ungated verbs" — arithmetic taken against Lock-5's own pre-Lock-3 baseline of EIGHT verbs. Lock-3's `handle` has since landed (P4-A), so the union is NINE and the ungated side is FIVE (`approve`/`reject`/`comment`/`revoke`/`handle`). The ratified PARTITION is implemented unchanged; only the counts were stale. The test derives them from the live union rather than the lock's literals and records the discrepancy inline. | goal-set instruction (2026-08-18); implementation finding on PR #4980, graded P3-3 by the independent gate |
| 2026-08-18 | Lock-6 OD-L6-8 vs Lock-5 (L6-E orphaned) | Lock-6 OD-L6-8(a) ("L6-E ownership moves to Lock-5") is RATIFIED and the ledger records the transfer, but Lock-5 contains ZERO `L6-E` / `撤回已同意` matches and its §1.5 deferral table omits it. Lock-6 DOES carry L6-E as a titled deferral subsection (`approval-lock6-requester-global-policy-20260817.md:175`), but `:195-196` explicitly DISCLAIMS ownership pending OD-L6-8 — so that deferral is a hand-off, not a home. Net: **no lock currently OWNS L6-E.** Not implemented in P5. **Lock-5 scope cannot be called closed until the owner dispositions L6-E** into one lock or an explicit deferral row. | goal-set instruction (2026-08-18); implementation finding on PR #4980, confirmed by the independent gate (P3-7) with the Lock-6 `:175` wording correction applied |
| 2026-08-18 | Lock-5 gate A-2 — PARTIAL (SUPERSEDED by P5-B; see the P5-B row below) | **As of P5-A (#4980) only:** the server door landed and is mutation-proven; the MEMBER-BAR mirror did not (the DTO carrier for the current node's effective policy did not exist). Residual harm at that head: a policy-denied verb still rendered a button that 409s, and the four deferred-verb handlers surfaced a generic retry-inviting toast. **This row describes #4980's head and MUST NOT be read as the current state** — P5-B (#4983) lands the FE mirror and the honest denial copy. See the P5-B row for A-2's post-P5-B status, which is STILL PARTIAL but for different reasons. | goal-set instruction (2026-08-18); adversarial-gate adjudication A + finding P2-3 on PR #4980, superseded per finding P2-2 on PR #4983 |
| 2026-08-18 | Lock-5 OD-L5-4(b) / gates B-1..B-5 — OWNER DECISION, four enumerated completions | OD-L5-4(b)'s literal shape ("the addees activate as a fresh nodeEntryEpoch round at the SAME node") is REFUSED by shipped machinery at a multi-seat 会签 node: `currentNodeEntryEpoch` fails closed on a node whose active seats span epochs, so the untouched sibling's next decision returns 500 `APPROVAL_NODE_ENTRY_EPOCH_MIXED` and the instance is bricked for that approver. Constructed, observed and pinned in `approval-add-sign-honesty.db.test.ts`, with a positive control (re-aligning the seats to ONE epoch makes the identical approve return 200) isolating the epoch SPAN as the cause — independently reproduced by the adversarial gate. The lock contains no disposition, so completing B-3 (and therefore B-1/B-4/B-5) is an owner call. **FOUR completions exist on the face of the code, not two:** (1) refuse `'after'` at multi-seat nodes — a values-free 409 at the INV-6 guard site; a contract ADDITION the lock does not authorize; (2) a deferred-round ledger — new persisted state, contradicting OD-L5-4(b)'s own "no graph mutation, existing machinery"; (3) **re-epoch EVERY live seat at the node** to the fresh epoch, so one round carries one epoch and the invariant holds — cost: the card binding at `ApprovalProductService.ts` requires a delivery's non-null `entry_epoch` to equal a LIVE seat's epoch, so an outstanding sibling DingTalk card goes stale (`APPROVAL_CARD_DELIVERY_STALE`); a stale card is a lesser harm than a bricked instance; (4) **seat the addees at the CURRENT epoch** and consume the actor's seat — no new state, existing machinery, but it drops the word "fresh" from OD-L5-4(b)'s literal, so it is an owner call rather than an implementer's reading. B-1 is deliberately NOT landed alone: widening the doors without the runtime would make `'after'` a NEW placebo, the very shape B-2 exists to retire. | goal-set instruction (2026-08-18); implementation finding on PR #4983, option set expanded per adversarial-gate finding P3-4 |
| 2026-08-18 | Lock-5 gate X-3 — NOT MET, premise corrected | X-3 (real-browser, three viewports) is unrun. **Premise correction:** `.github/workflows/approval-browser-verify.yml` is the FORM-BUILDER verification lane — it pins a single spec through `playwright.approval-verification.config.ts` with its own `paths` list — so it is not a lane X-3 can simply be added to; X-3 needs its own harness plus a viewport matrix, and its mobile arm additionally depends on the `approvalMobile` feature flag rather than viewport width alone. Post-P5-B the MEMBER-BAR half is at last runnable (the mirror ships); the AUTHORING-tab half still cannot run for `commentRequired` (no control renders — deferred per §2.5). **Lock-5 closure cannot be claimed while X-3 is open.** | goal-set instruction (2026-08-18); adversarial-gate finding P3-5 on PR #4983 |
| 2026-08-18 | **P5-B slice row** (Lock-5 §0:29 / §4 — one ledger row per slice) | **Slice:** P5-B — B-2 `'before'` honesty, §1.3 `commentRequired` enforcement, A-2 FE member-bar mirror. **PR:** #4983, stacked on #4980. **Delivered:** B-2 (identity pin + parallel-region non-vacuity control; the inert 前加签 radio retired, wire contract unchanged/widen-only); CR-1 and CR-2 (BOTH §1.3 hardcodings moved together — the unconditional reject check now reads the effective policy, and instance CREATE stops writing the `policy_snapshot` literal; new values-free `APPROVAL_COMMENT_REQUIRED` on the approve side, `REJECT_COMMENT_REQUIRED` preserved on the reject side); the A-2 carrier (actor-seat-scoped, most-restrictive, detail-read only, covering BOTH user- and role-typed seats). **Gate A-2 = PARTIAL, precisely:** real and mutation-proven for user-typed AND role-typed seats, but the three pre-existing M7 divergences are UNTOUCHED (parallel-region 退回, the history-derived `returnableNodes`, global-RBAC `canAct`) and X-3's real-browser half is UNRUN. **Gate CR-3 = PARTIAL (2 of 4 sites):** detail dialog and card view moved; `ApprovalCenterView` batch and per-row reject still read the instance literal because the LIST DTO stays byte-identical — conservative-never-permissive on the REJECT side, but batch APPROVE at an `'always'` node fails loud (400 per row into the failure manifest), which is a usability gap, not a conservative default. **Still open after this slice:** B-1, B-3/B-4/B-5 (owner decision — four enumerated completions, see the §2.1/B-3 rows), CR-3's list half, X-3, and the `commentRequired` AUTHORING control. **Wire-shape note:** `policy.rejectCommentRequired` no longer appears on the DTO for instances created after this slice; every shipped reader defaults with `!== false` / `?? true`, so behaviour is stable. **Flags:** none added, none changed. **No migration.** | goal-set instruction (2026-08-18); adversarial gate on PR #4983 (FIX-ROUND, 0 P1) |
| 2026-08-18 | Lock-5 §3 (required-lane requirement) — DEVIATION DISCLOSED | §3 states "Backend gates land in the required backend lane". `approval-realdb-node-operation-policy` is NOT a required context on `main`, and the suite is also excluded from the no-DB config, so **the §2.1 choke and the §1.4 denial-row write have no blocking coverage** — a regression in either can merge green. Precisely scoped: D-3's exclusion predicates ARE unit-pinned in the required `test (20.x)` (`tests/unit/approval-history-routing.test.ts`), the D-1 migration/bootstrap parity is now pinned there too, and the FE half is gated by the required `web-tests`; it is server-side enforcement + the audit write specifically that nothing blocking exercises. The standalone-lane choice matches all five sibling `approval-realdb-*` lanes and the verified s6a rationale (`plugin-tests.yml` is a `PINNED_EVIDENCE_FILES` input, so editing it forces a re-pin and a merge race) — a program-wide cost tradeoff, not an implementer choice. Mitigation: the lane's `pull_request`+`paths` trigger fires on any PR touching `ApprovalProductService.ts`, so a regression is VISIBLE, just not BLOCKING. **Owner decision required:** add the context to branch protection, or add the file to `plugin-tests.yml`'s existing `approval-real-db-integration` whitelist and absorb the s6a re-pin. | goal-set instruction (2026-08-18); adversarial-gate finding P2-2 on PR #4980 |
| 2026-08-18 | Lock-5 §1.4 / gate D-1 (positive control was vacuous) — REPAIRED | D-1's mandated control ("reverting only the migration reds the insert") did NOT hold: `approval-schema-bootstrap.ts` unconditionally DROP/ADDs `approval_records_action_check` and every real-DB suite calls `ensureApprovalSchemaReady()` in `beforeAll`, so the bootstrap OVERWRITES the migrated DDL — removing `'policy_denied'` from the production migration left the suite 20/20 green. The pre-existing bootstrap pin was an UNANCHORED substring that also passed with `handle` AND `policy_denied` both missing. Repaired in the REQUIRED `test (20.x)` lane by a structural migration↔bootstrap exact-set parity assertion (both directions, throw-on-missing-anchor, with its own negative control); the M-mig mutation now reds a named test. **Release requirement recorded:** the migration must be applied BEFORE or WITH the image carrying the choke — an image-first deploy makes every denial a raw pg error → HTTP 500 instead of the contracted 409, with no audit row. | goal-set instruction (2026-08-18); adversarial-gate finding P2-1 on PR #4980 |
| 2026-08-17 | Lock-3 §2.2 (Batch / 秒批 exclusion) | The claim that handler-task batch exclusion "falls out of §2.1 rather than needing new code" was FALSE — the member 待办 center listed/badged/batch-selected any active pending seat with no node-type filter, so a handler seat surfaced an inert `approve`/`reject` control (409, M7). Corrected in the lock: the exclusion is enforced by an EXPLICIT node-type gate landed in P4-A / PR #4956 (pending-list DTO `currentNodeType` + `isRowBatchSelectable` gate + pending-count exclusion). The ratified property is now TRUE against the code. | goal-set instruction (2026-08-17); adversarial-gate finding P2-1 on PR #4956 |
| 2026-08-18 | Lock-1 §K1 / OD-L1-2(a), gates G-5/G-6 (org-boundary framing) — CORRECTED | §K1's own text frames OD-L1-2(a) against "the discriminating question: does it reject a cross-boundary group at publish", and G-6 demands "two orgs … **identically-named** group fixtures" + "routing resolves only the requesting org's members". Both are **unsatisfiable as worded, independent of implementation quality**, for two structural reasons the lock did not name: (1) `platform_member_groups.name` carries a GLOBALLY UNIQUE index (`idx_platform_member_groups_name`, `zzzz20260409154000…:24-26`) — two identically-named groups cannot exist, so G-6's fixture is impossible to build; (2) groups carry NO org column and this codebase resolves no actor→org identity anywhere in the kernel create/publish path (verified: `resolveApprovalRequesterOrgRelations`'s own `orgId` option is unset by the kernel create path per §K1's own citation, and no `orgs` table exists) — so *resolution* (which members get seats) is org-agnostic by construction under option (a); org-selectivity exists ONLY at the publish gate, as a curated **namespace** a caller states explicitly, never as identity-resolved tenant anchoring. Live-probed on PR #4995: an unrelated non-admin caller holding only `approval-templates:manage` could list (`?orgId=org-b`, returns id+name) and publish against (`{"orgId":"org-b"}`) a namespace it has no relationship to — this is *intended* under option (a), not a leak, once curation itself is properly gated. **CORRECTED READING (record, do not re-engineer):** `orgId` is an administratively-curated NAMESPACE, not a tenant/caller boundary. Its only real enforcement is that a PLATFORM ADMIN controls what is bound into each namespace — i.e. the boundary lives entirely in who may write `approval_usable_member_groups`, not in who may name an `orgId` when reading or publishing. Building identity→org anchoring to satisfy G-6 literally would be NEW mechanism §1 Non-effects and §4 "Runtime authorization: NONE" do not authorize — explicitly not done. **G-6 is recorded UNSATISFIABLE-UNDER-(a)** for the two reasons above; its intended protection is delivered instead by the write-side fix below. G-5 stays PASS (the publish gate correctly rejects a dangling/uncurated reference) but its prose is narrowed to a curation check, not a tenant check. | goal-set instruction (2026-08-18); independent adversarial gate on PR #4995 (verdict FIX-ROUND; live PROBE-1/PROBE-2 reproduced the cross-namespace read/publish; gate MD `/tmp/pr4995-k1-gate-20260818.md`) |
| 2026-08-18 | Lock-1 §K1 / OD-L1-2(a) — curated binding write path (P1 SECURITY FIX) | The migration's own doc-block frames `approval_usable_member_groups` as EXTENDING the RA-1b curated-vocabulary pattern (`roles.approval_usable`) — but RA-1b's own precedent has NO API write path (curated out-of-band by design), and every existing `platform_member_groups` write/list surface in this codebase (`routes/admin-users.ts`: list `:2147`, create `:2164`, detail `:2206`, assign/unassign `:2383`, `:2653`) is `ensurePlatformAdmin`-gated. PR #4995's initial bind/unbind route instead shipped behind `approvalTemplateAdminGuard` — the SAME permission `/publish` requires, and one this codebase deliberately admits non-admin template authors under — making curation self-service for the exact principal OD-L1-2(a) exists to constrain: live-probed (adversarial gate PROBE-3, PR #4995), a non-admin `approval-templates:manage` caller could `publish` (400, correctly blocked) → self-`bind` a group (200) → `publish` again (200), all in one request-chain, with no admin ever involved. **FIXED, same PR:** bind/unbind moved behind `ensurePlatformAdmin`; the picker GET stays at `approvalTemplateAdminGuard` per §2.5's own explicit authorization ("behind the same template-admin guard as the shipped user/role lookups") — only the WRITE path changed. This is the fix that makes the erratum above's "corrected reading" (curation is the real boundary) actually TRUE; before it, "curated" was aspirational text with no enforcement. Mutation-verified: neutering the new guard reds exactly the 3 tests that depend on it (2 negatives + 1 source-shape assertion) in `approval-directory-endpoints.api.test.ts`, restored byte-identical via `cp`+sha256. | goal-set instruction (2026-08-18); independent adversarial gate finding P1 on PR #4995, fixed same PR |
